---
phase: 05-scoring-parity-testing
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - get-scouted-be/scoring/tests/test_parity_edge_cases.py
autonomous: true
requirements: [SCORE-06]

must_haves:
  truths:
    - "The zero-minutes player (Minutes_played == 0) is scored by the port and matches the oracle — the regression guard for Phase 3's silent-zero-fill fix."
    - "Players missing key input stats (e.g. null market_value / null role-fit inputs) return the oracle's value or correctly propagate null through the port, never a fabricated number."
    - "Boundary-age players (youngest with age>0 and oldest per the real data) are scored and match the oracle."
    - "The invalid main_position=='0' player has NaN rmm in BOTH the oracle and the port (both-null parity), and the port does not crash on it."
    - "Every edge case is mined dynamically from the current real data / oracle (not a hardcoded player_id), so it survives oracle regeneration."
  artifacts:
    - path: "get-scouted-be/scoring/tests/test_parity_edge_cases.py"
      provides: "Named real-data edge-case parity: zero-minutes, missing stats, boundary ages, invalid position"
      min_lines: 90
      contains: "Minutes_played"
  key_links:
    - from: "get-scouted-be/scoring/tests/test_parity_edge_cases.py"
      to: "scoring.services.rmm.get_rmm / scoring.services.summary.get_summary"
      via: "call real per-request services on the dynamically-mined edge player and diff vs oracle"
      pattern: "get_rmm\\(|get_summary\\("
    - from: "get-scouted-be/scoring/tests/test_parity_edge_cases.py"
      to: "scoring.tests._parity_helpers"
      via: "import compare_scalar / load_oracle_df"
      pattern: "from scoring.tests._parity_helpers import"
---

<objective>
Build the edge-case parity test: mine genuine edge-case players from the real 41,708-player dataset (zero-minute appearances, missing key stats, boundary ages, invalid position label), score them through the port, and assert each either matches the oracle or correctly propagates null — never fabricates a value and never crashes. Cases are discovered dynamically from the live data/oracle so the suite survives oracle regeneration.

Purpose: CONTEXT.md's locked edge-case decision — mine real edge cases, not synthetic fixtures. The zero-minutes case is specifically the regression guard for the silent-zero-fill bug Phase 3's curation already fixed once.
Output: `scoring/tests/test_parity_edge_cases.py`.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-scoring-parity-testing/05-CONTEXT.md
@.planning/phases/05-scoring-parity-testing/05-RESEARCH.md
@.planning/phases/05-scoring-parity-testing/05-VALIDATION.md

<critical_corrections_and_data_facts>
Verified live against the real dev DB / oracle (2026-07-24):
- Zero-minutes players are EXTREMELY rare: exactly 1 (`Minutes_played == 0`, an RW player) out of 41,708.
  Treat it as a single dynamically-found named-player regression test, NOT "pick N zero-minute players".
- `age == 0` appears for 22 players — a data-quality placeholder, NOT a real "0-year-old". Handle explicitly:
  the genuine "youngest" boundary test uses the youngest player with age > 0; the age==0 rows get their own
  "invalid/placeholder input -> port matches oracle and does not crash" assertion.
- Exactly 1 player has main_position == "0" (garbage). `normalise_position("0")` -> "0" (not one of the 10
  groups); `add_player_impact` has no branch for it so its rmm is NaN in the oracle. The port must ALSO
  return null rmm for it (both-null parity).
- TFM oracle column is LOG-scale; `get_financial_fit(...)["predicted_fee"]` is MONEY-scale. If an edge case
  checks TFM, convert the oracle value via `np.expm1` before comparing (rtol 0.001). But most edge cases here
  concern RMM / CS / TP null-propagation, which is where the silent-zero-fill risk lives.
</critical_corrections_and_data_facts>

<interfaces>
<!-- Shared helpers from Plan 01: -->
from scoring.tests._parity_helpers import load_oracle_df, compare_scalar, RMM_CS_TP_ATOL, TFM_RTOL

<!-- Reconstructed-column names (verified, scoring/characterization/reconstruct.py):
     Age -> "Age"; market_value -> "Market value"; Minutes_played -> "Minutes played" (aliased to "Minutes").
     ORM field names on players.models.Player: age, market_value, Minutes_played, main_position, club_id. -->
from players.models import Player
# e.g. Player.objects.filter(Minutes_played=0)  /  Player.objects.exclude(age=0).order_by("age").first()

<!-- Per-request services (call on the ONE mined edge player; memoize reconstruct/score like Plan 03): -->
from scoring.services.rmm import get_rmm                    # get_rmm(pid) -> {"rmm": float|None, ...}
from scoring.services.summary import get_summary            # get_summary(pid, club_id) -> nested dict
# own club: own_club_id = Player.objects.get(id=pid).club_id  (may be None for club-less players)

<!-- Oracle lookup by player_id (load_oracle_df returns a df indexed by player_id with rmm/cs/tfm/transfer_probability): -->
oracle = load_oracle_df(); row = oracle.loc[str(pid)]   # row["rmm"], row["cs"], row["tfm"], row["transfer_probability"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zero-minutes + missing-stats edge cases (dynamically mined, parity vs oracle)</name>
  <files>get-scouted-be/scoring/tests/test_parity_edge_cases.py</files>
  <read_first>
    - get-scouted-be/scoring/characterization/reconstruct.py (the "Minutes" fallback logic + column names, esp. lines ~197-201)
    - get-scouted-be/scoring/docs/CURATION_MAP.md (the documented silent-zero-fill fix this test guards)
    - get-scouted-be/scoring/services/rmm.py + summary.py (return dict shapes)
    - get-scouted-be/scoring/tests/test_views.py (memoization pattern to reuse for the service calls)
    - get-scouted-be/scoring/tests/_parity_helpers.py (load_oracle_df, compare_scalar, tolerances)
    - get-scouted-be/scoring/tests/conftest.py (real_data_available)
  </read_first>
  <action>
Create `get-scouted-be/scoring/tests/test_parity_edge_cases.py`. Gate the module on real data (module-scope skip via `django_db_blocker.unblock()` as in Plans 02/03, because case-mining queries the DB). Reuse the memoization cache pattern from test_views.py so repeated service calls do not each pay reconstruction cost.

`test_zero_minutes_player_parity`:
- Mine dynamically: `p = Player.objects.filter(Minutes_played=0).first()`. If None, `pytest.skip("no zero-minute player in current data")` (survives future data where none exists).
- Call `get_rmm(p.id)` (memoized reconstruct/score). Assert it does NOT raise and returns a dict with an `"rmm"` key.
- Assert parity: `assert compare_scalar(oracle.loc[str(p.id), "rmm"], get_rmm(p.id)["rmm"], atol=RMM_CS_TP_ATOL)`. This proves the port reproduces the oracle's handling of a zero-appearance player rather than silently zero-filling (the Phase-3 fix's regression guard). Add a comment naming this as that guard.

`test_missing_stats_players_parity` (parametrized over 2-3 mined players):
- Mine players missing a key input: e.g. `Player.objects.filter(market_value__isnull=True).first()` (drives TFM/value) and `Player.objects.filter(club__isnull=True).first()` (drives CS null propagation — MANIFEST lists `no_club` as an exclusion reason). Skip any that don't exist.
- For each, assert the port matches the oracle for whichever score their missing input affects, using compare_scalar with the both-null semantics — i.e. where the oracle is null for that player, the port must ALSO be null (via get_rmm / get_summary), and where non-null, values match within tolerance. Do NOT assert a specific magic value; assert PARITY with the oracle row.

All comparisons via `compare_scalar` from the shared helper only.
  </action>
  <verify>
    <automated>cd get-scouted-be && pytest scoring/tests/test_parity_edge_cases.py -k "zero_minutes or missing_stats" -x -q</automated>
  </verify>
  <done>The zero-minute player and the missing-stat players are found dynamically, scored by the port without crashing, and their scores match the oracle (or correctly propagate null) within the locked tolerances; each case skips cleanly if no such player exists in the current data.</done>
  <acceptance_criteria>
    - `test_parity_edge_cases.py` contains `Minutes_played=0` (dynamic mining, not a hardcoded player_id)
    - `test_parity_edge_cases.py` contains `def test_zero_minutes_player_parity` and references it as the silent-zero-fill regression guard in a comment
    - `test_parity_edge_cases.py` mines missing-stat players via `market_value__isnull=True` and/or `club__isnull=True` and asserts oracle parity (not a magic constant)
    - All score comparisons use `compare_scalar` from `_parity_helpers` (no hand-rolled isclose)
    - Each edge test `pytest.skip`s cleanly when no matching player exists (survives oracle/data regeneration)
    - `pytest scoring/tests/test_parity_edge_cases.py -k "zero_minutes or missing_stats" -x -q` exits 0 (real-data DB) or skips cleanly on empty DB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Boundary-age + invalid-position edge cases (dynamically mined, parity vs oracle)</name>
  <files>get-scouted-be/scoring/tests/test_parity_edge_cases.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/test_parity_edge_cases.py (Task 1 file — append; reuse its memoization cache + oracle load)
    - get-scouted-be/scoring/characterization/impact.py (normalise_position + that "0" has no calculator branch -> NaN rmm)
    - get-scouted-be/scoring/tests/_parity_helpers.py (compare_scalar, load_oracle_df)
  </read_first>
  <action>
Append boundary-age and invalid-position edge cases to `get-scouted-be/scoring/tests/test_parity_edge_cases.py`.

`test_boundary_age_players_parity` (parametrized over the youngest-with-age>0 and the oldest player):
- Mine dynamically, EXCLUDING the age==0 placeholder for the genuine "youngest" case:
  `youngest = Player.objects.exclude(age=0).filter(age__isnull=False).order_by("age").first()`
  `oldest = Player.objects.filter(age__isnull=False).order_by("-age").first()`
- For each, assert `compare_scalar(oracle.loc[str(p.id), "rmm"], get_rmm(p.id)["rmm"], atol=RMM_CS_TP_ATOL)` (port reproduces the oracle at the age extremes; RMM is the age-sensitive score here). Skip cleanly if None.

`test_age_zero_placeholder_parity`:
- Mine `p = Player.objects.filter(age=0).first()`. If None, skip.
- Assert the port does NOT crash on this placeholder input and its rmm matches the oracle (`compare_scalar(..., atol=RMM_CS_TP_ATOL)`) — this documents the conscious choice (per 05-RESEARCH Open Question 2) that age==0 is treated as an invalid-placeholder input whose port behavior must still equal the oracle's, not silently diverge.

`test_invalid_position_player_parity`:
- Mine `p = Player.objects.filter(main_position="0").first()`. If None, skip.
- Assert BOTH the oracle and the port report NULL rmm for this player (both-null parity): `assert compare_scalar(oracle.loc[str(p.id), "rmm"], get_rmm(p.id)["rmm"], atol=RMM_CS_TP_ATOL)` where the oracle value is NaN — compare_scalar's both-null branch makes this a pass ONLY if the port is also null, and a hard failure if the port fabricated a number for an unrecognized position. Assert additionally that `get_rmm(p.id)` did not raise (non-crash on garbage input).

All comparisons via the shared `compare_scalar`.
  </action>
  <verify>
    <automated>cd get-scouted-be && pytest scoring/tests/test_parity_edge_cases.py -k "boundary_age or age_zero or invalid_position" -x -q</automated>
  </verify>
  <done>Youngest (age>0) and oldest players match the oracle; the age==0 placeholder and the main_position=="0" garbage row are scored without crashing and match the oracle (both-null for the invalid position), each mined dynamically and skipping cleanly when absent.</done>
  <acceptance_criteria>
    - `test_parity_edge_cases.py` contains `exclude(age=0)` for the genuine youngest-boundary selection
    - `test_parity_edge_cases.py` contains `main_position="0"` invalid-position mining and asserts both-null rmm parity via compare_scalar
    - `test_parity_edge_cases.py` contains `def test_age_zero_placeholder_parity` documenting the conscious age==0 handling choice
    - All boundary/invalid tests assert oracle PARITY (compare_scalar), not hardcoded score values, and skip cleanly when no matching player exists
    - `pytest scoring/tests/test_parity_edge_cases.py -k "boundary_age or age_zero or invalid_position" -x -q` exits 0 (real-data DB) or skips cleanly on empty DB
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd get-scouted-be && pytest scoring/tests/test_parity_edge_cases.py -q` runs all named edge-case parity tests green against the real dev DB (or skips cleanly on empty DB / when a given edge case has no matching player in the current data).
</verification>

<success_criteria>
- Every locked edge-case category (missing stats, boundary ages, zero-appearance) is covered by an explicit test asserting oracle parity / correct null propagation, plus the invalid-position bonus case.
- Cases are mined dynamically from real data (not hardcoded ids), so re-running after an oracle regeneration needs no test edits.
- The zero-minutes case explicitly guards Phase 3's silent-zero-fill fix from regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-parity-testing/05-04-SUMMARY.md`
</output>

---
phase: 05-scoring-parity-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-scouted-be/scoring/tests/_parity_helpers.py
  - get-scouted-be/scoring/tests/test_oracle_snapshot.py
autonomous: true
requirements: [SCORE-06]

must_haves:
  truths:
    - "A single shared helper discovers the latest oracle CSV by glob (never a hardcoded filename), so the whole parity flow is repeatable after oracle regeneration."
    - "A single shared helper loads the oracle into a DataFrame with a canonical 10-group position_group column derived via the port's own normalise_position()."
    - "A single shared helper casts a UUID-object-dtype port index/column to string so port scores align with the oracle's string player_id index (the load-bearing .astype(str) convention every Phase 4 service already follows)."
    - "A single shared comparator treats both-null as a pass and one-null-one-not as a hard failure, applies abs tol 0.01 to RMM/CS/TP and rel tol 0.1% (on money scale) to TFM."
    - "A single shared writer emits a per-group mismatch-detail report (player_id, score, oracle, port, diff) for any failing comparison."
    - "test_oracle_snapshot.py imports the shared oracle-discovery helper (one source of truth, no duplicated glob) and still passes."
  artifacts:
    - path: "get-scouted-be/scoring/tests/_parity_helpers.py"
      provides: "Oracle discovery, oracle-load-with-position-group, UUID->str index normalizer, tolerance comparator (both-null aware), mismatch-report writer"
      min_lines: 90
      exports: ["find_latest_oracle_csv", "load_oracle_df", "to_str_index", "POSITION_GROUPS", "GROUPS_WITH_NULL_CS_TP", "compare_series", "compare_tfm_series", "compare_scalar", "write_mismatch_report", "RMM_CS_TP_ATOL", "TFM_RTOL"]
    - path: "get-scouted-be/scoring/tests/test_oracle_snapshot.py"
      provides: "Existing oracle-snapshot test refactored to import the shared discovery helper"
      contains: "from scoring.tests._parity_helpers import"
  key_links:
    - from: "get-scouted-be/scoring/tests/_parity_helpers.py"
      to: "scoring.characterization.impact.normalise_position"
      via: "import + apply on oracle main_position column"
      pattern: "from scoring.characterization.impact import normalise_position"
    - from: "get-scouted-be/scoring/tests/test_oracle_snapshot.py"
      to: "scoring.tests._parity_helpers.find_latest_oracle_csv"
      via: "import replaces the local _find_latest_oracle_csv glob"
      pattern: "from scoring.tests._parity_helpers import"
---

<objective>
Build the shared test-helper module every Phase 5 parity file (bulk, API-sample, edge-cases) depends on: version-agnostic oracle discovery, oracle-load with the corrected 10-group position bucketing, a UUID->string index normalizer, a both-null-aware tolerance comparator, and a mismatch-report writer. This is the interface-first foundation — the three parity test files (Plans 02-04) import these contracts rather than each re-implementing glob/tolerance/reporting/id-casting logic.

Purpose: Give the three parallel Wave-2 parity plans one shared, correct comparison substrate so tolerance semantics, the UUID->str id-alignment convention, and the "both-null is a pass, one-null is a hard failure" invariant (CONTEXT.md locked decision) live in exactly one place.
Output: `scoring/tests/_parity_helpers.py` (new) plus a small refactor of `test_oracle_snapshot.py` to consume the shared discovery helper.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scoring-parity-testing/05-CONTEXT.md
@.planning/phases/05-scoring-parity-testing/05-RESEARCH.md
@.planning/phases/05-scoring-parity-testing/05-VALIDATION.md

<critical_corrections>
<!-- These OVERRIDE the research doc where they conflict. All verified live against the real oracle CSV / dev DB during planning (2026-07-24). -->

1. TFM ORACLE SCALE (research doc is WRONG here): The oracle CSV's `tfm` column is
   LOG-SCALE, range 13.11-17.09 (mean 14.46), NOT money-scale. `generate_scoring_oracle.py`
   line 206 writes the RAW `pipeline.predict(X)` output with NO `np.expm1`. Research
   Pattern 3 / Pitfall 2 wrongly claim the oracle is money-scale — do NOT follow that.
   CONTEXT.md's "TFM: relative tolerance 0.1% on money scale" is still honored by
   converting BOTH sides to money scale via `np.expm1(...)` before the relative comparison
   (np.expm1 is monotonic, so a 0.1% money-scale bound is well-defined and meaningful there).

2. POSITION GROUPS ARE 10, NOT 8 (research doc is correct, CONTEXT.md's working
   assumption was wrong): the 10 real groups from `normalise_position()` are exactly
   GK, CB, LB, RB, CM, DMF, AMF, LW, RW, CF. GK, LB, RB have ZERO non-null cs/transfer_probability
   across all 41,708 players (verified) — that is the correct current state, not a bug.
   One garbage row has main_position "0" (normalise_position("0") -> "0"); it is NOT one
   of the 10 groups and its rmm is NaN.

3. PLAYER_ID DTYPE MISMATCH (load-bearing — the port and the oracle key on the SAME id
   in DIFFERENT dtypes): `build_players_df()`'s `player_id` column holds `uuid.UUID`
   objects (dtype=object); the oracle CSV's `player_id`, loaded via `pd.read_csv`, is
   plain `str`. Every Phase 4 service (`rmm.py`, `compatibility.py`, `summary.py`,
   `financial_fit.py`, `transfer_probability.py`) already does `.astype(str)` on
   `player_id`/`cs_tp.index` before any lookup/merge against a string-keyed structure —
   this is an established, load-bearing codebase convention. Every port score Series
   MUST be string-indexed (via the `to_str_index()` helper below) before it is
   `.reindex()`-ed against / compared to the oracle, or every alignment silently returns
   NaN and the both-null-aware comparator reports a one-null hard failure for the entire
   population. The oracle side is already `str` from `pd.read_csv`; only the PORT side
   needs the cast.
</critical_corrections>

<interfaces>
<!-- From scoring/tests/test_oracle_snapshot.py (existing pattern to extract): -->
ORACLE_DIR = Path(__file__).resolve().parents[1] / "oracle"
def _find_latest_oracle_csv() -> Path | None:
    if not ORACLE_DIR.exists():
        return None
    candidates = sorted(ORACLE_DIR.glob("scoring_oracle_v1_*.csv"))
    return candidates[-1] if candidates else None

# Oracle CSV columns (verified, 41,708 rows):
#   player_id, player_name, main_position, rmm, cs, tfm, transfer_probability
# Null counts (verified live 2026-07-24): rmm 1 null; cs/transfer_probability 26,657 null; tfm 0 null.
# Oracle player_id dtype after pd.read_csv: str. Port player_id dtype from build_players_df: uuid.UUID (object).

# From scoring/characterization/impact.py:
from scoring.characterization.impact import normalise_position  # str -> one of GK/CB/LB/RB/CM/DMF/AMF/LW/RW/CF (or passthrough "0")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Oracle discovery + oracle-load-with-position-group + UUID->str index helpers</name>
  <files>get-scouted-be/scoring/tests/_parity_helpers.py, get-scouted-be/scoring/tests/test_oracle_snapshot.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/test_oracle_snapshot.py (current glob + column list to extract/reuse)
    - get-scouted-be/scoring/characterization/impact.py (the normalise_position signature — read the function only, do NOT copy its body)
    - get-scouted-be/scoring/services/financial_fit.py (the existing `.astype(str)` id-cast convention this plan centralizes into to_str_index)
    - get-scouted-be/scoring/tests/conftest.py (existing real_data_available fixture; do not change it)
  </read_first>
  <action>
Create `get-scouted-be/scoring/tests/_parity_helpers.py`. It is a plain importable module (NOT a conftest), used by the three parity test files.

Add these module-level constants (values verified live against the oracle 2026-07-24):
```python
POSITION_GROUPS = ["GK", "CB", "LB", "RB", "CM", "DMF", "AMF", "LW", "RW", "CF"]  # the 10 real normalise_position groups
GROUPS_WITH_NULL_CS_TP = {"GK", "LB", "RB"}  # structurally 100% null cs/transfer_probability across all 41,708 players
ORACLE_COLUMNS = ["player_id", "player_name", "main_position", "rmm", "cs", "tfm", "transfer_probability"]
```

Add `find_latest_oracle_csv() -> Path | None`: copy the EXACT glob from test_oracle_snapshot.py — `ORACLE_DIR = Path(__file__).resolve().parents[1] / "oracle"`, glob `"scoring_oracle_v1_*.csv"`, return `sorted(...)[-1]` or None. Never hardcode a filename.

Add `to_str_index(obj)` — the single, shared UUID->str normalizer that centralizes the load-bearing `.astype(str)` id convention every Phase 4 service already applies (see critical_corrections #3):
```python
def to_str_index(obj):
    """Return a Series/DataFrame whose index is cast to str, so a UUID-object-dtype
    port index aligns with the oracle's string player_id index. Mirrors the .astype(str)
    id-cast convention in rmm.py/compatibility.py/summary.py/financial_fit.py/
    transfer_probability.py — Plans 02/03/04 pass every port score Series through this
    BEFORE reindexing/comparing against the string-keyed oracle."""
    return obj.set_axis(obj.index.astype(str))
```
This is the ONLY place the port-side id cast is defined; parity files import it rather than sprinkling `.astype(str)`.

Add `load_oracle_df() -> pd.DataFrame`: call `find_latest_oracle_csv()`; if None, `pytest.skip("No oracle CSV in scoring/oracle/ -- run `python manage.py generate_scoring_oracle` first.")`. Read the CSV with `pd.read_csv(path)`. Add a `position_group` column: `df["position_group"] = df["main_position"].apply(normalise_position)` — importing `from scoring.characterization.impact import normalise_position` (do NOT hand-roll a mapping dict — single source of truth). Cast `df["player_id"] = df["player_id"].astype(str)` defensively (already str from read_csv, but explicit) and return the DataFrame indexed by `player_id` (set_index("player_id", drop=False)) so parity files can `.loc[str(player_id)]` cheaply.

Then refactor `get-scouted-be/scoring/tests/test_oracle_snapshot.py`: replace its local `_find_latest_oracle_csv` definition and the two call-sites with `from scoring.tests._parity_helpers import find_latest_oracle_csv` and calls to `find_latest_oracle_csv()`. Leave EVERYTHING else in that file (EXPECTED_COLUMNS, the two test functions, their assertions) byte-for-byte unchanged — this is a pure de-duplication, not a behavior change.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "from scoring.tests._parity_helpers import find_latest_oracle_csv, to_str_index, POSITION_GROUPS, GROUPS_WITH_NULL_CS_TP; import pandas as pd, uuid; s=pd.Series([1.0,2.0], index=[uuid.uuid4(), uuid.uuid4()]); assert to_str_index(s).index.map(type).eq(str).all(); assert len(POSITION_GROUPS)==10; assert GROUPS_WITH_NULL_CS_TP=={'GK','LB','RB'}; print('ok')"</automated>
    <automated>cd get-scouted-be && pytest scoring/tests/test_oracle_snapshot.py -x -q</automated>
  </verify>
  <done>_parity_helpers.py exists with find_latest_oracle_csv, load_oracle_df, to_str_index, the 3 constants; to_str_index turns a UUID-indexed Series into a str-indexed one; test_oracle_snapshot.py imports find_latest_oracle_csv from it and its suite still passes.</done>
  <acceptance_criteria>
    - `_parity_helpers.py` contains `POSITION_GROUPS = ["GK", "CB", "LB", "RB", "CM", "DMF", "AMF", "LW", "RW", "CF"]`
    - `_parity_helpers.py` contains `GROUPS_WITH_NULL_CS_TP = {"GK", "LB", "RB"}`
    - `_parity_helpers.py` contains `def find_latest_oracle_csv(`, `def load_oracle_df(`, and `def to_str_index(`
    - `to_str_index` casts the index to str (`.index.astype(str)`) so a UUID-object-dtype port index aligns with the oracle's string index
    - `_parity_helpers.py` contains `from scoring.characterization.impact import normalise_position` (no hand-rolled position dict)
    - `_parity_helpers.py` contains `scoring_oracle_v1_*.csv` (glob, not a hardcoded dated filename)
    - `test_oracle_snapshot.py` contains `from scoring.tests._parity_helpers import` and no longer contains `def _find_latest_oracle_csv(`
    - `pytest scoring/tests/test_oracle_snapshot.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Both-null-aware tolerance comparator + mismatch-report writer</name>
  <files>get-scouted-be/scoring/tests/_parity_helpers.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/_parity_helpers.py (the file created in Task 1 — append to it)
    - get-scouted-be/scoring/oracle/MANIFEST.md (per-score null semantics — NaN means "correctly abstained", never zero-fill)
    - .planning/phases/05-scoring-parity-testing/05-CONTEXT.md (the "Tolerance & missing-value policy" locked decision — abs 0.01 for RMM/CS/TP, rel 0.1% for TFM, null-mismatch is a HARD failure)
  </read_first>
  <action>
Append comparison + reporting helpers to `get-scouted-be/scoring/tests/_parity_helpers.py`. Import `numpy as np`, `pandas as pd`, `pytest`, and `from pathlib import Path`.

Add `compare_scalar(oracle_val, port_val, *, atol=None, rtol=None) -> bool` implementing the locked both-null policy EXACTLY:
```python
o_null = oracle_val is None or (isinstance(oracle_val, float) and np.isnan(oracle_val)) or pd.isna(oracle_val)
p_null = port_val is None or (isinstance(port_val, float) and np.isnan(port_val)) or pd.isna(port_val)
if o_null and p_null:
    return True          # both abstained -> pass
if o_null != p_null:
    return False         # one null, one not -> HARD failure (fabricated-or-dropped value)
o, p = float(oracle_val), float(port_val)
if atol is not None:
    return abs(o - p) <= atol
if rtol is not None:
    return abs(o - p) <= rtol * abs(o)
raise ValueError("compare_scalar requires exactly one of atol/rtol")
```

Add `compare_series(oracle: pd.Series, port: pd.Series, *, atol=None, rtol=None) -> pd.DataFrame`: align both series on their shared index (assume callers have already passed the port side through `to_str_index` so both indices are string — do NOT re-cast here, keep the id-normalization responsibility in one place), run `compare_scalar` per row, and return a DataFrame of ONLY the mismatched rows with columns `["player_id", "oracle", "port", "diff"]` (diff = `port - oracle` where both non-null, else the string "null_mismatch"). Empty DataFrame == all match. Do not raise here — the caller asserts on `len(...) == 0`.

Add a TFM-specific convenience `compare_tfm_series(oracle_log: pd.Series, port_money: pd.Series) -> pd.DataFrame`: convert the LOG-scale oracle column to money via `np.expm1(oracle_log)` and compare against the already-money-scale `port_money` with `rtol=0.001` (0.1%). Document in the docstring that the oracle `tfm` column is log-scale (13-17) and the per-request service's `predicted_fee` is money-scale — this helper is the single place that reconciles the two scales, so no test file re-derives the conversion.

Add tolerance constants: `RMM_CS_TP_ATOL = 0.01` and `TFM_RTOL = 0.001`.

Add `write_mismatch_report(name: str, mismatches: pd.DataFrame) -> Path`: write `mismatches` to `Path(__file__).resolve().parent / "_parity_reports" / f"{name}.csv"` (mkdir parents, exist_ok). Return the path. Called by a failing parity assertion so a developer gets a debuggable per-group CSV, not just a bare assert. If `mismatches` is empty, still return the path but write nothing / an empty file — callers only invoke it on failure.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "
from scoring.tests._parity_helpers import compare_scalar, RMM_CS_TP_ATOL, TFM_RTOL
import numpy as np
assert compare_scalar(50.00, 50.005, atol=0.01) is True
assert compare_scalar(50.00, 50.02, atol=0.01) is False
assert compare_scalar(float('nan'), None, atol=0.01) is True       # both-null pass
assert compare_scalar(50.0, None, atol=0.01) is False              # one-null hard fail
assert compare_scalar(None, 50.0, atol=0.01) is False
assert compare_scalar(1000.0, 1000.5, rtol=0.001) is True
assert compare_scalar(1000.0, 1002.0, rtol=0.001) is False
assert RMM_CS_TP_ATOL == 0.01 and TFM_RTOL == 0.001
print('ok')"</automated>
  </verify>
  <done>compare_scalar/compare_series/compare_tfm_series/write_mismatch_report and the tolerance constants exist; the both-null-pass and one-null-fail semantics are proven by the inline checks.</done>
  <acceptance_criteria>
    - `_parity_helpers.py` contains `def compare_scalar(`, `def compare_series(`, `def compare_tfm_series(`, `def write_mismatch_report(`
    - `_parity_helpers.py` contains `RMM_CS_TP_ATOL = 0.01` and `TFM_RTOL = 0.001`
    - `_parity_helpers.py` contains `np.expm1(` inside compare_tfm_series (money-scale reconciliation of the log-scale oracle column)
    - The Task-2 `<automated>` python check exits 0 (proves both-null pass, one-null hard-fail, abs and rel tolerances)
    - `write_mismatch_report` writes under a `_parity_reports/` directory beside the test files
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd get-scouted-be && pytest scoring/tests/test_oracle_snapshot.py -x -q` passes (existing suite unbroken by the refactor).
- `python -c "from scoring.tests._parity_helpers import find_latest_oracle_csv, load_oracle_df, to_str_index, compare_scalar, compare_series, compare_tfm_series, write_mismatch_report, POSITION_GROUPS, GROUPS_WITH_NULL_CS_TP, RMM_CS_TP_ATOL, TFM_RTOL"` imports cleanly.
</verification>

<success_criteria>
- One shared module owns oracle discovery (glob, version-agnostic), oracle load + 10-group bucketing, UUID->str id normalization, both-null-aware tolerance comparison, and mismatch reporting.
- No parity file (Plans 02-04) will need to re-implement any of these; they import from `_parity_helpers` (including the id-cast, so the UUID-vs-str alignment bug is fixed in exactly one place).
- The existing oracle-snapshot test consumes the shared discovery helper (single source of truth) and still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-parity-testing/05-01-SUMMARY.md`
</output>
</content>
</invoke>

---
phase: 01-data-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-scouted-be/clubs/models.py
  - get-scouted-be/clubs/migrations/0001_initial.py
  - get-scouted-be/players/models.py
  - get-scouted-be/players/migrations/0001_initial.py
  - get-scouted-be/transfers/models.py
  - get-scouted-be/transfers/migrations/0001_initial.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "`python manage.py makemigrations --check --dry-run` reports no missing migrations"
    - "`python manage.py migrate` creates all tables against Postgres without error"
    - "Every model that will be bulk-upserted has a REAL DB-level unique constraint (not just Meta.unique_together) for update_conflicts to target"
  artifacts:
    - path: "get-scouted-be/players/models.py"
      provides: "Player (unique_id unique, club FK, extended_stats JSONField, legacy_total_score), PlayerRoleScore, PlayerClubCompatibility"
      contains: "extended_stats"
    - path: "get-scouted-be/clubs/models.py"
      provides: "Club (name unique, league, 8 playing_style floats, nullable manager/formation/country)"
      contains: "playing_style"
    - path: "get-scouted-be/transfers/models.py"
      provides: "Transfer (club FK, dealing_club CharField, player FK nullable, composite unique constraint)"
      contains: "dealing_club"
  key_links:
    - from: "get-scouted-be/players/models.py"
      to: "clubs.Club"
      via: "Player.club = ForeignKey"
      pattern: "ForeignKey\\(.*Club"
    - from: "get-scouted-be/players/models.py"
      to: "PlayerClubCompatibility unique constraint"
      via: "UniqueConstraint(player, club_name_raw)"
      pattern: "club_name_raw"
---

<objective>
Define all five Phase 1 data models (Club, Player, PlayerRoleScore, PlayerClubCompatibility, Transfer) and generate their initial migrations. Field naming mirrors the CSV snake_case exactly (per CONTEXT.md locked decision) so Phase 5 parity testing traces 1:1 to impact_model_v4.1.py.

Purpose: Every import command writes into these tables. The unique-constraint choices here are load-bearing for idempotent re-runs: `bulk_create(update_conflicts=True)` requires a REAL database unique constraint on the conflict target, and the compatibility table specifically must key on `(player, club_name_raw)` — NOT `(player, club)` — because `club` is null for unresolved headers and Postgres treats multiple NULLs as non-conflicting (RESEARCH.md Open Question 2).
Output: Three models.py files + three 0001_initial migrations that apply cleanly.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@get-scouted-be/docs/FIELD_MAPPING.md

<interfaces>
<!-- Column sources for each model. Full per-field detail is in FIELD_MAPPING.md (read it). -->
Player: unique_id (from UniqueID, unique=True), player (name), season, club FK (from Team_within_selected_timeframe), plus ~121 stat columns mirroring CSV snake_case, legacy_total_score (from Total_Score), extended_stats JSONField (14 movement columns keyed by raw CSV name).
Club: name (unique), league, country/manager/formation (nullable), 8 style floats: control_possession, gegenpressing, direct_play, defensive_counter_attack, tiki_taka, counter_attack, wing_play, low_block.
PlayerRoleScore: player FK, position_group, role_name (sanitized), role_name_raw, score. Unique (player, role_name).
PlayerClubCompatibility: player FK, club FK (nullable), club_name_raw, position_group, score. Unique (player, club_name_raw).
Transfer: club FK, dealing_club (CharField), player FK (nullable), player_name_raw, plus fee/market_value/movement/window/year/is_loan/etc. Unique (player_name_raw, year, window, movement, club, dealing_club).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Club, PlayerRoleScore, and PlayerClubCompatibility models</name>
  <files>get-scouted-be/clubs/models.py, get-scouted-be/players/models.py (PlayerRoleScore + PlayerClubCompatibility portions)</files>
  <read_first>
    - get-scouted-be/docs/FIELD_MAPPING.md (section 3 Club fields; role + compatibility shapes)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (sections "Club Model & Derivation", "PlayerRoleScore Model", "PlayerClubCompatibility Model", and Open Question 2)
    - API-Updated-/dataset/Playstyles.csv (header — confirm the 8 style column names)
  </read_first>
  <action>
    clubs/models.py — `class Club(models.Model)`:
    - `name = models.CharField(max_length=255, unique=True)` (REAL unique constraint — bulk upsert target)
    - `league = models.CharField(max_length=255, null=True, blank=True)`
    - `country = models.CharField(max_length=255, null=True, blank=True)`
    - `manager = models.CharField(max_length=255, null=True, blank=True)`
    - `formation = models.CharField(max_length=50, null=True, blank=True)`
    - `source_unique_id = models.IntegerField(null=True, blank=True)` (the transferdata/Playstyles club-scoped UniqueID, for cross-source confirmation — NOT a player id)
    - 8 nullable FloatFields: `control_possession, gegenpressing, direct_play, defensive_counter_attack, tiki_taka, counter_attack, wing_play, low_block` (all `null=True, blank=True`)
    - `__str__` returns name. No squad_size field (live count, Phase 7/11).

    players/models.py — add `class PlayerRoleScore(models.Model)`:
    - `player = models.ForeignKey('players.Player', on_delete=models.CASCADE, related_name='role_scores')`
    - `position_group = models.CharField(max_length=8)` (e.g. "CB","AM")
    - `role_name = models.CharField(max_length=100)` (sanitized, e.g. "wide_centre_back_lcb")
    - `role_name_raw = models.CharField(max_length=150)` (original header, e.g. "Wide_Centre-Back_(LCB)")
    - `score = models.FloatField(null=True, blank=True)`
    - `Meta`: `constraints = [models.UniqueConstraint(fields=['player','role_name'], name='uniq_player_role')]`

    players/models.py — add `class PlayerClubCompatibility(models.Model)`:
    - `player = models.ForeignKey('players.Player', on_delete=models.CASCADE, related_name='compatibilities')`
    - `club = models.ForeignKey('clubs.Club', on_delete=models.SET_NULL, null=True, blank=True, related_name='compatibilities')`
    - `club_name_raw = models.CharField(max_length=255)` (always populated from the CSV column header — the idempotency key)
    - `position_group = models.CharField(max_length=8)`
    - `score = models.FloatField(null=True, blank=True)`
    - `Meta`: `constraints = [models.UniqueConstraint(fields=['player','club_name_raw'], name='uniq_player_clubname')]`, plus `indexes = [models.Index(fields=['club','-score']), models.Index(fields=['player','-score'])]` (both query directions for Phase 11/12).

    Note: Player class itself is defined in Task 2; PlayerRoleScore/PlayerClubCompatibility reference it via the string 'players.Player' so ordering within the file does not matter.
  </action>
  <verify>
    <automated>cd get-scouted-be && python manage.py makemigrations clubs --dry-run 2>&1 | grep -qi "Create model Club" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - clubs/models.py contains `class Club` with `name = models.CharField(max_length=255, unique=True)` and all 8 style fields incl `control_possession` and `low_block`
    - players/models.py contains `class PlayerRoleScore` with `UniqueConstraint(fields=['player','role_name']`
    - players/models.py contains `class PlayerClubCompatibility` with `club_name_raw` and `UniqueConstraint(fields=['player','club_name_raw']`
    - PlayerClubCompatibility has an Index on `['club','-score']` AND `['player','-score']`
    - `python manage.py makemigrations --dry-run` shows Club/PlayerRoleScore/PlayerClubCompatibility creation
  </acceptance_criteria>
  <done>Club, PlayerRoleScore, and PlayerClubCompatibility models exist with real DB-level unique constraints (Club.name unique; player+role_name; player+club_name_raw) and the dual score indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Define Player and Transfer models + generate and apply all migrations</name>
  <files>get-scouted-be/players/models.py (Player portion), get-scouted-be/transfers/models.py, get-scouted-be/clubs/migrations/0001_initial.py, get-scouted-be/players/migrations/0001_initial.py, get-scouted-be/transfers/migrations/0001_initial.py</files>
  <read_first>
    - get-scouted-be/docs/FIELD_MAPPING.md (section 1 Player fields, section 2 extended_stats, section 4 Transfer fields)
    - API-Updated-/dataset/Players.csv (header — the 135 columns to mirror as snake_case fields)
    - API-Updated-/dataset/transferdata final.csv (header — 17 columns)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (sections "Player Model", "Transfer Model — Critical Pitfall")
  </read_first>
  <action>
    players/models.py — `class Player(models.Model)`:
    - `unique_id = models.IntegerField(unique=True)` (REAL unique constraint — Player bulk-upsert target; from CSV UniqueID)
    - `player = models.CharField(max_length=255)` (name), `season = models.CharField(max_length=32, null=True, blank=True)`
    - `club = models.ForeignKey('clubs.Club', on_delete=models.SET_NULL, null=True, blank=True, related_name='players')` (from Team_within_selected_timeframe)
    - `legacy_total_score = models.FloatField(null=True, blank=True)` (from Total_Score — OLD system, NOT RMM)
    - Profile fields: `league, positions, main_position, position` (CharField null=True); `age` (IntegerField null=True), `market_value` (BigIntegerField null=True), `contract_expires` (DateField null=True), `birth_country, passport_country, foot` (CharField null=True), `height, weight` (IntegerField null=True), `on_loan` (BooleanField null=True).
    - The ~100 numeric per-90/percentage stat columns from the CSV header: mirror each as a `FloatField(null=True, blank=True)` using the EXACT CSV snake_case name lowercased-as-written (keep the CSV casing where it is already a valid identifier, e.g. `xG_per_90`, `Duels_per_90`, `Successful_defensive_actions_per_90`, `PAdj_Interceptions`). Do NOT invent names — copy them 1:1 from the header per FIELD_MAPPING.md.
    - `extended_stats = models.JSONField(default=dict, blank=True)` — holds the 14 non-identifier movement columns keyed by original CSV name (see FIELD_MAPPING section 2). Do NOT create individual fields for those 14.
    - `Meta`: index on `position`, `age`, `market_value`, `club` (baseline filter fields; precise composite indexes deferred to Phase 7).

    transfers/models.py — `class Transfer(models.Model)`:
    - `club = models.ForeignKey('clubs.Club', on_delete=models.SET_NULL, null=True, blank=True, related_name='transfers')` (from transferdata.Club, resolved via the club-scoped UniqueID/Club pair — NEVER Player.UniqueID)
    - `source_unique_id = models.IntegerField(null=True, blank=True)` (transferdata.UniqueID — a CLUB id, captured for confirmation only)
    - `player = models.ForeignKey('players.Player', on_delete=models.SET_NULL, null=True, blank=True, related_name='transfers')` (best-effort name match, nullable)
    - `player_name_raw = models.CharField(max_length=255)` (transferdata.Player string — always populated, part of the idempotency key)
    - `age_at_transfer = models.IntegerField(null=True, blank=True)` (from Age — NOT current age), `nationality, position, short_position` (CharField null=True)
    - `market_value_at_transfer = models.BigIntegerField(null=True, blank=True)` (from Market_Value)
    - `dealing_club = models.CharField(max_length=255)` (plain string, NOT a FK — 2,909 counterparties), `dealing_country` (CharField null=True)
    - `fee = models.CharField(max_length=100, null=True, blank=True)` (fees are non-numeric strings like "Free"/"loan"; keep raw), `movement` (CharField), `window` (CharField), `league_name` (CharField null=True), `year` (IntegerField), `is_loan` (BooleanField null=True), `loan_status` (CharField null=True)
    - `Meta`: `constraints = [models.UniqueConstraint(fields=['player_name_raw','year','window','movement','club','dealing_club'], name='uniq_transfer_event')]` (all six verified always-populated), index on `club`.

    Then run `python manage.py makemigrations clubs players transfers` and `python manage.py migrate`. Commit the generated 0001_initial.py files.
  </action>
  <verify>
    <automated>cd get-scouted-be && python manage.py makemigrations --check --dry-run && python manage.py migrate --run-syncdb 2>&1 | tail -1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - players/models.py contains `class Player` with `unique_id = models.IntegerField(unique=True)` AND `legacy_total_score` AND `extended_stats = models.JSONField` AND `club = models.ForeignKey('clubs.Club'`
    - players/models.py mirrors CSV stat names 1:1 (grep contains `Successful_defensive_actions_per_90` AND `xG_per_90` AND `PAdj_Interceptions`)
    - transfers/models.py contains `dealing_club = models.CharField` (NOT ForeignKey) AND `UniqueConstraint(fields=['player_name_raw','year','window','movement','club','dealing_club']`
    - `python manage.py makemigrations --check --dry-run` exits 0 (no missing migrations)
    - `python manage.py migrate` applies all migrations without error
  </acceptance_criteria>
  <done>Player and Transfer models exist with CSV-mirrored field names, real unique constraints for idempotent upsert, extended_stats JSONField, and dealing_club as a plain string; all migrations generate and apply cleanly against Postgres.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && python manage.py makemigrations --check --dry-run` exits 0
- `python manage.py migrate` succeeds
- `grep -q "extended_stats = models.JSONField" get-scouted-be/players/models.py`
- `grep -q "uniq_player_clubname" get-scouted-be/players/models.py`
- `grep -q "dealing_club = models.CharField" get-scouted-be/transfers/models.py`
</verification>

<success_criteria>
- Five models exist with field names mirroring the CSVs 1:1 for parity traceability
- Real DB unique constraints exist on every bulk-upsert target (Player.unique_id, Club.name, player+role_name, player+club_name_raw, the transfer composite)
- Migrations apply cleanly against Postgres
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>

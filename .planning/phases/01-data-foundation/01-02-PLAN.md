---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-scouted-be/requirements/dev.txt
  - get-scouted-be/pyproject.toml
  - get-scouted-be/core/tests/__init__.py
  - get-scouted-be/core/tests/conftest.py
  - get-scouted-be/core/tests/fixtures/players_sample.csv
  - get-scouted-be/core/tests/fixtures/playstyles_sample.csv
  - get-scouted-be/core/tests/fixtures/positions_CB_sample.csv
  - get-scouted-be/core/tests/fixtures/compatibility_CB_sample.csv
  - get-scouted-be/core/tests/fixtures/transferdata_sample.csv
  - get-scouted-be/core/tests/fixtures/README.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-04, DATA-05]

must_haves:
  truths:
    - "`pytest --collect-only` runs against the get-scouted-be project without configuration errors"
    - "Small fixture CSVs exist for every source file, each seeded with the known data-quality edge cases"
    - "The transferdata fixture contains a row that would break if UniqueID were treated as a player id (the trap case)"
  artifacts:
    - path: "get-scouted-be/pyproject.toml"
      provides: "pytest-django configuration (DJANGO_SETTINGS_MODULE, testpaths, markers)"
      contains: "DJANGO_SETTINGS_MODULE"
    - path: "get-scouted-be/core/tests/conftest.py"
      provides: "Shared Postgres test-DB fixtures and a fixture_dir path helper"
      contains: "fixture_dir"
    - path: "get-scouted-be/core/tests/fixtures/players_sample.csv"
      provides: "10-20 representative player rows incl. missing Market_value, dirty Foot ('0'/'unknown'), ambiguous club league"
      contains: "Team_within_selected_timeframe"
  key_links:
    - from: "get-scouted-be/pyproject.toml"
      to: "config.settings.local"
      via: "pytest-django DJANGO_SETTINGS_MODULE"
      pattern: "DJANGO_SETTINGS_MODULE"
---

<objective>
Install and configure the test framework (pytest + pytest-django + factory_boy) and author the small fixture CSVs every import test will run against. This is the Phase 1 "Wave 0" test-infrastructure prerequisite from 01-VALIDATION.md — it must exist before any import task (which are all test-verified).

Purpose: The real CSVs (41.7K players, 8.1M compatibility values) are far too large for per-commit unit tests. Every import task's fast feedback loop (`pytest -x -q -k "not integration"`) runs against these tiny, hand-seeded fixtures that deliberately contain one row per known data-quality trap so the tests actually exercise the flagging logic.
Output: pytest configured against config.settings.local, conftest.py with DB + fixture-path fixtures, and five seeded fixture CSVs.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Real headers to mirror in the fixture CSVs (copy the exact column order). -->
Players.csv header starts: Season,UniqueID,Total_Score,League,Player,Team,Team_within_selected_timeframe,Positions,Main_Position,Position,Age,Market_value,Contract_expires,Matches_played,Minutes_played,Goals,...  (135 cols total)
Playstyles.csv header: Team,UniqueID,Control_Possession,Gegenpressing,Direct_Play,Defensive_Counter_Attack,Tiki_Taka,Counter_Attack,Wing_Play,Low_Block
Positions/CB with league.csv header: UniqueID,Wide_Centre-Back_(LCB),No_Nonsense_Centre_Back,Ball_Playing_Defender,Wide_Centre_Back_(RCB),Libero
Compatability Scores/CS_CB_25.csv header: UniqueID,Position,<212 club-name columns...>  (214 cols total)
transferdata final.csv header: UniqueID,Club,Player,Age,Nationality,Position,Short_Position,Market_Value,Dealing_Club,Dealing_Country,Fee,Movement,Window,League_Name,Year,is_loan,loan_status
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure pytest-django</name>
  <files>get-scouted-be/requirements/dev.txt, get-scouted-be/pyproject.toml, get-scouted-be/core/tests/__init__.py, get-scouted-be/core/tests/conftest.py</files>
  <read_first>
    - get-scouted-be/config/settings/base.py (confirm settings module path + DATASET_DIR name)
    - .planning/phases/01-data-foundation/01-VALIDATION.md (section "Wave 0 Requirements" + "Test Infrastructure")
    - .planning/research/STACK.md (pytest + pytest-django recommendation)
  </read_first>
  <action>
    requirements/dev.txt:
    ```
    -r base.txt
    pytest>=8.0
    pytest-django>=4.9
    factory_boy>=3.3
    ```

    pyproject.toml — add a `[tool.pytest.ini_options]` table with EXACTLY:
    ```
    [tool.pytest.ini_options]
    DJANGO_SETTINGS_MODULE = "config.settings.local"
    python_files = ["test_*.py"]
    testpaths = ["clubs", "players", "transfers", "core"]
    markers = ["integration: tests that run against real/large CSV samples (slower)"]
    addopts = "-ra"
    ```

    core/tests/__init__.py: empty.
    core/tests/conftest.py must define:
    - `import pytest`, `from pathlib import Path`
    - `@pytest.fixture def fixture_dir(): return Path(__file__).resolve().parent / "fixtures"` — the canonical path every import test uses to locate its sample CSV.
    - `@pytest.fixture def enable_db_access(db): pass` is unnecessary; instead document in a module docstring that tests needing the DB use the `@pytest.mark.django_db` marker (pytest-django provides `db`).
    - Do NOT hardcode any absolute path; derive everything from `__file__`.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest --collect-only -q 2>&1 | grep -qiv "error collecting" && grep -q "DJANGO_SETTINGS_MODULE" pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - get-scouted-be/requirements/dev.txt contains `pytest-django` AND `factory_boy` AND `-r base.txt`
    - get-scouted-be/pyproject.toml contains `[tool.pytest.ini_options]` AND `DJANGO_SETTINGS_MODULE = "config.settings.local"` AND `integration:` marker
    - get-scouted-be/core/tests/conftest.py contains `fixture_dir` and derives its path from `__file__` (no absolute path literal)
    - `pytest --collect-only` runs without a collection/configuration error
  </acceptance_criteria>
  <done>pytest-django is installed and configured against config.settings.local; a shared `fixture_dir` fixture exists; `pytest --collect-only` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Author seeded fixture CSVs for every source file</name>
  <files>get-scouted-be/core/tests/fixtures/players_sample.csv, get-scouted-be/core/tests/fixtures/playstyles_sample.csv, get-scouted-be/core/tests/fixtures/positions_CB_sample.csv, get-scouted-be/core/tests/fixtures/compatibility_CB_sample.csv, get-scouted-be/core/tests/fixtures/transferdata_sample.csv, get-scouted-be/core/tests/fixtures/README.md</files>
  <read_first>
    - API-Updated-/dataset/Players.csv (copy the exact 135-column header row)
    - API-Updated-/dataset/Playstyles.csv (exact header)
    - API-Updated-/dataset/Positions/CB with league.csv (exact header)
    - API-Updated-/dataset/Compatability Scores/CS_CB_25.csv (exact header — 214 cols)
    - API-Updated-/dataset/transferdata final.csv (exact header)
    - .planning/phases/01-data-foundation/01-VALIDATION.md (Wave 0 Requirements — the exact data-quality cases each fixture must include)
  </read_first>
  <action>
    Create tiny CSVs (each begins with the EXACT header copied from the real file, then 10-20 rows). Seed the specific edge cases below so import tests can assert on them.

    players_sample.csv (~15 rows) MUST include:
    - At least 3 clubs. One club ("Manchester City") appears in multiple rows with DIFFERENT League values (e.g. 3 rows "Premier League (England)", 1 row "La Liga (Spain)") so the mode-derivation + tie-break logic is exercised.
    - One row with blank Market_value, one row with Market_value = 0.
    - One row with blank Contract_expires; one row with Contract_expires = "30/06/2027" (DD/MM/YYYY).
    - One row with Foot = "0"; one row with Foot = "unknown"; the rest with "right"/"left".
    - One GK row (Position=GK) — used to assert GKs get zero role/compatibility rows by design.
    - At least 2 non-GK CB rows whose UniqueIDs also appear in positions_CB_sample.csv and compatibility_CB_sample.csv.
    - Fill unused stat columns with plausible numbers or blanks; only the columns named above must carry specific values. Every UniqueID must be unique.

    playstyles_sample.csv: rows for only SOME of the players' clubs (e.g. 2 of the 3 clubs) so the ~77% playing-style-null coverage case is exercised (some clubs have style, some do not). Use real 0-1 style floats.

    positions_CB_sample.csv: one row per CB UniqueID present in players_sample.csv, with the 5 CB role float columns populated.

    compatibility_CB_sample.csv: header = UniqueID,Position, then a SMALL set of club columns (~6, not 212) including at least one club name that matches a Club in players_sample.csv exactly, and at least one deliberately-unresolvable header (e.g. "St_DOT_ Louis City") that will NOT match any Club — to exercise the club=null / club_name_raw fallback. One row per CB UniqueID.

    transferdata_sample.csv (THE TRAP FIXTURE): ~12 rows. Use UniqueID values that are CLUB ids (e.g. 12 -> "Atalanta"): make several rows share the SAME UniqueID but DIFFERENT Player names and the same Club — proving UniqueID is club-scoped. Critically, pick a UniqueID value that ALSO exists as a genuine player UniqueID in players_sample.csv but maps to a DIFFERENT player — so a test can assert the importer did NOT link the transfer to that player. Include both Movement="arrival" and "departure" rows, a Dealing_Club counterparty NOT present as any Club, populated Year/Window/Movement/Fee, and one is_loan="TRUE" row.

    README.md: a table listing each fixture file and exactly which edge case(s) each seeded row demonstrates, so a downstream test author knows which row to assert against.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "import csv,collections; r=list(csv.DictReader(open('core/tests/fixtures/transferdata_sample.csv'))); c=collections.Counter(x['UniqueID'] for x in r); assert max(c.values())>1, 'need a repeated club-scoped UniqueID'; print('trap fixture OK', dict(c))"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 fixture CSVs exist and each first line equals the real file's header (verify players_sample.csv header contains `Team_within_selected_timeframe` and `Contract_expires`)
    - players_sample.csv contains a row with `,0,` style blank/zero Market_value and a Foot value of `unknown` (grep `unknown`) and `Manchester City` appearing on >1 row
    - compatibility_CB_sample.csv header contains `UniqueID,Position` and at least one unresolvable club header (grep for `_DOT_` OR a header known absent from players_sample.csv)
    - transferdata_sample.csv has at least one UniqueID value appearing on 2+ rows with different Player names (the trap) — the python assertion in verify exits 0
    - fixtures/README.md documents which row demonstrates which edge case
  </acceptance_criteria>
  <done>Five seeded fixture CSVs exist mirroring the real headers, each carrying the known data-quality traps (missing/zero Market_value, dirty Foot, ambiguous league, unresolvable compatibility header, and the transferdata club-scoped-UniqueID trap), documented in a README.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && pytest --collect-only -q` runs without configuration error
- `grep -q "DJANGO_SETTINGS_MODULE" get-scouted-be/pyproject.toml`
- All 5 fixture CSVs present under get-scouted-be/core/tests/fixtures/
- Trap fixture assertion (repeated club-scoped UniqueID) passes
</verification>

<success_criteria>
- pytest + pytest-django + factory_boy installed and wired to config.settings.local
- Fixture CSVs seeded with every known data-quality case, ready for import tests to assert against
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>

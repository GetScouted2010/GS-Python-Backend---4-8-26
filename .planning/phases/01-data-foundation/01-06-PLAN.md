---
phase: 01-data-foundation
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - get-scouted-be/players/management/commands/import_position_roles.py
  - get-scouted-be/players/tests/test_import.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "Importing the 9 Positions/*.csv files creates one PlayerRoleScore row per (player, role) across all non-GK positions"
    - "Every non-GK player with a Positions row gets role scores; GK players get exactly zero rows (by design, noted in the report)"
    - "Role column headers are sanitized to role_name while role_name_raw preserves the original header"
    - "Re-running the import produces zero net PlayerRoleScore change (idempotent on player+role_name)"
  artifacts:
    - path: "get-scouted-be/players/management/commands/import_position_roles.py"
      provides: "Normalizes 9 wide per-position role files into long PlayerRoleScore rows, idempotent upsert"
      contains: "update_conflicts=True"
    - path: "get-scouted-be/players/tests/test_import.py"
      provides: "test_role_score_coverage appended to the existing Player test module"
      contains: "test_role_score_coverage"
  key_links:
    - from: "import_position_roles.py"
      to: "players.Player (via unique_id -> id map)"
      via: "resolve player_id from UniqueID column"
      pattern: "player_id_map"
    - from: "import_position_roles.py"
      to: "PlayerRoleScore unique constraint"
      via: "bulk_create update_conflicts on (player, role_name)"
      pattern: "role_name"
---

<objective>
Normalize the 9 per-position role files (Positions/*.csv) from wide (one row per player, 4-7 role columns) into long PlayerRoleScore rows (one per player-role), resolving player FK by UniqueID.

Purpose: DATA-03 (position-aware player data, queryable). The long shape lets Phase 3+ query "best-fit role per player" without per-position schema variance. GK players legitimately have zero rows (there is no GK Positions file) — the report must state this so it is not mistaken for a join failure. UniqueID here IS a genuine player id (verified 100% overlap with Players.csv), unlike transferdata.
Output: import_position_roles.py + test_role_score_coverage appended to players/tests/test_import.py.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md "PlayerRoleScore Model". The 9 files and their position_group: -->
"AM with league.csv"->AM, "CB with league.csv"->CB, "CM with league.csv"->CM, "DM with league.csv"->DM, "FWD with league Updated.csv"->FWD, "LB with league.csv"->LB, "LW with league2.csv"->LW, "RB with league.csv"->RB, "RW with league.csv"->RW.
Each file shape: UniqueID, <role_1>, <role_2>, ... (4-7 role float columns).
Example CB header: UniqueID,Wide_Centre-Back_(LCB),No_Nonsense_Centre_Back,Ball_Playing_Defender,Wide_Centre_Back_(RCB),Libero
Header quirks to expect: hyphens/parens (Wide_Centre-Back_(LCB)); a stray-space typo "Advanced _Playmaker" in the AM file (preserve raw, sanitize for role_name).
role_name sanitization: lowercase, strip parens, replace non-alnum runs with single underscore, trim underscores -> e.g. "Wide_Centre-Back_(LCB)" -> "wide_centre_back_lcb".
PlayerRoleScore upsert: unique_fields = player + role_name; update_fields = score, role_name_raw, position_group.
GK has NO Positions file — GK players get zero PlayerRoleScore rows by design (~3,081 players).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build import_position_roles command (wide->long normalization, idempotent)</name>
  <files>get-scouted-be/players/management/commands/import_position_roles.py</files>
  <read_first>
    - get-scouted-be/core/import_utils.py (reader, resolve_dataset_path, ImportReport)
    - get-scouted-be/players/models.py (PlayerRoleScore fields + uniq_player_role constraint)
    - API-Updated-/dataset/Positions/CB with league.csv (example header)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (section "PlayerRoleScore Model")
  </read_first>
  <action>
    Create players/management/commands/import_position_roles.py as a BaseCommand with `--positions-dir` (default resolve_dataset_path("Positions")) and `--report-dir`.

    Logic:
    1. Build `player_id_map = dict(Player.objects.values_list('unique_id','id'))`. If empty, error out telling the operator to run import_players first.
    2. Define a hardcoded `FILE_TO_POSITION` dict mapping the exact 9 filenames (incl "FWD with league Updated.csv" and "LW with league2.csv") to their position_group.
    3. For each file:
       - Read with explicit dtype (UniqueID int64, role columns Float64).
       - role columns = every column except UniqueID.
       - `melt` wide->long to (UniqueID, role_name_raw, score).
       - Per row: player_id = player_id_map.get(UniqueID); if missing, add_field_issue("UniqueID","no_matching_player", uid) and skip that score row (verified ~100% overlap, so ~0 expected). role_name = sanitize_role_name(role_name_raw). Build PlayerRoleScore(player_id=..., position_group=..., role_name=..., role_name_raw=..., score=...).
    4. `PlayerRoleScore.objects.bulk_create(objs, batch_size=5000, update_conflicts=True, unique_fields=['player','role_name'], update_fields=['score','role_name_raw','position_group'])`.
    5. Report sections: per-position row counts, total role rows, count of unmatched UniqueIDs, and the explicit note "GK players have zero PlayerRoleScore rows by design (no GK Positions file)". Write JSON + Markdown.
    Include a module-level `sanitize_role_name(raw)` helper implementing the rule in the interfaces block (use `re.sub`).
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "import ast; ast.parse(open('players/management/commands/import_position_roles.py').read()); print('OK')" && grep -q "update_conflicts=True" players/management/commands/import_position_roles.py && grep -q "sanitize_role_name" players/management/commands/import_position_roles.py</automated>
  </verify>
  <acceptance_criteria>
    - import_position_roles.py contains `update_conflicts=True` and unique_fields listing both `player` and `role_name`
    - Contains a `sanitize_role_name` helper using `re.sub`
    - Contains a FILE_TO_POSITION dict with the literal string `FWD with league Updated.csv` and `LW with league2.csv`
    - Uses `melt` for wide->long normalization (grep `melt`)
    - The `python ast.parse` verify command exits 0
  </acceptance_criteria>
  <done>import_position_roles normalizes all 9 wide role files into long PlayerRoleScore rows, resolves player FK by UniqueID, sanitizes role headers (preserving raw), upserts idempotently on (player, role_name), and reports the GK zero-row expectation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append test_role_score_coverage to players/tests/test_import.py</name>
  <files>get-scouted-be/players/tests/test_import.py</files>
  <read_first>
    - get-scouted-be/players/management/commands/import_position_roles.py (command under test)
    - get-scouted-be/players/tests/test_import.py (existing Player tests + the club/player seeding pattern — REUSE it, do not duplicate helpers destructively)
    - get-scouted-be/core/tests/fixtures/positions_CB_sample.csv (the CB role fixture) and players_sample.csv (contains a GK row)
    - .planning/phases/01-data-foundation/01-VALIDATION.md (DATA-03 test name)
  </read_first>
  <action>
    APPEND (do not rewrite the whole file) a `@pytest.mark.django_db` test `test_role_score_coverage` to players/tests/test_import.py. It must:
    1. Seed Clubs (call_command import_clubs_playstyles on fixtures), then Players (import_players on players_sample.csv), then run import_position_roles pointing at a fixtures dir that contains positions_CB_sample.csv (pass `--positions-dir` = the fixtures dir; the command should tolerate only some of the 9 files being present, skipping missing filenames without error — confirm/adjust the command to skip absent files rather than crash).
    2. Assert every CB player from the fixture has >= 1 PlayerRoleScore row.
    3. Assert the GK player from players_sample.csv has exactly 0 PlayerRoleScore rows.
    4. Assert one known role_name is the sanitized form (e.g. `wide_centre_back_lcb`) and its role_name_raw preserves the original header.
    5. Re-run import_position_roles and assert PlayerRoleScore count is unchanged (idempotent).
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest players/tests/test_import.py::test_role_score_coverage -x -q</automated>
  </verify>
  <acceptance_criteria>
    - players/tests/test_import.py contains `def test_role_score_coverage`
    - The test asserts the GK player has 0 role rows and CB players have >=1
    - The test asserts a sanitized role_name value and re-run idempotency
    - `pytest players/tests/test_import.py::test_role_score_coverage -x` exits 0
    - The previously-passing Player tests still pass (`pytest players/tests/test_import.py -q` green)
  </acceptance_criteria>
  <done>test_role_score_coverage passes: non-GK players get role rows, GK players get none, headers are sanitized with raw preserved, and re-runs are idempotent.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && pytest players/tests/test_import.py::test_role_score_coverage -q` green
- `pytest players/tests/test_import.py -q` (all Player + role tests) green
- Manual full-scale: `python manage.py import_position_roles` creates ~38,600 rows; report notes ~3,081 GK players with zero rows
</verification>

<success_criteria>
- 9 wide role files normalized to long PlayerRoleScore rows, player FK resolved by UniqueID
- GK zero-coverage stated in the report; idempotent re-run
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-06-SUMMARY.md`
</output>

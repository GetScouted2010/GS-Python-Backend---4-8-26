---
phase: 01-data-foundation
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - get-scouted-be/players/management/__init__.py
  - get-scouted-be/players/management/commands/__init__.py
  - get-scouted-be/players/management/commands/import_players.py
  - get-scouted-be/players/tests/__init__.py
  - get-scouted-be/players/tests/test_import.py
autonomous: true
requirements: [DATA-01, DATA-03, DATA-05]

must_haves:
  truths:
    - "Importing Players.csv creates one Player row per source row, resolving club FK from Team_within_selected_timeframe"
    - "Missing/dirty values (blank Market_value, Foot='0'/'unknown', blank Contract_expires) import as null/flagged and are logged in the report — the row is never skipped"
    - "The 14 movement columns land in extended_stats JSONField keyed by original CSV name; Total_Score lands in legacy_total_score"
    - "Re-running the import produces zero net Player row change (idempotent upsert on unique_id)"
  artifacts:
    - path: "get-scouted-be/players/management/commands/import_players.py"
      provides: "Chunked idempotent Player import with club-FK resolution, extended_stats packing, contract-date parsing, outlier flagging"
      contains: "update_conflicts=True"
    - path: "get-scouted-be/players/tests/test_import.py"
      provides: "test_player_row_count, test_player_missing_field_handling, test_reimport_idempotent"
      contains: "test_reimport_idempotent"
  key_links:
    - from: "import_players.py"
      to: "clubs.Club"
      via: "in-memory name->id map for club_id resolution"
      pattern: "club_id"
    - from: "import_players.py"
      to: "Player.extended_stats"
      via: "packing 14 movement columns keyed by raw CSV name"
      pattern: "extended_stats"
---

<objective>
Import the ~41,708 player rows from Players.csv into the Player table: chunked, idempotent, club-FK-resolved, with the 14 movement columns packed into extended_stats, Total_Score renamed to legacy_total_score, DD/MM/YYYY contract dates parsed explicitly, and all missing/dirty/outlier values flagged (not dropped) in the import report.

Purpose: This satisfies DATA-01 (real player profile data) and DATA-03 (the per-player stat columns live on Player). It depends on Plan 04 for Club rows (FK resolution) and the ImportReport/reader utilities. The data-quality policy is strict: never skip a row, always flag — per CONTEXT.md.
Output: import_players.py + players/tests/test_import.py.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@get-scouted-be/docs/FIELD_MAPPING.md

<interfaces>
<!-- From RESEARCH.md "Player Model" + "Code Examples". -->
Club FK resolution: build `club_id_map = dict(Club.objects.values_list('name','id'))`, resolve `club_id = club_id_map.get(row.Team_within_selected_timeframe)` (null if unresolved — log it).
Player upsert: Player.objects.bulk_create(objs, batch_size=2000, update_conflicts=True, unique_fields=['unique_id'], update_fields=[all non-id/non-unique_id fields]).
Contract_expires format is DD/MM/YYYY (e.g. 30/06/2027) — parse with an explicit format, do NOT let pandas infer.
extended_stats keys = the 14 raw CSV column names (Meter/Min, Max_Speed_(km/h), Count_* , Total_Distance_per_90, etc. — see FIELD_MAPPING section 2).
Verified null/outlier rates to surface: Contract_expires 18%, Market_value(blank or 0) 14%, Weight 3.9%, Height 3.0%, Foot '0' 512 rows + 'unknown' 287 rows, Position 1 blank row.
NOTE: bulk_create bypasses auto_now — if any updated_at field exists, set it explicitly (models here have none, so no action needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build import_players command (chunked, idempotent, club-FK, extended_stats, outlier flagging)</name>
  <files>get-scouted-be/players/management/__init__.py, get-scouted-be/players/management/commands/__init__.py, get-scouted-be/players/management/commands/import_players.py</files>
  <read_first>
    - get-scouted-be/core/import_utils.py (read_csv_chunks, resolve_dataset_path, ImportReport)
    - get-scouted-be/players/models.py (Player fields, unique_id constraint, extended_stats, legacy_total_score)
    - get-scouted-be/docs/FIELD_MAPPING.md (sections 1 + 2 — the exact CSV->field mapping and the 14 extended_stats columns)
    - API-Updated-/dataset/Players.csv (header — the 135 columns)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (section "Player Model", null/outlier table, and the two-pass code example)
  </read_first>
  <action>
    Create players/management/commands/import_players.py as a BaseCommand with options `--players-csv` (default resolve_dataset_path("Players.csv")), `--report-dir`, `--chunksize` (default 2000).

    Logic:
    1. Build `club_id_map = dict(Club.objects.values_list('name','id'))` up front (Clubs already imported by Plan 04's command — assume they exist; if empty, print a clear error telling the operator to run import_clubs_playstyles first).
    2. Stream Players.csv via read_csv_chunks with explicit dtypes: UniqueID int64, Player/Team_within_selected_timeframe/League/Foot/Contract_expires as string, Market_value/Age/Height/Weight as nullable numeric (pandas "Int64"/"Float64" — never plain float that silently upcasts NaN).
    3. Per row, construct a Player with:
       - unique_id=UniqueID, player=Player, season=Season, club_id=club_id_map.get(Team_within_selected_timeframe), legacy_total_score=Total_Score.
       - The ~121 stat/profile fields mirrored 1:1 by CSV snake_case name (per FIELD_MAPPING section 1).
       - extended_stats = a dict of the 14 movement columns keyed by ORIGINAL CSV name (only non-null values), per FIELD_MAPPING section 2.
       - Contract_expires parsed with `datetime.strptime(val, "%d/%m/%Y").date()` inside a try/except; on failure or blank -> None + report.add_field_issue("Contract_expires","missing"/"unparseable", unique_id).
    4. Data-quality flagging (log, never skip): blank/zero Market_value -> null + add_field_issue("Market_value","missing_or_zero"); Foot in {"0","unknown"} -> keep the raw value but add_field_issue("Foot", f"invalid_value:'{val}'"); blank Position -> add_field_issue("Position","missing"); Age>60 or Market_value<0 -> add_field_issue(outlier) but KEEP the value.
    5. Upsert per chunk inside `transaction.atomic()`: `Player.objects.bulk_create(objs, batch_size=chunksize, update_conflicts=True, unique_fields=['unique_id'], update_fields=[f.name for f in Player._meta.fields if f.name not in ('id','unique_id')])`.
    6. Track rows_created/updated via return of bulk_create where available, else count source rows; set report counts; write JSON + Markdown; print report paths and any unresolved club-name count.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "import ast; ast.parse(open('players/management/commands/import_players.py').read()); print('OK')" && grep -q "update_conflicts=True" players/management/commands/import_players.py</automated>
  </verify>
  <acceptance_criteria>
    - import_players.py contains `update_conflicts=True` AND `unique_fields=['unique_id']`
    - Contains `strptime` with format `%d/%m/%Y` for Contract_expires parsing
    - Packs extended_stats (grep `extended_stats`) keyed by raw CSV names
    - Foot values '0' and 'unknown' are flagged via `add_field_issue` (grep `Foot`), and the row is still constructed (no `continue`/skip on bad Foot)
    - Reads via `read_csv_chunks` with explicit dtypes (grep `dtype` or the util call), never bare `pd.read_csv` without dtypes
  </acceptance_criteria>
  <done>import_players streams Players.csv in chunks, resolves club FK, packs extended_stats + legacy_total_score, parses DD/MM/YYYY contract dates, flags every missing/dirty/outlier value without skipping rows, and upserts idempotently on unique_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Player import tests (row count, missing-field handling, idempotent re-run)</name>
  <files>get-scouted-be/players/tests/__init__.py, get-scouted-be/players/tests/test_import.py</files>
  <read_first>
    - get-scouted-be/players/management/commands/import_players.py (the command under test)
    - get-scouted-be/clubs/management/commands/import_clubs_playstyles.py (must run first in the test to seed Clubs)
    - get-scouted-be/core/tests/fixtures/players_sample.csv (seeded edge-case rows)
    - .planning/phases/01-data-foundation/01-VALIDATION.md (Per-Task Verification Map — the DATA-01/DATA-05 test names)
  </read_first>
  <action>
    Create players/tests/test_import.py with `@pytest.mark.django_db` tests calling the commands via `call_command` against the fixtures (fixture_dir fixture). First run import_clubs_playstyles against the fixtures so Club rows exist, then import_players.

    - test_player_row_count: after import, `Player.objects.count()` == number of data rows in players_sample.csv. Every player's club FK is either resolved or null (no crash), and at least one is resolved to a real Club.
    - test_player_missing_field_handling: the row seeded with blank Market_value has `market_value is None`; the Foot='0' and Foot='unknown' rows are IMPORTED (present in DB) and appear in the report's field_issues under a Foot invalid_value issue; the blank Contract_expires row has `contract_expires is None`; the "30/06/2027" row parses to date(2027,6,30). No row from the fixture is missing. PLUS — backing must_haves truth #3 (extended_stats / legacy_total_score) at RUNTIME, not just via a source grep: pick a known fixture player row and assert (a) that player's `extended_stats` dict is non-empty and contains an EXPECTED movement-column key with its EXPECTED value (e.g. `player.extended_stats["Max_Speed_(km/h)"] == <fixture value>`, using the exact raw CSV column name and the value present in players_sample.csv), and (b) that player's `legacy_total_score` equals that same fixture row's `Total_Score` value. Choose a fixture player whose movement columns and Total_Score are populated (add/confirm such a row in players_sample.csv if needed).
    - test_reimport_idempotent: run import_players twice; `Player.objects.count()` is identical after the second run (no duplicates) — proves unique_id upsert.
    - (test_role_score_coverage and test_role_score-related tests are added by Plan 06 to THIS SAME file; leave the file importable and additive.)
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest players/tests/test_import.py::test_player_row_count players/tests/test_import.py::test_player_missing_field_handling players/tests/test_import.py::test_reimport_idempotent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - players/tests/test_import.py contains `def test_player_row_count`, `def test_player_missing_field_handling`, `def test_reimport_idempotent`
    - test_player_missing_field_handling asserts a known fixture player's `extended_stats` dict contains an expected movement-column key mapped to its expected value (raw CSV column name as the key), AND that the same player's `legacy_total_score` equals that fixture row's `Total_Score` — this is a runtime assertion, not a source grep
    - Tests seed Clubs first via `call_command('import_clubs_playstyles', ...)` then `call_command('import_players', ...)`
    - test_reimport_idempotent asserts equal counts across two runs
    - `pytest players/tests/test_import.py -x` exits 0 for the three named tests
  </acceptance_criteria>
  <done>All three Player import tests pass: correct row count, missing/dirty fields handled as null/flagged without skipping, extended_stats + legacy_total_score verified against a known fixture row at runtime, and a second import run adds zero rows (idempotent).</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && pytest players/tests/test_import.py -q -k "row_count or missing_field or idempotent"` green
- `cd get-scouted-be && pytest players/tests/test_import.py::test_player_missing_field_handling -x -q` green (also asserts extended_stats key/value + legacy_total_score against a known fixture row at runtime)
- `grep -q "unique_fields=\['unique_id'\]" get-scouted-be/players/management/commands/import_players.py`
- Manual full-scale: `python manage.py import_players` imports 41,708 rows; report shows Contract_expires ~18% + Market_value ~14% missing
</verification>

<success_criteria>
- One Player row per source row, club FK resolved, extended_stats + legacy_total_score populated (verified against a known fixture row at runtime)
- Missing/dirty/outlier values flagged in the report, never dropped
- Idempotent re-run adds zero rows
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-05-SUMMARY.md`
</output>
</content>

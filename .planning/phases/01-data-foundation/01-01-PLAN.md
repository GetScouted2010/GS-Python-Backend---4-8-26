---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-scouted-be/manage.py
  - get-scouted-be/config/__init__.py
  - get-scouted-be/config/settings/__init__.py
  - get-scouted-be/config/settings/base.py
  - get-scouted-be/config/settings/local.py
  - get-scouted-be/config/settings/production.py
  - get-scouted-be/config/urls.py
  - get-scouted-be/config/wsgi.py
  - get-scouted-be/config/asgi.py
  - get-scouted-be/clubs/__init__.py
  - get-scouted-be/clubs/apps.py
  - get-scouted-be/players/__init__.py
  - get-scouted-be/players/apps.py
  - get-scouted-be/transfers/__init__.py
  - get-scouted-be/transfers/apps.py
  - get-scouted-be/core/__init__.py
  - get-scouted-be/core/apps.py
  - get-scouted-be/requirements/base.txt
  - get-scouted-be/.env.example
  - get-scouted-be/.gitignore
  - get-scouted-be/docs/FIELD_MAPPING.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
user_setup:
  - service: postgres
    why: "Local Postgres 16/17 database for the Django project (no cloud host in Phase 1)"
    env_vars:
      - name: DATABASE_URL
        source: "Local Postgres — e.g. postgresql://postgres:postgres@localhost:5432/getscouted"

must_haves:
  truths:
    - "Running `python manage.py check` in get-scouted-be/ exits 0 with no errors"
    - "The project connects to Postgres via DATABASE_URL and settings split into base/local/production"
    - "FIELD_MAPPING.md documents, per CSV column, the Django field name AND the impact_model_v4.1.py internal (space-separated) name"
  artifacts:
    - path: "get-scouted-be/config/settings/base.py"
      provides: "Shared Django settings, INSTALLED_APPS with django.contrib.postgres + clubs/players/transfers/core, DATABASES via django-environ, DATASET_DIR"
      contains: "django.contrib.postgres"
    - path: "get-scouted-be/manage.py"
      provides: "Django entrypoint pointing at config.settings.local by default"
      contains: "config.settings"
    - path: "get-scouted-be/docs/FIELD_MAPPING.md"
      provides: "Canonical CSV -> Django field -> scoring-script name mapping deliverable (DATA-05 prerequisite)"
      min_lines: 120
  key_links:
    - from: "get-scouted-be/config/settings/base.py"
      to: "DATABASE_URL env var"
      via: "environ.Env().db()"
      pattern: "env\\.db\\("
---

<objective>
Stand up the Django/DRF-ready project skeleton from nothing (no Django project exists anywhere in this repo yet) and produce the required FIELD_MAPPING.md planning deliverable that resolves the known CSV field-name mismatches once, up front.

Purpose: Every downstream plan (models, imports, tests) needs a working `config.settings` module, a Postgres connection, and the three data apps to exist. FIELD_MAPPING.md is a required Phase 1 deliverable per CONTEXT.md ("resolved by writing an explicit FIELD_MAPPING.md during planning — not guessed ad hoc inside import code") and prevents Phase 3 rework by capturing the CSV-name vs. impact_model_v4.1.py internal-name divergence while columns are being enumerated.
Output: A runnable Django project under get-scouted-be/ and get-scouted-be/docs/FIELD_MAPPING.md.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- Recommended layout from RESEARCH.md "Django Project Scaffolding". Use get-scouted-be/ as the project root (empty dir already exists). -->
Apps: clubs/ (Club), players/ (Player, PlayerRoleScore, PlayerClubCompatibility), transfers/ (Transfer), core/ (shared import utils + orchestrator command).
Settings package is config/ (NOT an app). Default settings module = config.settings.local.
Dataset lives OUTSIDE the Django project at repo-root: API-Updated-/dataset/ — expose as settings.DATASET_DIR.

Players.csv header (135 cols, verified) begins:
Season,UniqueID,Total_Score,League,Player,Team,Team_within_selected_timeframe,Positions,Main_Position,Position,Age,Market_value,Contract_expires,...
The 14 non-identifier "movement/physical" columns (verified UNUSED by impact_model_v4.1.py) are:
Total_Distance_per_90, Running_Distance_per_90_(15-20_km/h), HSR_Distance_per_90_(20-25_km/h), Sprinting_Distance_per_90_(+25_km/h), HI_Distance_per_90_(+20_km/h), Meter/Min, Max_Speed_(km/h), Count_Medium_Acceleration_per_90_(1_DOT_5_m/s²_to_3_m/s²), Count_High_Acceleration_per_90_(+3_m/s²), Count_Medium_Deceleration_per_90_(-1_DOT_5_m/s²_to_-3_m/s²), Count_High_Deceleration_per_90_(-3_m/s²), Count_HSR_per_90_(20-25_km/h), Count_Sprint_per_90_(+25_km/h), Count_HI_per_90_(+20_km/h)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Django project (config settings package + 4 apps + Postgres connection)</name>
  <files>get-scouted-be/manage.py, get-scouted-be/config/__init__.py, get-scouted-be/config/settings/__init__.py, get-scouted-be/config/settings/base.py, get-scouted-be/config/settings/local.py, get-scouted-be/config/settings/production.py, get-scouted-be/config/urls.py, get-scouted-be/config/wsgi.py, get-scouted-be/config/asgi.py, get-scouted-be/clubs/__init__.py, get-scouted-be/clubs/apps.py, get-scouted-be/players/__init__.py, get-scouted-be/players/apps.py, get-scouted-be/transfers/__init__.py, get-scouted-be/transfers/apps.py, get-scouted-be/core/__init__.py, get-scouted-be/core/apps.py, get-scouted-be/requirements/base.txt, get-scouted-be/.env.example, get-scouted-be/.gitignore</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (section "Django Project Scaffolding (from scratch)" — recommended layout + settings specifics)
    - .planning/research/STACK.md (Django 5.2 LTS, DRF 3.17, psycopg 3, pandas pin, django-environ 12-factor recommendation)
  </read_first>
  <action>
    Create a Django 5.2 project rooted at get-scouted-be/ (the empty dir already exists). Do NOT rely on `django-admin startproject` output verbatim — hand-build the split-settings layout below.

    requirements/base.txt (pin to STACK.md versions):
    ```
    Django>=5.2,<5.3
    djangorestframework>=3.15,<3.18
    psycopg[binary,pool]>=3.2
    django-environ>=0.11
    pandas>=2.2,<3.0
    ```

    config/settings/base.py must contain:
    - `import environ` + `env = environ.Env()` and `environ.Env.read_env(BASE_DIR / '.env')`
    - `BASE_DIR = Path(__file__).resolve().parent.parent.parent`
    - `SECRET_KEY = env('SECRET_KEY', default='dev-insecure-change-me')`
    - `INSTALLED_APPS = [` the six django.contrib defaults, then `'django.contrib.postgres'`, then `'clubs', 'players', 'transfers', 'core']`
    - `DATABASES = {'default': env.db('DATABASE_URL', default='postgresql://postgres:postgres@localhost:5432/getscouted')}`
    - `DATABASES['default']['CONN_MAX_AGE'] = 60`
    - `DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'`
    - `ROOT_URLCONF = 'config.urls'`, `WSGI_APPLICATION = 'config.wsgi.application'`
    - `DATASET_DIR = env('DATASET_DIR', default=str(BASE_DIR.parent / 'API-Updated-' / 'dataset'))` — path to API-Updated-/dataset/ relative to the repo root (get-scouted-be/ is one level below repo root).
    - Standard TIME_ZONE='UTC', USE_TZ=True, LANGUAGE_CODE='en-us', MIDDLEWARE (Django defaults), TEMPLATES (default), STATIC_URL='static/'.

    config/settings/local.py: `from .base import *` then `DEBUG = True`, `ALLOWED_HOSTS = ['*']`.
    config/settings/production.py: `from .base import *` then `DEBUG = False`, `ALLOWED_HOSTS = env.list('ALLOWED_HOSTS', default=[])`. Add a top-of-file comment: "# Hosting target deferred per PROJECT.md — placeholder only."
    config/settings/__init__.py: empty.
    manage.py + wsgi.py + asgi.py: set default `DJANGO_SETTINGS_MODULE = 'config.settings.local'`.
    config/urls.py: `urlpatterns = [path('admin/', admin.site.urls)]` (DRF routing is Phase 2+, leave minimal).

    Each app (clubs, players, transfers, core): create __init__.py and apps.py with a proper AppConfig (`default_auto_field = 'django.db.models.BigAutoField'`, `name = '<app>'`). Do NOT create models.py yet (Plan 03 creates them). Create an empty migrations/ package (__init__.py) under clubs, players, transfers.

    .env.example: `SECRET_KEY=`, `DEBUG=True`, `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/getscouted`, `DATASET_DIR=` (comment: leave blank to use the default repo-relative API-Updated-/dataset path).
    .gitignore: `*.pyc`, `__pycache__/`, `.env`, `db.sqlite3`, `.venv/`, `venv/`.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "import ast; ast.parse(open('config/settings/base.py').read()); ast.parse(open('manage.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - get-scouted-be/manage.py contains `config.settings`
    - get-scouted-be/config/settings/base.py contains `django.contrib.postgres` AND `'clubs'` AND `'players'` AND `'transfers'` AND `'core'`
    - get-scouted-be/config/settings/base.py contains `env.db(` AND `DATASET_DIR`
    - get-scouted-be/requirements/base.txt contains `Django>=5.2` AND `psycopg[binary,pool]` AND `pandas>=2.2,<3.0` AND `django-environ`
    - Files exist: get-scouted-be/clubs/apps.py, get-scouted-be/players/apps.py, get-scouted-be/transfers/apps.py, get-scouted-be/core/apps.py
    - get-scouted-be/.env.example contains `DATABASE_URL=` AND `DATASET_DIR=`
    - `python -c "ast.parse(...)"` on base.py and manage.py exits 0
  </acceptance_criteria>
  <done>The Django project skeleton exists under get-scouted-be/ with split settings, four apps registered, django.contrib.postgres enabled, and a Postgres connection driven by DATABASE_URL. `python manage.py check` passes once dependencies are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Write FIELD_MAPPING.md (CSV column -> Django field -> scoring-script name)</name>
  <files>get-scouted-be/docs/FIELD_MAPPING.md</files>
  <read_first>
    - API-Updated-/dataset/Players.csv (read the header row only — the 135 column names)
    - API-Updated-/dataset/transferdata final.csv (header row — 17 columns)
    - API-Updated-/dataset/Playstyles.csv (header row — 10 columns)
    - .planning/codebase/CONCERNS.md (section "Known Issue: Missing Field Mapping" — Team vs Team_within_selected_timeframe, Market_value vs "Market Value", Position/Main_Position/Positions variants)
    - API-Updated-/utils/fieldMapper.js (existing partial mapping — reference for intent/edge cases)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py (grep the column-name string literals it references internally, e.g. "Successful defensive actions per 90", "PAdj Interceptions", "Accurate passes, %")
  </read_first>
  <action>
    Create get-scouted-be/docs/FIELD_MAPPING.md as the canonical mapping deliverable, with these sections:

    ## 1. Player fields (Players.csv)
    A Markdown table: `CSV column | Django field | impact_model_v4.1.py internal name | notes`.
    - For the ~121 stat columns that are valid identifiers, Django field = exact CSV snake_case name (mirror it, per CONTEXT.md locked decision). Fill the script-internal-name column with the space/comma form the script uses where it differs (e.g. CSV `Successful_defensive_actions_per_90` -> script `"Successful defensive actions per 90"`; CSV `PAdj_Interceptions` -> script `"PAdj Interceptions"`; CSV `Accurate_passes_percentage` -> script `"Accurate passes, %"`). Where the script does not reference the column, write "not referenced".
    - Special renames (dedicated subsection, called out explicitly): `UniqueID` -> `unique_id` (player-level key, unique=True); `Total_Score` -> `legacy_total_score` (pre-existing OLD-system score, NOT the Phase 4-6 RMM output — flag loudly); `Team_within_selected_timeframe` -> `club` FK (the field the script renames to "Team" internally — correct FK source, NOT `Team`); `Team` -> dropped (duplicate concept, not carried forward); `Positions`/`Main_Position`/`Position` -> document which populates which model field.

    ## 2. Player.extended_stats JSONField (14 movement/physical columns)
    List all 14 columns verbatim from the interfaces block. State: keyed by ORIGINAL CSV column-name string inside a JSONField; none referenced by impact_model_v4.1.py; not sanitized into individual fields.

    ## 3. Club fields (derived — no source file)
    Table: `Club field | source | notes`. name = Team_within_selected_timeframe (union Playstyles.Team); league = MODE of League per club (alphabetical tie-break); playing_style_* (8 columns from Playstyles.csv: Control_Possession, Gegenpressing, Direct_Play, Defensive_Counter_Attack, Tiki_Taka, Counter_Attack, Wing_Play, Low_Block); manager/formation/country = null (no source, v1); squad_size = NOT migrated (live count, Phase 7/11).

    ## 4. Transfer fields (transferdata final.csv)
    Table for all 17 columns. CALL OUT the trap: `UniqueID` here = a CLUB id (207 distinct, 1:1 with `Club`), NOT a player id — resolves `Transfer.club`, must NEVER join to Player.UniqueID. `Dealing_Club` = plain CharField (2,909 distinct counterparties, no FK). `Player` = name string, resolves Transfer.player via best-effort name match (nullable). `Age`/`Market_Value` = at-transfer-time values, do not conflate with Player.Age/Market_value.

    ## 5. Known mismatches resolved (from CONCERNS.md)
    Bullet list restating each CONCERNS.md field-mapping issue and how this document resolves it.
  </action>
  <verify>
    <automated>grep -q "legacy_total_score" get-scouted-be/docs/FIELD_MAPPING.md && grep -q "Team_within_selected_timeframe" get-scouted-be/docs/FIELD_MAPPING.md && grep -q "extended_stats" get-scouted-be/docs/FIELD_MAPPING.md && grep -q "Successful defensive actions per 90" get-scouted-be/docs/FIELD_MAPPING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - get-scouted-be/docs/FIELD_MAPPING.md contains `legacy_total_score` AND a note that it is the OLD-system score, not RMM
    - Contains `Team_within_selected_timeframe` and states it is the `club` FK source (not `Team`)
    - Contains `extended_stats` and lists all 14 movement columns including `Meter/Min` and `Max_Speed_(km/h)`
    - Contains the literal string `Successful defensive actions per 90` (proving CSV-name vs script-name divergence is captured)
    - Section 4 states `UniqueID` in transferdata is a Club id, not a player id
    - File is at least 120 lines (`wc -l` >= 120)
  </acceptance_criteria>
  <done>FIELD_MAPPING.md exists documenting every CSV column's Django field name and, where it diverges, the impact_model_v4.1.py internal name; the extended_stats JSON block, legacy_total_score, club-FK source, and the transfer UniqueID trap are all explicitly captured.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && python -c "import ast; ast.parse(open('config/settings/base.py').read())"` exits 0
- `grep -q "django.contrib.postgres" get-scouted-be/config/settings/base.py`
- `grep -q "Successful defensive actions per 90" get-scouted-be/docs/FIELD_MAPPING.md`
- All four app directories (clubs, players, transfers, core) contain apps.py
</verification>

<success_criteria>
- Django project skeleton runs `python manage.py check` cleanly once deps are installed
- Split settings (base/local/production), Postgres via DATABASE_URL, DATASET_DIR pointing at API-Updated-/dataset
- FIELD_MAPPING.md captures CSV -> Django -> script-name mapping, extended_stats block, and the transfer UniqueID trap
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>

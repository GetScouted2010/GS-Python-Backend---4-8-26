---
phase: 10-ai-grounded-report-generation
plan: 04
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - get-scouted-be/clubs/tests/conftest.py
  - get-scouted-be/clubs/tests/test_ai_safety_net.py
  - get-scouted-be/clubs/services.py
  - get-scouted-be/clubs/tests/test_services_club_insights.py
  - get-scouted-be/clubs/views.py
  - get-scouted-be/clubs/urls.py
  - get-scouted-be/clubs/tests/test_ai_club_insights.py
autonomous: true
requirements: [AI-04]

must_haves:
  truths:
    - "clubs/tests/conftest.py has the autouse _block_real_anthropic_calls guard (ported from players/tests/conftest.py), so no club-insights test can fire a real billed Anthropic call"
    - "position_needs_aggregate(club) returns, per position, squad_depth + avg_age + contracts_expiring_within_12mo via a single bounded ORM .values('position').annotate(...) query scoped to one club's squad -- never a full-dataset pandas operation"
    - "POST /api/clubs/{id}/insights/ returns narrative sections (recruitment_gaps/over_aged_positions/financial_constraints) plus the grounding dict combining position-needs aggregation with ClubDetailSerializer's transfer aggregates"
    - "Generation failure (ReportGeneratorError) returns a clean 503, never a fabricated insights report"
  artifacts:
    - path: "get-scouted-be/clubs/tests/conftest.py"
      provides: "autouse _block_real_anthropic_calls fixture for clubs/tests/"
      contains: "_block_real_anthropic_calls"
    - path: "get-scouted-be/clubs/services.py"
      provides: "position_needs_aggregate(club) + generate_club_insights(club_id)"
      contains: "def position_needs_aggregate"
    - path: "get-scouted-be/clubs/views.py"
      provides: "ClubInsightsView (POST) catching ReportGeneratorError -> 503"
      contains: "class ClubInsightsView"
    - path: "get-scouted-be/clubs/urls.py"
      provides: "insights/ route"
      contains: "insights"
  key_links:
    - from: "get-scouted-be/clubs/services.py"
      to: "position_needs_aggregate + ClubDetailSerializer.get_transfer_aggregates + players.ai.report_factory.get_report_generator"
      via: "generate_club_insights builds grounding from both aggregates and generates via the cross-app factory"
      pattern: "get_report_generator\\(\\)\\.generate"
    - from: "get-scouted-be/clubs/views.py"
      to: "get-scouted-be/clubs/services.py"
      via: "ClubInsightsView.post calls generate_club_insights"
      pattern: "generate_club_insights\\("
---

<objective>
Ship the club-insights slice (AI-04): a lightweight, single-club position-needs ORM aggregation + a `generate_club_insights(club_id)` orchestration in a NEW `clubs/services.py`, the `POST /api/clubs/{id}/insights/` endpoint, and — critically, sequenced FIRST — the port of the autouse Anthropic-blocking safety net into `clubs/tests/conftest.py` (currently absent, verified) so no club-insights test can ever fire a real billed call.

Purpose: This is AI-04 end-to-end. Club insights are club-scoped data, so the orchestration lives in `clubs/` (mirroring the existing `clubs/serializers.py` -> `players.serializers` cross-app precedent) and imports the shared LLM plumbing from `players/ai/`. The position-needs aggregation is deliberately narrow (just the numbers a report needs, not Phase 11's full strong/weak/at-risk feature) and is a bounded per-club ORM query, never pandas.
Output: the ported conftest guard + its test, the new `clubs/services.py`, `ClubInsightsView`, the route, and three test files.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-grounded-report-generation/10-CONTEXT.md
@.planning/phases/10-ai-grounded-report-generation/10-RESEARCH.md

# Interfaces built in Waves 1-2 + the exact things to reuse/mirror:
@get-scouted-be/players/ai/report_generator.py
@get-scouted-be/players/ai/report_factory.py
@get-scouted-be/clubs/serializers.py
@get-scouted-be/clubs/views.py
@get-scouted-be/clubs/urls.py
@get-scouted-be/players/tests/conftest.py

<interfaces>
<!-- From 10-01/10-02 (already on disk):
     players/ai/report_generator.py: GeneratedReport(narrative: dict, grounding: dict), ReportGeneratorError
     players/ai/report_factory.py: get_report_generator() -> ReportGenerator
     ReportGenerator.generate(grounding, report_type) with report_type == "club_insights"
       narrative keys == recruitment_gaps / over_aged_positions / financial_constraints -->
<!-- Reuse, don't recompute (VERIFIED):
     clubs/serializers.py::ClubDetailSerializer.get_transfer_aggregates(self, club) -> dict
       returns total_transfers/arrivals/departures/avg_market_value_at_transfer/
       total_market_value_at_transfer/by_window. It reads Transfer.market_value_at_transfer
       ONLY (never the free-text Transfer.fee). Call it directly WITHOUT triggering the heavy
       squad serialization: `ClubDetailSerializer().get_transfer_aggregates(club)`
       (the method takes club as an arg and needs no instance/context). -->
<!-- Player fields (VERIFIED in players/models.py): position CharField(null=True),
     age IntegerField(null=True), contract_expires DateField(null=True), club FK
     related_name="players". Club.players.all() is the club's current squad. -->
<!-- Cross-app import precedent (VERIFIED): clubs/serializers.py already does
     `from players.serializers import PlayerListSerializer` -- clubs/services.py importing
     `from players.ai.report_factory import get_report_generator` follows the same convention. -->
<!-- Safety-net gap (VERIFIED): clubs/tests/conftest.py (19 lines) has real_data_available
     but NOT _block_real_anthropic_calls. players/tests/conftest.py has the exact autouse
     fixture (lines 23-39) to copy verbatim. clubs/tests/ is a sibling of players/tests/,
     so the players guard does NOT cover it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the autouse Anthropic-block safety net into clubs/tests/ (sequenced FIRST)</name>
  <files>get-scouted-be/clubs/tests/conftest.py, get-scouted-be/clubs/tests/test_ai_safety_net.py</files>
  <read_first>
    - get-scouted-be/players/tests/conftest.py (the exact _block_real_anthropic_calls autouse fixture, lines 23-39, to copy verbatim -- per-app duplication is this project's established convention, e.g. ClubExportView's duplicated Echo class)
    - get-scouted-be/clubs/tests/conftest.py (the existing 19-line file to APPEND to -- do NOT remove its real_data_available fixture)
    - get-scouted-be/players/tests/test_ai_safety_net.py (the existing player-side guard test whose shape to mirror, if present)
  </read_first>
  <action>
    Append to clubs/tests/conftest.py (keep the existing real_data_available fixture intact) a verbatim copy of players/tests/conftest.py's `_block_real_anthropic_calls` autouse fixture: an `@pytest.fixture(autouse=True)` that imports anthropic, defines a `_GuardAnthropic` class raising RuntimeError on `__init__`, and `monkeypatch.setattr(anthropic, "Anthropic", _GuardAnthropic)`. Add a short docstring noting this is a per-app duplicate of the players guard (pytest conftest fixtures are directory-scoped; clubs/tests/ is a sibling of players/tests/, not a descendant).

    Create clubs/tests/test_ai_safety_net.py with a test proving the guard is active in clubs/tests/: `import anthropic; with pytest.raises(RuntimeError): anthropic.Anthropic(api_key="x")` -- confirms the autouse fixture intercepts real client construction here (mirroring the players-side safety-net test).
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest clubs/tests/test_ai_safety_net.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_block_real_anthropic_calls" get-scouted-be/clubs/tests/conftest.py` returns a match
    - `grep -n "autouse=True" get-scouted-be/clubs/tests/conftest.py` returns a match
    - `grep -n "def real_data_available" get-scouted-be/clubs/tests/conftest.py` still returns a match (existing fixture preserved)
    - `grep -n "pytest.raises(RuntimeError)" get-scouted-be/clubs/tests/test_ai_safety_net.py` returns a match
    - `cd get-scouted-be && .venv/bin/pytest clubs/tests/test_ai_safety_net.py -x` exits 0
  </acceptance_criteria>
  <done>clubs/tests/ now has its own autouse guard blocking real Anthropic construction, proven by a dedicated test -- every subsequent club-insights test in this plan runs under it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clubs/services.py -- position_needs_aggregate() + generate_club_insights()</name>
  <files>get-scouted-be/clubs/services.py, get-scouted-be/clubs/tests/test_services_club_insights.py</files>
  <read_first>
    - get-scouted-be/clubs/serializers.py (ClubDetailSerializer.get_transfer_aggregates -- reuse it directly for the transfer half of grounding)
    - get-scouted-be/players/ai/report_factory.py (get_report_generator() signature, from 10-02)
    - get-scouted-be/players/ai/report_generator.py (GeneratedReport + ReportGeneratorError, from 10-01)
    - .planning/phases/10-ai-grounded-report-generation/10-RESEARCH.md (Pattern 4 -- the exact position_needs_aggregate ORM query, including the contract_expires__isnull=False guard)
  </read_first>
  <behavior>
    - Test (aggregation shape): position_needs_aggregate(club) over a factory-built squad (e.g. 3 CBs, 2 STs with known ages/contract dates) returns a dict keyed by position, each value {squad_depth, avg_age, contracts_expiring_within_12mo}; squad_depth counts match, avg_age rounded to 1 dp, only contracts within 365 days counted
    - Test (bounded/single-club scope): a player belonging to a DIFFERENT club is NOT counted in the target club's aggregation (query is scoped to club.players)
    - Test (contract null-safety): a player with contract_expires=None is NOT counted as expiring (the isnull=False guard); a player with an already-past contract date within the window boundary behaves per the <= cutoff rule
    - Test (generate happy path): with get_report_generator patched (at clubs.services.get_report_generator) to a fake returning a 3-section GeneratedReport, generate_club_insights(club_id) returns {"narrative": {recruitment_gaps/over_aged_positions/financial_constraints}, "grounding": <dict containing both position_needs and transfer_aggregates>}
    - Test (generation failure propagates): fake generator raising ReportGeneratorError -> generate_club_insights re-raises it (never swallowed / templated)
  </behavior>
  <action>
    Create clubs/services.py (new module) with module-top imports: `from datetime import date, timedelta`, `from django.db.models import Avg, Count, Q`, `from django.shortcuts import get_object_or_404`, `from clubs.models import Club`, `from clubs.serializers import ClubDetailSerializer`, `from players.ai.report_factory import get_report_generator`.

    - `def position_needs_aggregate(club) -> dict:` implementing 10-RESEARCH.md Pattern 4 exactly -- a single bounded ORM query, NEVER pandas / never a full-dataset op:
        cutoff = date.today() + timedelta(days=365)
        rows = (club.players.exclude(position__isnull=True).values("position").annotate(
            squad_depth=Count("id"),
            avg_age=Avg("age"),
            expiring_within_12mo=Count("id", filter=Q(contract_expires__lte=cutoff, contract_expires__isnull=False)),
        ).order_by("position"))
        return {row["position"]: {"squad_depth": row["squad_depth"],
                "avg_age": round(row["avg_age"], 1) if row["avg_age"] is not None else None,
                "contracts_expiring_within_12mo": row["expiring_within_12mo"]} for row in rows}
      Docstring: internal-only grounding aggregation for AI-04; deliberately NARROWER than Phase 11's PLAN-01 (no strong/weak/at-risk labels, no public endpoint); bounded to one club's ~few-dozen-row squad via related_name="players".
    - `def generate_club_insights(club_id) -> dict:` docstring: AI-04 orchestration; grounding = position needs + reused transfer aggregates; narrative from the provider-agnostic get_report_generator(); ReportGeneratorError deliberately NOT caught (the view maps it to a clean 503). Body:
        club = get_object_or_404(Club, pk=club_id)
        grounding = {
            "club": {"name": club.name, "league": club.league},
            "position_needs": position_needs_aggregate(club),
            "transfer_aggregates": ClubDetailSerializer().get_transfer_aggregates(club),
        }
        report = get_report_generator().generate(grounding, "club_insights")
        return {"narrative": report.narrative, "grounding": report.grounding}
      (Call get_transfer_aggregates directly on a bare serializer instance so the heavy squad serialization is NOT triggered.)

    Create clubs/tests/test_services_club_insights.py covering the Behavior cases. Use ClubFactory/PlayerFactory (or build real objects with the db fixture) for the aggregation tests; for generate_club_insights, patch `clubs.services.get_report_generator` to a fake generator returning a 3-section GeneratedReport, and (optionally) patch `clubs.services.position_needs_aggregate`/reuse a real small club so no real scoring runs.
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest clubs/tests/test_services_club_insights.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def position_needs_aggregate" get-scouted-be/clubs/services.py` returns a match
    - `grep -n "\.values(\"position\").annotate(" get-scouted-be/clubs/services.py` returns a match (bounded ORM group-by, not pandas)
    - `grep -niE "import pandas|read_csv|score_population|reconstruct_population" get-scouted-be/clubs/services.py` returns NO matches (no full-dataset op)
    - `grep -n "contract_expires__isnull=False" get-scouted-be/clubs/services.py` returns a match (null-safety guard)
    - `grep -n "get_report_generator()\.generate(grounding, \"club_insights\")" get-scouted-be/clubs/services.py` returns a match
    - `grep -n "get_transfer_aggregates(club)" get-scouted-be/clubs/services.py` returns a match (reuse, not re-derive)
    - `cd get-scouted-be && .venv/bin/pytest clubs/tests/test_services_club_insights.py -x` exits 0
  </acceptance_criteria>
  <done>position_needs_aggregate is a bounded single-club ORM group-by returning per-position depth/age/expiring counts; generate_club_insights combines it with reused transfer aggregates and generates via the cross-app factory, propagating ReportGeneratorError -- all proven without pandas or a real API call.</done>
</task>

<task type="auto">
  <name>Task 3: ClubInsightsView + route + endpoint integration tests (clean-error-on-failure)</name>
  <files>get-scouted-be/clubs/views.py, get-scouted-be/clubs/urls.py, get-scouted-be/clubs/tests/test_ai_club_insights.py</files>
  <read_first>
    - get-scouted-be/clubs/views.py (ClubExportView's APIView + get_object_or_404 style; the existing import block to extend)
    - get-scouted-be/clubs/urls.py (the exact 3-route pattern to append to -- note the export route uses <uuid:pk>/export/)
    - get-scouted-be/clubs/services.py (generate_club_insights from Task 2)
    - get-scouted-be/players/tests/test_search_view.py (the APIClient + force_authenticate + monkeypatch-at-service-binding integration-test style to mirror)
  </read_first>
  <action>
    In clubs/views.py:
    - Add to the import block: `from rest_framework import status`, `from rest_framework.response import Response`, `from clubs import services`, `from players.ai.report_generator import ReportGeneratorError`.
    - Add `class ClubInsightsView(APIView):` with `def post(self, request, pk):` (no explicit permission_classes -- global IsAuthenticated default, matching ClubExportView; club data is not user-owned so no IsOwner). Body:
        try: insights = services.generate_club_insights(pk)
        except ReportGeneratorError: return Response({"error": "insights generation failed"}, status=status.HTTP_503_SERVICE_UNAVAILABLE)
        return Response(insights)
      Let a nonexistent club's Http404 (from generate_club_insights' get_object_or_404) surface naturally as 404 -- do NOT catch it.
    In clubs/urls.py: add `ClubInsightsView` to the import and `path("<uuid:pk>/insights/", ClubInsightsView.as_view(), name="club-insights")` (the /insights/ suffix differentiates it from the bare detail route, same as /export/).

    Create clubs/tests/test_ai_club_insights.py (runs under Task 1's ported safety net):
    - auth_client fixture (APIClient + force_authenticate) mirroring test_search_view.py.
    - test_club_insights_success: patch `clubs.services.get_report_generator` to a fake returning a fully-grounded 3-section GeneratedReport; build a small real/factory club with a squad + a couple of transfers so the real position_needs_aggregate + get_transfer_aggregates run; POST to reverse("club-insights", args=[club.id]); assert 200, JSON has "narrative" (3 keys) and "grounding" (with position_needs + transfer_aggregates present).
    - test_club_insights_failure (clean-error-on-failure, locked decision #6): fake generator raising ReportGeneratorError -> assert 503 and NO fabricated narrative in the body.
    - test_club_insights_unknown_club_404: POST to a random uuid -> assert 404 (Http404 surfaces naturally).
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest clubs/tests/test_ai_club_insights.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ClubInsightsView(APIView)" get-scouted-be/clubs/views.py` returns a match
    - `grep -n "HTTP_503_SERVICE_UNAVAILABLE" get-scouted-be/clubs/views.py` returns a match inside an `except ReportGeneratorError` block
    - `grep -n "insights/" get-scouted-be/clubs/urls.py` returns a match
    - `grep -n "def test_club_insights_failure" get-scouted-be/clubs/tests/test_ai_club_insights.py` returns a match
    - `cd get-scouted-be && .venv/bin/pytest clubs/tests/test_ai_club_insights.py -k failure -x` exits 0
    - `cd get-scouted-be && .venv/bin/pytest clubs/tests/test_ai_club_insights.py -x` exits 0
  </acceptance_criteria>
  <done>POST /api/clubs/{id}/insights/ returns {narrative, grounding} on success, a clean 503 on ReportGeneratorError (never a fabricated report), and a natural 404 for an unknown club -- proven under the ported safety net with an injected fake generator.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && .venv/bin/pytest clubs/tests/test_ai_safety_net.py clubs/tests/test_services_club_insights.py clubs/tests/test_ai_club_insights.py -x` -- all green.
- `cd get-scouted-be && .venv/bin/pytest clubs -q` -- no regression to Phase 7 clubs tests; the ported autouse guard confirms no real client is constructed.
</verification>

<success_criteria>
- clubs/tests/ has its own autouse Anthropic-block guard (ported first), closing the confirmed cross-app gap.
- position_needs_aggregate is a bounded single-club ORM query (no pandas / no full-dataset op).
- POST /api/clubs/{id}/insights/ returns grounded {narrative, grounding} or a clean 503/404 -- never a fabricated report.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-grounded-report-generation/10-04-SUMMARY.md`.
</output>

---
phase: 10-ai-grounded-report-generation
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - get-scouted-be/players/services.py
  - get-scouted-be/players/views.py
  - get-scouted-be/players/urls.py
  - get-scouted-be/players/tests/test_scouting_report_view.py
autonomous: true
requirements: [AI-03]

must_haves:
  truths:
    - "POST /api/players/{id}/scouting-report/ returns narrative sections (strengths/weaknesses/tactical_fit/financial_fit/best_use_case) plus the exact grounding dict used, for an authenticated user with a resolvable club context"
    - "The narrative is generated from get_summary()'s grounding only; grounding validation (built in 10-01/10-02) is what guarantees no LLM-invented number reaches the response"
    - "When generation fails (ReportGeneratorError: API error, malformed sections, or ungrounded numbers after retry) the endpoint returns a clean 503 error, never a fabricated/template report"
    - "When club_id is omitted and the player has no own club, the endpoint returns a clean 400 (club_id required), not a misleading Http404 from get_summary()"
  artifacts:
    - path: "get-scouted-be/players/services.py"
      provides: "generate_scouting_report(player_id, club_id) orchestration: get_summary grounding -> get_report_generator().generate -> {narrative, grounding}"
      contains: "def generate_scouting_report"
    - path: "get-scouted-be/players/views.py"
      provides: "PlayerScoutingReportView (POST) catching ReportGeneratorError -> 503"
      contains: "class PlayerScoutingReportView"
    - path: "get-scouted-be/players/urls.py"
      provides: "scouting-report/ route"
      contains: "scouting-report"
    - path: "get-scouted-be/players/tests/test_scouting_report_view.py"
      provides: "endpoint integration tests incl. clean-error-on-failure and club_id-missing branch"
  key_links:
    - from: "get-scouted-be/players/views.py"
      to: "get-scouted-be/players/services.py"
      via: "PlayerScoutingReportView.post calls generate_scouting_report"
      pattern: "generate_scouting_report\\("
    - from: "get-scouted-be/players/services.py"
      to: "scoring.services.summary.get_summary + players.ai.report_factory.get_report_generator"
      via: "grounding from get_summary, narrative from get_report_generator().generate"
      pattern: "get_report_generator\\(\\)\\.generate"
---

<objective>
Ship the player scouting-report slice (AI-03): a `generate_scouting_report(player_id, club_id)` service that assembles grounding from the already-tested `get_summary()`, runs it through the Wave-2 `get_report_generator()`, and returns structured `{narrative, grounding}` — plus the `POST /api/players/{id}/scouting-report/` endpoint that returns it or a clean error, never a fabricated report.

Purpose: This is AI-03 end-to-end. It composes existing verified pieces (get_summary grounding source, the ReportGenerator/grounding plumbing from 10-01/10-02) into a user-facing endpoint. The endpoint stays thin: all grounding/retry/validation logic lives inside the generator (10-02), so the view only orchestrates and catches `ReportGeneratorError`.
Output: `generate_scouting_report` service, `PlayerScoutingReportView`, the route, and the endpoint integration test file.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-grounded-report-generation/10-CONTEXT.md
@.planning/phases/10-ai-grounded-report-generation/10-RESEARCH.md

# Interfaces built in Waves 1-2 (already on disk):
@get-scouted-be/players/ai/report_generator.py
@get-scouted-be/players/ai/report_factory.py

# Grounding source + existing endpoint/routing/test conventions to mirror:
@get-scouted-be/scoring/services/summary.py
@get-scouted-be/players/views.py
@get-scouted-be/players/urls.py
@get-scouted-be/players/services.py

<interfaces>
<!-- From 10-01/10-02 (already on disk):
     players/ai/report_generator.py: GeneratedReport(narrative: dict, grounding: dict), ReportGeneratorError(Exception)
     players/ai/report_factory.py: get_report_generator() -> ReportGenerator
     ReportGenerator.generate(grounding: dict, report_type: str) -> GeneratedReport
       report_type for this slice == "player_scouting_report"
       narrative keys == strengths / weaknesses / tactical_fit / financial_fit / best_use_case -->
<!-- get_summary(player_id, club_id) -> dict (the grounding source). VERIFIED:
     - signature: def get_summary(player_id, club_id) -> dict  (scoring/services/summary.py:54)
     - RAISES django.http.Http404 if club_id/player_id is unresolvable (resolve_club_name(None) -> get_object_or_404).
       So DO NOT call get_summary with a None club_id -- branch first (Pitfall 1). -->
<!-- Test-mock convention (VERIFIED against players/tests/test_search_view.py):
     the endpoint's collaborators are patched at the SERVICE module binding, e.g.
     monkeypatch.setattr("players.services.get_report_generator", lambda: fake_generator)
     so the service must import get_report_generator at module top (a patchable binding). -->
<!-- PlayerDetailView.get() already establishes the club_id-resolution branch to mirror:
     club_id = request.query_params.get("club_id") or player.club_id; if None -> handle explicitly. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate_scouting_report() service (grounding assembly + generation orchestration)</name>
  <files>get-scouted-be/players/services.py, get-scouted-be/players/tests/test_scouting_report_view.py</files>
  <read_first>
    - get-scouted-be/players/services.py (the existing search_players service style + module-level imports to append to; do NOT rewrite search_players)
    - get-scouted-be/players/ai/report_generator.py (GeneratedReport shape + ReportGeneratorError, from 10-01)
    - get-scouted-be/players/ai/report_factory.py (get_report_generator() signature, from 10-02)
    - get-scouted-be/scoring/services/summary.py (get_summary(player_id, club_id) signature + its Http404-on-unresolvable-club behavior)
  </read_first>
  <behavior>
    - Test (happy path): with a fake generator (patched at players.services.get_report_generator) that returns GeneratedReport(narrative={"strengths":..., all 5 keys}, grounding=<the passed grounding>), generate_scouting_report(player_id, club_id) returns a dict with keys "narrative" (the 5-section dict) and "grounding" (the exact grounding get_summary produced)
    - Test (generation failure propagates): a fake generator whose generate() raises ReportGeneratorError -> generate_scouting_report re-raises ReportGeneratorError (the service does NOT swallow it or substitute a template)
    - Test (grounding is get_summary's output): generate_scouting_report passes get_summary(player_id, club_id)'s return value through to generator.generate(grounding, "player_scouting_report") unchanged (assert report_type == "player_scouting_report" and grounding identity)
  </behavior>
  <action>
    Append to players/services.py (keep the existing search_players function and its imports untouched):
    - Add module-top imports: `from players.ai.report_factory import get_report_generator` and `from scoring.services.summary import get_summary`. (Import get_report_generator at module top so tests can monkeypatch `players.services.get_report_generator`, matching the test_search_view.py binding-patch convention.)
    - `def generate_scouting_report(player_id, club_id) -> dict:` with a docstring stating: AI-03 orchestration; grounding comes ONLY from get_summary (never re-derived), narrative comes from the provider-agnostic get_report_generator(); ReportGeneratorError is deliberately NOT caught here (the view maps it to a clean 503) so a fabricated report is structurally impossible.
      Body:
      1. `grounding = get_summary(player_id, club_id)`  (may raise Http404 for an unresolvable club/player -- caller is responsible for resolving club_id first; see the view).
      2. `report = get_report_generator().generate(grounding, "player_scouting_report")`
      3. `return {"narrative": report.narrative, "grounding": report.grounding}`

    Create players/tests/test_scouting_report_view.py and put the Task-1 SERVICE-level tests above in it (the endpoint tests land in Task 2 in the same file). Use a small fake generator object with a `.generate(self, grounding, report_type)` method returning a GeneratedReport, injected via `monkeypatch.setattr("players.services.get_report_generator", lambda: fake)`. Mock get_summary via `monkeypatch.setattr("players.services.get_summary", lambda pid, cid: {...a small grounded dict...})` so the service tests need no real DB. Import ReportGeneratorError / GeneratedReport from players.ai.report_generator.
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest players/tests/test_scouting_report_view.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def generate_scouting_report" get-scouted-be/players/services.py` returns a match
    - `grep -n "get_report_generator()\.generate(grounding, \"player_scouting_report\")" get-scouted-be/players/services.py` returns a match
    - `grep -n "from players.ai.report_factory import get_report_generator" get-scouted-be/players/services.py` returns a match at module top (not inside a function)
    - `grep -n "except ReportGeneratorError\|try:" get-scouted-be/players/services.py` returns NO try/except around the generate call in generate_scouting_report (the error must propagate to the view)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_scouting_report_view.py -x` exits 0
  </acceptance_criteria>
  <done>generate_scouting_report assembles grounding from get_summary, generates via the provider-agnostic factory, returns {narrative, grounding}, and lets ReportGeneratorError propagate -- proven with an injected fake generator, no real API call.</done>
</task>

<task type="auto">
  <name>Task 2: PlayerScoutingReportView + route + endpoint integration tests (clean-error + club_id branch)</name>
  <files>get-scouted-be/players/views.py, get-scouted-be/players/urls.py, get-scouted-be/players/tests/test_scouting_report_view.py</files>
  <read_first>
    - get-scouted-be/players/views.py (PlayerDetailView's club_id-resolution branch + PlayerSearchView's APIView/Response style to mirror; the existing import block to extend)
    - get-scouted-be/players/urls.py (the exact 3-route pattern to append to)
    - get-scouted-be/players/tests/test_search_view.py (the APIClient + force_authenticate + monkeypatch-at-view/service-binding integration-test style to mirror)
    - get-scouted-be/players/services.py (generate_scouting_report from Task 1)
  </read_first>
  <action>
    In players/views.py:
    - Add `from players.ai.report_generator import ReportGeneratorError` and `from rest_framework import status` (if not already imported) to the import block; `services` is already imported.
    - Add `class PlayerScoutingReportView(APIView):` with a `def post(self, request, pk):` (no explicit permission_classes -- the global IsAuthenticated default applies, matching every other players view). Body:
      1. `player = get_object_or_404(Player, id=pk)` (get_object_or_404 already imported).
      2. Resolve club: `club_id = request.data.get("club_id") or player.club_id`. If `club_id is None`: `return Response({"error": "club_id required for a club-relative scouting report"}, status=status.HTTP_400_BAD_REQUEST)` (Pitfall 1 -- never let get_summary's Http404 be the signal for a club-less player).
      3. `try: report = services.generate_scouting_report(pk, club_id)` `except ReportGeneratorError: return Response({"error": "report generation failed"}, status=status.HTTP_503_SERVICE_UNAVAILABLE)`.
      4. `return Response(report)` (report is already {"narrative":..., "grounding":...}).
      Do NOT catch Http404 (unknown club_id) -- let it surface as a natural 404, matching PlayerDetailView's posture.
    In players/urls.py: add `PlayerScoutingReportView` to the import and add `path("<uuid:pk>/scouting-report/", PlayerScoutingReportView.as_view(), name="player-scouting-report")` (place it BEFORE the bare `<uuid:pk>/` detail route is fine since the suffix differentiates them; keep search/ ordering intact).

    Add endpoint integration tests to players/tests/test_scouting_report_view.py (same file as Task 1):
    - auth_client fixture (APIClient + force_authenticate) mirroring test_search_view.py.
    - test_scouting_report_success: patch `players.services.get_report_generator` to a fake returning a fully-grounded 5-section GeneratedReport, and patch `players.services.get_summary` to a small grounded dict; POST with {"club_id": "<uuid>"} to reverse("player-scouting-report", args=[player.id]); assert 200, response JSON has "narrative" (5 keys) and "grounding".
    - test_scouting_report_failure (this is the `-k failure` case, locked decision #6): patch the fake generator's generate to raise ReportGeneratorError; assert status 503 and that the body contains NO fabricated narrative sections (assert "narrative" not in response.json() or it's an error payload).
    - test_scouting_report_missing_club_returns_400: a player with club=None (PlayerFactory / a real club-less player) and no club_id in the body -> assert 400, not 404, and get_summary is never reached (assert the generator was not called).
    - Use a PlayerFactory or real_data_available as appropriate; for the club=None branch, follow test_views.py's established pattern of monkeypatching scoring calls so no real reconstruction runs against the empty test DB.
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest players/tests/test_scouting_report_view.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class PlayerScoutingReportView(APIView)" get-scouted-be/players/views.py` returns a match
    - `grep -n "HTTP_503_SERVICE_UNAVAILABLE" get-scouted-be/players/views.py` returns a match inside an `except ReportGeneratorError` block
    - `grep -n "HTTP_400_BAD_REQUEST" get-scouted-be/players/views.py` returns a match (club_id-missing clean 400)
    - `grep -n "scouting-report/" get-scouted-be/players/urls.py` returns a match
    - `grep -n "def test_scouting_report_failure\|def test_scouting_report_missing_club" get-scouted-be/players/tests/test_scouting_report_view.py` returns both
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_scouting_report_view.py -k failure -x` exits 0 (the clean-error-on-failure case runs and passes)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_scouting_report_view.py -x` exits 0
  </acceptance_criteria>
  <done>POST /api/players/{id}/scouting-report/ returns {narrative, grounding} on success, a clean 503 on ReportGeneratorError (never a fabricated report), and a clean 400 when no club context can be resolved -- all proven with an injected fake generator.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && .venv/bin/pytest players/tests/test_scouting_report_view.py -x` -- green (both service and endpoint tests).
- `cd get-scouted-be && .venv/bin/pytest players -q` -- no regression to Phase 7/9 players tests; the autouse _block_real_anthropic_calls guard confirms no real client is constructed.
</verification>

<success_criteria>
- generate_scouting_report composes get_summary grounding + the provider-agnostic generator; ReportGeneratorError propagates (never swallowed).
- The endpoint returns structured {narrative, grounding} or a clean 503/400 error -- never a fabricated report.
- Every number in a successful narrative is grounding-validated (guaranteed by the 10-02 generator this slice consumes).
</success_criteria>

<output>
After completion, create `.planning/phases/10-ai-grounded-report-generation/10-03-SUMMARY.md`.
</output>

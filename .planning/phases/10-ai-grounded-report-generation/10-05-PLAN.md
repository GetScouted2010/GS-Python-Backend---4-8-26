---
phase: 10-ai-grounded-report-generation
plan: 05
type: execute
wave: 4
depends_on: ["10-03", "10-04"]
files_modified:
  - .planning/phases/10-ai-grounded-report-generation/10-05-SUMMARY.md
autonomous: true
requirements: [AI-03, AI-04]

must_haves:
  truths:
    - "The full pytest suite passes with 0 failures and all 9 new Phase-10 test files are collected (not silently excluded)"
    - "Phases 7/8/9's existing players and clubs tests still pass -- zero regression introduced by the two new AI endpoints and the new clubs/services.py"
    - "The two new routes resolve: reverse('player-scouting-report') and reverse('club-insights') both point at their views"
    - "A live real-LLM verification is either performed (with a real ANTHROPIC_API_KEY) or explicitly recorded as a documented manual follow-up when no key is available, per the phase's live-DB verification convention"
  artifacts:
    - path: ".planning/phases/10-ai-grounded-report-generation/10-05-SUMMARY.md"
      provides: "recorded full-suite result (pass/fail counts), the 9 collected Phase-10 test files, and the live-verification outcome/deferral"
  key_links:
    - from: "the full pytest suite"
      to: "all Phase 7/8/9/10 tests"
      via: "single .venv/bin/pytest run reporting 0 failed"
      pattern: "0 failed|passed"
---

<objective>
Prove Phase 10 introduced zero regression: run the FULL test suite, confirm all nine new Phase-10 test files are collected and green, confirm Phases 7/8/9's players/clubs tests still pass, and record a live real-LLM spot-check (or its explicit deferral if no API key is available) — matching this project's established "always verify the full suite, and live-verify against the real dev DB" convention from every prior phase.

Purpose: The two AI endpoints and the new `clubs/services.py` touch shared files (players/clubs views, urls, services). A mocked-client unit/integration suite proves the plumbing, but only a full-suite run proves nothing downstream broke, and only a live call proves a real Anthropic response actually produces well-grounded prose. This plan is the phase gate.
Output: a single SUMMARY recording the full-suite result, the collected Phase-10 test files, and the live-verification outcome.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ai-grounded-report-generation/10-VALIDATION.md
@.planning/phases/10-ai-grounded-report-generation/10-CONTEXT.md

<interfaces>
<!-- The 9 Phase-10 test files that MUST be collected and green:
     players/tests/test_ai_report_generator.py (10-01)
     players/tests/test_ai_grounding.py (10-01)
     players/tests/test_ai_anthropic_report_generator.py (10-02)
     players/tests/test_ai_report_factory.py (10-02)
     players/tests/test_scouting_report_view.py (10-03)
     clubs/tests/test_ai_safety_net.py (10-04)
     clubs/tests/test_services_club_insights.py (10-04)
     clubs/tests/test_ai_club_insights.py (10-04) -->
<!-- pyproject.toml testpaths already includes both "players" and "clubs" (VERIFIED in research)
     -- no testpaths edit needed; but confirm collection anyway, since Phase 8's 08-06 discovered
     a real "app silently excluded from testpaths" gap. -->
<!-- Live verification needs ANTHROPIC_API_KEY. Phase 9 (09-01) recorded no local key was available;
     if still unavailable, the autouse guard means a live call cannot run in-process -- record the
     live check as a documented manual follow-up rather than faking it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite regression run + collection check + live-verification record</name>
  <files>.planning/phases/10-ai-grounded-report-generation/10-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/10-ai-grounded-report-generation/10-VALIDATION.md (the Sampling Rate "Phase gate" row + the Manual-Only Verifications table -- the exact live-check this task records)
    - get-scouted-be/pyproject.toml (confirm testpaths includes both players and clubs, so all 9 Phase-10 test files are actually collected)
  </read_first>
  <action>
    1. Run the FULL suite and capture the summary line:
       `cd get-scouted-be && .venv/bin/pytest -q`
       Record the exact passed/skipped/failed counts. It MUST report 0 failed.
    2. Confirm all nine Phase-10 test files are collected (not silently skipped/excluded):
       `cd get-scouted-be && .venv/bin/pytest --collect-only -q players/tests/test_ai_report_generator.py players/tests/test_ai_grounding.py players/tests/test_ai_anthropic_report_generator.py players/tests/test_ai_report_factory.py players/tests/test_scouting_report_view.py clubs/tests/test_ai_safety_net.py clubs/tests/test_services_club_insights.py clubs/tests/test_ai_club_insights.py`
       Record the collected test count per file; every file MUST collect at least one test.
    3. Confirm zero regression to prior-phase tests specifically:
       `cd get-scouted-be && .venv/bin/pytest players clubs -q` -- record passed/failed; MUST be 0 failed. (Note any PRE-EXISTING known failures from deferred-items.md, e.g. accounts test_director_read_only_visibility, are out of scope for this phase and must be distinguished from any NEW failure.)
    4. Confirm the two new routes resolve via a Django shell check:
       `cd get-scouted-be && .venv/bin/python manage.py shell -c "from django.urls import reverse; import uuid; u=uuid.uuid4(); print(reverse('player-scouting-report', args=[u])); print(reverse('club-insights', args=[u]))"`
       Record both printed URLs (they should end in /scouting-report/ and /insights/).
    5. Live real-LLM verification (phase-gate manual check per 10-VALIDATION.md):
       - If `ANTHROPIC_API_KEY` is configured AND real dev-DB data is present: run one real scouting-report request and one real club-insights request via `manage.py shell` (call `players.services.generate_scouting_report(<real player id>, <real club id>)` and `clubs.services.generate_club_insights(<real club id>)`), and manually confirm the narrative reads sensibly and every number traces to the grounding dict. Record a short excerpt + the grounding-vs-narrative check result.
       - If NO API key is available (as recorded for Phase 9): DO NOT fake it. Record in the SUMMARY that the live real-LLM narrative check is a documented manual follow-up requiring a real ANTHROPIC_API_KEY, exactly as 09-01 handled the same constraint. The mocked-client suite already proves the plumbing/validator logic.
    6. Write `.planning/phases/10-ai-grounded-report-generation/10-05-SUMMARY.md` recording: the full-suite counts, the 9 collected test files with their test counts, the players+clubs regression result, the two resolved route URLs, and the live-verification outcome (performed, with excerpt, OR explicitly deferred with reason).
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && .venv/bin/pytest -q` exits 0 (0 failed)
    - `cd get-scouted-be && .venv/bin/pytest --collect-only -q clubs/tests/test_ai_club_insights.py clubs/tests/test_services_club_insights.py clubs/tests/test_ai_safety_net.py players/tests/test_scouting_report_view.py` collects at least one test from each of the four files (exit 0)
    - `cd get-scouted-be && .venv/bin/pytest players clubs -q` exits 0 (no new regression; any pre-existing accounts failure is out of this scope and not counted here)
    - SUMMARY file exists and records: full-suite passed/skipped/failed counts, all 9 Phase-10 test files as collected, both resolved route URLs, and the live-verification outcome (performed OR explicitly deferred with reason)
    - `grep -n "scouting-report/\|insights/" .planning/phases/10-ai-grounded-report-generation/10-05-SUMMARY.md` returns matches (the resolved routes were recorded)
  </acceptance_criteria>
  <done>The full suite is green with all 9 Phase-10 test files collected, Phases 7/8/9 show zero new regression, both new routes resolve, and the live real-LLM check is either performed or explicitly deferred with reason -- all recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && .venv/bin/pytest -q` -- 0 failed, all Phase-10 test files collected.
- SUMMARY records the counts, collected files, resolved routes, and live-verification outcome.
</verification>

<success_criteria>
- Full suite green with zero NEW regression to Phases 7/8/9.
- All 9 Phase-10 test files collected (not silently excluded, guarding against the 08-06-class testpaths gap).
- Live real-LLM narrative check performed against the real dev DB, or explicitly deferred with a recorded reason when no API key is available.
</success_criteria>

<output>
After completion, `.planning/phases/10-ai-grounded-report-generation/10-05-SUMMARY.md` exists with the full regression + live-verification record.
</output>

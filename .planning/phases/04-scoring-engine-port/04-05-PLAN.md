---
phase: 04-scoring-engine-port
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - get-scouted-be/scoring/services/summary.py
  - get-scouted-be/scoring/serializers.py
  - get-scouted-be/scoring/tests/test_services_summary.py
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04, SCORE-05]

must_haves:
  truths:
    - "get_summary(player_id, club_id) returns all four scores (rmm, compatibility, financial_fit, transfer_probability) each with its full breakdown, in one response, matching the PRD Player Profile page"
    - "The summary reconstructs the population ONCE and runs the RMM-first score_population sequence ONCE, then reuses the *_from_row / *_from_scored / financial_fit_from_population helpers — RMM is computed before CS/TP here too"
    - "Each of the four sub-scores independently degrades to its own null+reason envelope when its data is missing, without failing the whole summary"
    - "serializers.py defines the output contract for all 5 endpoint responses, including the shared null+reason envelope"
  artifacts:
    - path: "get-scouted-be/scoring/services/summary.py"
      provides: "get_summary orchestrator reusing all 4 per-score helpers off one reconstruction"
      contains: "score_population"
      min_lines: 40
    - path: "get-scouted-be/scoring/serializers.py"
      provides: "DRF serializers/response-shape definitions for the 5 endpoints + null envelope"
      contains: "Serializer"
  key_links:
    - from: "get-scouted-be/scoring/services/summary.py"
      to: "rmm.rmm_breakdown_from_scored + compatibility.cs_breakdown_from_row + transfer_probability.tp_breakdown_from_row + financial_fit.financial_fit_from_population"
      via: "one reconstruct_population + score_population, results sliced per-player and passed to each helper"
      pattern: "financial_fit_from_population"
---

<objective>
Build the combined summary orchestrator (all four scores in one response for the PRD Player Profile page) and the serializers.py output contract for all five endpoints.

Purpose: Satisfies the combined-summary decision from CONTEXT (avoids the frontend making 4 round-trips) while reusing every per-score helper Plans 02-04 exposed, off a single population reconstruction — honoring the RMM-first sequencing once. serializers.py documents the locked response shapes (per-score breakdowns + shared null+reason envelope) that the views (Plan 06) render.
Output: `scoring/services/summary.py`, `scoring/serializers.py`, + summary tests.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scoring-engine-port/04-CONTEXT.md
@.planning/phases/04-scoring-engine-port/04-RESEARCH.md

<interfaces>
Reusable per-score helpers (from Plans 02-04):
  scoring/services/rmm.py:                  `rmm_breakdown_from_scored(scored_row)` -> dict
  scoring/services/compatibility.py:        `cs_breakdown_from_row(cs_tp_row, player_row, club_name, team_styles_df)` -> dict
  scoring/services/transfer_probability.py: `tp_breakdown_from_row(cs_tp_row)` -> dict
  scoring/services/financial_fit.py:        `financial_fit_from_population(player_id, club_name, pop)` -> dict
Shared substrate (Plan 01):
  scoring/services/population.py:  `reconstruct_population() -> Population`, `score_population(pop, club_name) -> (scored_df, cs_tp_df)`, `resolve_club_name(club_id)`
  scoring/exceptions.py:           `null_with_reason(field, code)`

scored_df is keyed rows (has "player_id" col + "Player Impact*"/"Impact Comp -*"); cs_tp_df is indexed by player_id.
player_id values are Player UUID objects — coerce to str to match the URL id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the summary orchestrator (RED)</name>
  <files>get-scouted-be/scoring/tests/test_services_summary.py</files>
  <read_first>
    - get-scouted-be/scoring/services/rmm.py, compatibility.py, transfer_probability.py, financial_fit.py (the helper signatures to compose)
    - get-scouted-be/scoring/services/population.py (score_population)
    - get-scouted-be/scoring/tests/conftest.py (real_data_available)
  </read_first>
  <behavior>
    - Test (real_data_available): get_summary(real_player_id, real_club_id) returns a dict with exactly the top-level keys {"rmm", "compatibility", "financial_fit", "transfer_probability"}, each value being either its breakdown dict or its null+reason envelope
    - Test: the rmm sub-object carries a "components" breakdown; compatibility carries role_fit_score/similarity_pct/bonus; financial_fit carries predicted_fee (money-scale > 1000 when present) + value_verdict; transfer_probability carries the 4 weighted terms
    - Test: reconstruction happens once — monkeypatch/patch `reconstruct_population` and assert it is called exactly once per get_summary call (call_count == 1), proving the summary does NOT reconstruct 4x
    - Test: one missing sub-score (e.g. club with no style → CS null) does not raise; the other three still return their values
  </behavior>
  <action>
    Create `get-scouted-be/scoring/tests/test_services_summary.py`. Import `get_summary` from `scoring.services.summary` (not yet built — RED). Use `unittest.mock.patch` on `scoring.services.summary.reconstruct_population` (or wherever it is imported) to assert `call_count == 1`. Use `real_data_available` for the end-to-end shape test.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest scoring/tests/test_services_summary.py -x 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/tests/test_services_summary.py` exists and references `get_summary`
    - grep `call_count` in the file returns a match (single-reconstruction assertion)
    - Test run fails (RED) with ImportError for `scoring.services.summary`
  </acceptance_criteria>
  <done>Summary test file exists asserting single reconstruction (call_count==1) + 4 score keys; RED.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the summary orchestrator (GREEN)</name>
  <files>get-scouted-be/scoring/services/summary.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/test_services_summary.py (implement to pass, incl. the single-reconstruction assertion)
    - get-scouted-be/scoring/services/population.py (reconstruct_population, score_population)
    - get-scouted-be/scoring/services/rmm.py / compatibility.py / transfer_probability.py / financial_fit.py (helpers)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/services/summary.py` with `get_summary(player_id, club_id)`:
    - `club_name = resolve_club_name(club_id)`
    - `pop = reconstruct_population()`  (exactly ONCE)
    - `scored, cs_tp = score_population(pop, club_name)`  (RMM-first sequence, ONCE)
    - Slice the player: coerce str(player_id); locate scored_row (from `scored`), player_row (from `pop.players_df`), cs_tp_row (from `cs_tp`). If the player is absent entirely → `raise Http404`.
    - Compose, each independently guarded so a missing sub-score doesn't break the others:
      ```python
      return {
          "rmm": rmm_breakdown_from_scored(scored_row),
          "compatibility": cs_breakdown_from_row(cs_tp_row, player_row, club_name, pop.team_styles_df),
          "financial_fit": financial_fit_from_population(player_id, club_name, pop),
          "transfer_probability": tp_breakdown_from_row(cs_tp_row),
      }
      ```
      (Each helper already returns its own null+reason envelope when its data is missing — do not wrap in broad try/except that would swallow reconstruction ValueErrors; let genuine ValueErrors propagate per Pitfall 5.)
    Reuse the already-reconstructed `pop` for financial_fit via `financial_fit_from_population` — do NOT call the high-level `get_financial_fit` (which would reconstruct again).
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest scoring/tests/test_services_summary.py -x 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/services/summary.py` contains `def get_summary`
    - grep `reconstruct_population` summary.py appears exactly once as a call site (single reconstruction)
    - grep `financial_fit_from_population` summary.py returns a match (reuses population; does NOT call get_financial_fit)
    - grep `score_population` summary.py returns a match
    - `python -m pytest scoring/tests/test_services_summary.py -x` exits 0
  </acceptance_criteria>
  <done>get_summary returns all 4 scores+breakdowns from ONE reconstruct_population + score_population; test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Define serializers.py output contract for all 5 endpoints</name>
  <files>get-scouted-be/scoring/serializers.py</files>
  <read_first>
    - get-scouted-be/scoring/services/summary.py (the composed response shape)
    - get-scouted-be/scoring/services/rmm.py / compatibility.py / transfer_probability.py / financial_fit.py (each response dict shape)
    - get-scouted-be/accounts/serializers.py (existing DRF serializer conventions to mirror)
    - get-scouted-be/scoring/exceptions.py (null envelope shape)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/serializers.py` with DRF serializers documenting the output contract for the 5 endpoints. Because the service functions already return well-formed dicts (with breakdowns and null+reason envelopes), these serializers are thin pass-through/validation shapes for browsable-API rendering and contract documentation. Define:
    - `RMMSerializer` — fields: rmm (float, nullable), positive, negative, components (dict), reliability, plus optional `reason` (for the null case)
    - `CompatibilitySerializer` — compatibility_score (nullable), components (role_fit_score, similarity_pct, bonus), reason
    - `FinancialFitSerializer` — predicted_fee (nullable), market_value, value_comparison (dict), value_verdict, buying_club, reason
    - `TransferProbabilitySerializer` — transfer_probability (nullable), components (4 weighted terms), reason
    - `SummarySerializer` — nests the above four under keys rmm/compatibility/financial_fit/transfer_probability
    Use `serializers.Serializer` with `serializers.DictField`/`JSONField` for the nested breakdown blocks where a strict nested schema would be brittle (component names are position-dependent for RMM). Keep the shared null envelope representable: allow the score field to be null and expose an optional `reason` string on every serializer. Add a module docstring stating these mirror the service-layer dicts and that services are the source of truth for shape.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.local'); django.setup(); import scoring.serializers as s; print([n for n in dir(s) if n.endswith('Serializer')])" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/serializers.py` contains `RMMSerializer`, `CompatibilitySerializer`, `FinancialFitSerializer`, `TransferProbabilitySerializer`, `SummarySerializer`
    - The import-and-list command above prints all five serializer names without error
    - grep `reason` serializers.py returns a match (null+reason envelope representable)
  </acceptance_criteria>
  <done>Five serializers (RMM/Compatibility/FinancialFit/TransferProbability/Summary) import cleanly under Django with reason field.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && python -m pytest scoring/tests/test_services_summary.py -x` passes
- serializers.py imports cleanly under Django
- `python -m pytest scoring -x` still green
- No edits to any `scoring/characterization/` file
</verification>

<success_criteria>
- Combined summary returns all 4 scores + breakdowns in one call, off a single population reconstruction with RMM-first ordering
- serializers.py documents the 5-endpoint output contract including the shared null+reason envelope
</success_criteria>

<output>
After completion, create `.planning/phases/04-scoring-engine-port/04-05-SUMMARY.md`
</output>

---
phase: 04-scoring-engine-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-scouted-be/scoring/services/__init__.py
  - get-scouted-be/scoring/exceptions.py
  - get-scouted-be/scoring/services/population.py
  - get-scouted-be/scoring/tests/test_services_population.py
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04, SCORE-05]

must_haves:
  truths:
    - "A single reconstruct_population() call returns the four script-shaped DataFrames (players_df, role_scores_wide, team_styles_df, transfers_df) every score service needs, built once from the ORM"
    - "score_population(pop, club_name) computes RMM FIRST via add_player_impact, merges it onto players_df as player_impact, THEN calls compute_cs_tp_for_pairs — the exact wiring order proven by generate_scoring_oracle.py, never reordered"
    - "resolve_club_name(club_id) returns Club.name (a string), raising Http404 for an unknown UUID — no characterization code ever receives a UUID"
    - "The trained TFM joblib Pipeline is loaded at most once (memoized), and its authoritative feature_cols come from the .metrics.json sidecar, not a hardcoded list"
    - "One shared null_with_reason(field, code) helper produces the {\"<field>\": null, \"reason\": \"<code>\"} envelope used identically by all 4 scores"
  artifacts:
    - path: "get-scouted-be/scoring/services/population.py"
      provides: "reconstruct_population, score_population (RMM-first sequence), resolve_club_name, get_tfm_pipeline"
      contains: "def score_population"
      min_lines: 60
    - path: "get-scouted-be/scoring/exceptions.py"
      provides: "null_with_reason shared envelope helper"
      contains: "def null_with_reason"
    - path: "get-scouted-be/scoring/services/__init__.py"
      provides: "services package marker"
  key_links:
    - from: "get-scouted-be/scoring/services/population.py"
      to: "scoring.characterization.impact.add_player_impact + deterministic_scores.compute_cs_tp_for_pairs"
      via: "RMM computed and merged onto players_df BEFORE compute_cs_tp_for_pairs is called"
      pattern: "add_player_impact"
    - from: "get-scouted-be/scoring/services/population.py"
      to: "get-scouted-be/scoring/ml_artifacts/tfm_value_model_v1.joblib + .metrics.json"
      via: "lru_cache-memoized joblib.load + json feature_cols read"
      pattern: "joblib\\.load"
---

<objective>
Build the shared service-layer substrate every Phase 4 score endpoint stands on: a single population-reconstruction + canonical-scoring-sequence module, a memoized TFM artifact loader, a UUID→club-name resolver, and the one shared null+reason envelope helper.

Purpose: All four score services and the summary endpoint reuse the exact reconstruct→RMM→CS/TP wiring order (proven correct in Phase 3's generate_scoring_oracle.py) and the same missing-data envelope. Centralizing this here prevents each of the four downstream service plans from reinventing (and drifting on) the load-bearing sequencing, club-name resolution, and envelope shape.
Output: `scoring/services/population.py`, `scoring/exceptions.py`, `scoring/services/__init__.py`, and their tests.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scoring-engine-port/04-CONTEXT.md
@.planning/phases/04-scoring-engine-port/04-RESEARCH.md

<interfaces>
<!-- Signatures the executor wraps. Extracted from the actual Phase 3 code — use directly, do NOT explore. -->

From scoring/characterization/reconstruct.py (all take NO filtering args — return whole tables):
```python
def build_players_df() -> pd.DataFrame          # columns include: player_id (Player UUID), Team (=Club.name), Main_Position, Position, Age, "Market value", "Contract expires"
def build_role_scores_wide() -> pd.DataFrame    # indexed/keyed by player_id
def build_team_styles_df() -> pd.DataFrame       # one row per Club; "Team" column = Club.name, + 8 style columns
def build_transfers_df() -> pd.DataFrame
```

From scoring/characterization/impact.py:
```python
def add_player_impact(df) -> pd.DataFrame        # returns df + "Player Impact", "Player Impact Positive", "Player Impact Negative", "Impact Reliability", and "Impact Comp - <name>" columns
def compute_rmm_column(players_df) -> pd.Series   # thin: player_id -> RMM only, DISCARDS components. Do NOT use when a breakdown is needed.
```

From scoring/characterization/deterministic_scores.py:
```python
def compute_cs_tp_for_pairs(
    players_df, team_styles_df, role_scores_wide,
    club_context: str | None,    # a Club NAME string (NOT a UUID); None => each player vs own club
    player_impact: pd.Series,    # REQUIRED — RMM keyed by player_id; raises ValueError if absent
    as_of_year: int | None = None,
) -> pd.DataFrame  # indexed by player_id, columns: club_context, compatibility_score, financial_score,
                   # performance_score, contract_fit, role_pct, transfer_probability
```

From clubs/models.py: `Club` has a UUID primary key `id` and a `name` field.
TFM artifact: `scoring/ml_artifacts/tfm_value_model_v1.joblib` (joblib Pipeline), sidecar `tfm_value_model_v1.metrics.json` with a `"feature_cols"` array (23 entries) and `"sklearn_version": "1.9.0"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the population substrate</name>
  <files>get-scouted-be/scoring/services/__init__.py, get-scouted-be/scoring/tests/test_services_population.py</files>
  <read_first>
    - get-scouted-be/scoring/characterization/reconstruct.py (build_* signatures, player_id/Team column names)
    - get-scouted-be/scoring/characterization/deterministic_scores.py (compute_cs_tp_for_pairs signature + returned columns + the player_impact-required ValueError guard)
    - get-scouted-be/scoring/tests/conftest.py (the real_data_available fixture pattern to reuse)
    - get-scouted-be/scoring/tests/test_deterministic_scores.py (existing synthetic-fixture test style to mirror)
    - get-scouted-be/scoring/ml_artifacts/tfm_value_model_v1.metrics.json (the feature_cols source of truth)
  </read_first>
  <behavior>
    - Test: null_with_reason("compatibility_score", "club_style_data_unavailable") == {"compatibility_score": None, "reason": "club_style_data_unavailable"}
    - Test: resolve_club_name(random_uuid_not_in_db) raises django.http.Http404 (use pytest.raises + @pytest.mark.django_db)
    - Test: get_tfm_pipeline() returns a tuple (pipeline, feature_cols) where feature_cols == json.load(open(metrics.json))["feature_cols"] (23 entries) and calling get_tfm_pipeline() twice returns the SAME pipeline object (identity check `is`, proving memoization)
    - Test (real_data_available): reconstruct_population() returns an object/namedtuple whose players_df has columns "player_id" and "Team"; score_population(pop, None) returns (scored_df, cs_tp_df) where scored_df contains "Player Impact" AND players_df now carries a "player_impact" column, and cs_tp_df has columns compatibility_score, performance_score, financial_score, contract_fit, transfer_probability
    - Test: score_population never calls compute_cs_tp_for_pairs without player_impact (assert cs_tp_df has no NaN-from-missing-impact anomaly — simplest form: assert "player_impact" is a column on the players_df passed in, and cs_tp_df is non-empty)
  </behavior>
  <action>
    Create `get-scouted-be/scoring/services/__init__.py` (empty package marker so imports resolve).
    Create `get-scouted-be/scoring/tests/test_services_population.py` with the tests described in <behavior>. Import from `scoring.exceptions` and `scoring.services.population` (these do not exist yet — the tests MUST fail at import/collection, which is the RED state). Reuse the `real_data_available` fixture from conftest.py for the reconstruct/score tests so they skip cleanly on an empty DB. For the resolve_club_name 404 test, generate a random uuid4 and assert `Http404` is raised. For get_tfm_pipeline, read the sidecar path as `settings.BASE_DIR`-relative or via the same path the loader uses; assert the returned feature_cols equals the json array exactly (list equality) and that repeated calls return an identical object.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest scoring/tests/test_services_population.py --collect-only -q 2>&1 | tail -5; python -m pytest scoring/tests/test_services_population.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/services/__init__.py` exists
    - `get-scouted-be/scoring/tests/test_services_population.py` exists and contains `null_with_reason`, `resolve_club_name`, `get_tfm_pipeline`, `score_population`, and `reconstruct_population`
    - Running the test file fails (RED) with ImportError/ModuleNotFoundError for `scoring.services.population` or `scoring.exceptions` (proves tests reference not-yet-built code)
    - grep `real_data_available` scoring/tests/test_services_population.py returns a match (DB-touching tests skip on empty DB)
  </acceptance_criteria>
  <done>Package marker + failing test file exist; test run RED with ImportError for scoring.services.population/scoring.exceptions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the envelope helper and population substrate (GREEN)</name>
  <files>get-scouted-be/scoring/exceptions.py, get-scouted-be/scoring/services/population.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/test_services_population.py (the tests just written — implement to pass them)
    - get-scouted-be/scoring/management/commands/generate_scoring_oracle.py (lines ~150-210: the canonical reconstruct → add_player_impact/compute_rmm → merge player_impact → compute_cs_tp_for_pairs order to replicate)
    - get-scouted-be/scoring/characterization/impact.py (add_player_impact around lines 916-1039: what columns it attaches)
    - get-scouted-be/scoring/characterization/deterministic_scores.py (compute_cs_tp_for_pairs)
    - get-scouted-be/scoring/ml_artifacts/tfm_value_model_v1.metrics.json
  </read_first>
  <action>
    Create `get-scouted-be/scoring/exceptions.py` with:
    ```python
    def null_with_reason(field: str, code: str) -> dict:
        """Shared missing-data envelope for all 4 scores (CONTEXT.md locked shape)."""
        return {field: None, "reason": code}
    ```
    Create `get-scouted-be/scoring/services/population.py` with these four callables:

    1. `get_tfm_pipeline()` — memoized with `functools.lru_cache(maxsize=1)`. Resolves the artifact + sidecar paths relative to this module's directory (e.g. `Path(__file__).resolve().parent.parent / "ml_artifacts" / "tfm_value_model_v1.joblib"` and `...metrics.json`). Returns `(pipeline, feature_cols)` where `pipeline = joblib.load(artifact_path)` and `feature_cols = json.load(open(metrics_path))["feature_cols"]`. Do NOT hardcode the 23 feature names — always read them from the sidecar.

    2. `resolve_club_name(club_id) -> str` — `from django.shortcuts import get_object_or_404`; `from clubs.models import Club`; return `get_object_or_404(Club, id=club_id).name`. Never returns/accepts a UUID beyond this call.

    3. `reconstruct_population()` — call `build_players_df()`, `build_role_scores_wide()`, `build_team_styles_df()`, `build_transfers_df()` once each and return them bundled (a `dataclasses.dataclass` or `typing.NamedTuple` named `Population` with fields `players_df, role_scores_wide, team_styles_df, transfers_df`). This is the single reconstruction point downstream services reuse.

    4. `score_population(pop, club_name)` — replicate generate_scoring_oracle.py's proven order EXACTLY:
       - `scored = add_player_impact(pop.players_df.copy())`  # full df WITH components (not compute_rmm_column — summary/RMM need the breakdown)
       - `rmm_series = scored.set_index("player_id")["Player Impact"]`
       - `players_df = pop.players_df.copy(); players_df["player_impact"] = players_df["player_id"].map(rmm_series)`
       - `cs_tp = compute_cs_tp_for_pairs(players_df, pop.team_styles_df, pop.role_scores_wide, club_context=club_name, player_impact=rmm_series)`
       - `return scored, cs_tp`
       RMM MUST be merged before compute_cs_tp_for_pairs (that function raises ValueError if player_impact is missing — this ordering is load-bearing, do not reorder).
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest scoring/tests/test_services_population.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/exceptions.py` contains `def null_with_reason`
    - `get-scouted-be/scoring/services/population.py` contains `def score_population`, `def reconstruct_population`, `def resolve_club_name`, `def get_tfm_pipeline`
    - grep `lru_cache` scoring/services/population.py returns a match
    - grep `add_player_impact` scoring/services/population.py returns a match AND appears on a line BEFORE the `compute_cs_tp_for_pairs` call (RMM-first ordering)
    - grep `json` and `metrics.json` present in population.py (feature_cols read from sidecar, not hardcoded)
    - `python -m pytest scoring/tests/test_services_population.py -x` exits 0
  </acceptance_criteria>
  <done>null_with_reason + reconstruct_population/score_population/resolve_club_name/get_tfm_pipeline implemented; test_services_population.py passes.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && python -m pytest scoring/tests/test_services_population.py -x` passes
- `python -m pytest scoring -x` still green (no regression of Phase 3 tests)
- No changes to any file under `scoring/characterization/` (Phase 3 modules stay byte-for-byte)
</verification>

<success_criteria>
- The shared substrate (population reconstruction, RMM-first scoring sequence, club-name resolution, memoized TFM loader, null envelope) exists and is tested
- Downstream service plans (02-05) can import `score_population`, `reconstruct_population`, `resolve_club_name`, `get_tfm_pipeline`, and `null_with_reason` without rebuilding any of it
</success_criteria>

<output>
After completion, create `.planning/phases/04-scoring-engine-port/04-01-SUMMARY.md`
</output>

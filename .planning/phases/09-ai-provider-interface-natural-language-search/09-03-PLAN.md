---
phase: 09-ai-provider-interface-natural-language-search
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - get-scouted-be/players/ai/fallback.py
  - get-scouted-be/players/services.py
  - get-scouted-be/players/tests/test_ai_fallback.py
  - get-scouted-be/players/tests/test_services_search.py
autonomous: true
requirements: [AI-01, AI-02]

must_haves:
  truths:
    - "The deterministic keyword extractor turns 'young left-backs under €5M' into real whitelist filters (position=LB, market_value_max=5000000) with no LLM/network involved"
    - "The keyword extractor returns {} (not an error) when it can extract nothing usable"
    - "search_players(filters, request) composes PlayerFilter + a SEPARATE manual club__<field>__gte style step + IdsBypassPagination + PlayerListSerializer, and returns the same paginated envelope as GET /api/players/"
    - "A style filter key like club__control_possession_min round-trips to a correct .filter(club__control_possession__gte=value) ORM call, never a FieldError"
  artifacts:
    - path: "get-scouted-be/players/ai/fallback.py"
      provides: "keyword_extract(query) -> dict — tier-2 deterministic regex/synonym extractor"
      contains: "def keyword_extract"
    - path: "get-scouted-be/players/services.py"
      provides: "search_players(filters, request) -> paginated dict envelope"
      contains: "def search_players"
  key_links:
    - from: "players/services.py"
      to: "PlayerFilter"
      via: "PlayerFilter(data=player_filter_data, queryset=...).qs"
      pattern: "PlayerFilter\\(data="
    - from: "players/services.py"
      to: "Player queryset"
      via: "manual .filter(club__<field>__gte=value) for style terms"
      pattern: "club__.*__gte"
---

<objective>
Build the two deterministic, no-LLM pieces the search endpoint needs: (1) the tier-2 keyword fallback extractor that pulls whitelist filters out of free text using regex + synonym dictionaries (position synonyms, a "€5M"/"under 5m" money parser, league substring matching), and (2) the `search_players(filters, request)` service that composes Phase 7's existing, tested filter/pagination/serializer primitives against a filter dict — including the SEPARATE manual style-filter step that `PlayerFilter` cannot do.

Purpose: The keyword extractor guarantees AI-02's "never an error" fallback tier when the LLM call fails. The service function is the shared composition both the search view (Plan 04) and any future caller use, and it correctly handles the two research-flagged traps: PlayerFilter silently ignores `club__` lookups, and PlayerListView's bound methods must NOT be reused.
Output: `fallback.py`, `services.py`, and two comprehensive unit-test files (no network at all).
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-provider-interface-natural-language-search/09-CONTEXT.md
@.planning/phases/09-ai-provider-interface-natural-language-search/09-RESEARCH.md
@.planning/phases/09-ai-provider-interface-natural-language-search/09-VALIDATION.md

<interfaces>
<!-- Phase 7 primitives to REUSE (verified signatures): -->
<!-- players/filters.py::PlayerFilter -- declared filters: ids, position, league, age_min, age_max,
     market_value_min, market_value_max, impact_score_min, compatibility_score_min,
     financial_fit_score_min, transfer_probability_score_min. django_filters.FilterSet accepts
     any Mapping for data=, so a plain dict works: PlayerFilter(data=filters_dict, queryset=qs).qs
     CRITICAL: PlayerFilter has NO club__ filters and SILENTLY IGNORES undeclared keys (no error). -->
<!-- core/pagination.py::IdsBypassPagination.paginate_queryset(queryset, request, view=None) --
     reads request.query_params.get("ids"); for POST search there is no ids param so it paginates. -->
<!-- players/serializers.py::PlayerListSerializer(page, many=True).data -- the reused list shape. -->
<!-- PlayerListView default ordering (match it): ordering = ["-impact_score"] -->

<!-- The 8 real style fields (from clubs/filters.py), emitted by the parser as club__<field>_min: -->
control_possession, gegenpressing, direct_play, tiki_taka, counter_attack, wing_play, low_block, defensive_counter_attack

<!-- Real values for the tier-2 extractor (verified live, 09-RESEARCH.md Code Examples): -->
<!-- position (10): --> AM, CB, CM, DM, FWD, GK, LB, LW, RB, RW
<!-- league (25): --> Allsvenskan (Sweden), Bundesliga (Austria), Bundesliga (Germany), Bundesliga 2, Challenger Pro League, EFL Championship, EFL League One, Eerste Divisie, Eliteserien, Eredivisie (Netherlands), La Liga (Spain), La Liga 2, Liga Portugal 2, Ligue 1 (France), Ligue 2 (France), MLS (USA), Premier League (England), Primeira Liga (Portugal), Pro League (Belgium), SPL, Serie A (Brazil), Serie A (Italy), Serie B, Super Lig (Turkey), Superliga (Denmark)
<!-- market_value is a plain BigIntegerField in raw euros (max real 180_000_000). "under €5M" => market_value_max = 5_000_000. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier-2 keyword fallback extractor (deterministic, no LLM)</name>
  <files>get-scouted-be/players/ai/fallback.py, get-scouted-be/players/tests/test_ai_fallback.py</files>
  <read_first>
    - get-scouted-be/players/filters.py (the exact whitelist keys the output dict must use)
    - get-scouted-be/players/ai/base.py (context: this returns a plain filters dict; the view wraps it — it does NOT need to import base.py)
    - get-scouted-be/players/tests/conftest.py (the autouse safety-net is active but irrelevant here — this module never touches anthropic)
  </read_first>
  <behavior>
    - Test 1 (money, keyword subset): `keyword_extract("strikers under €5M")` -> includes `market_value_max == 5000000` (parse "€5M"/"5m"/"5 million" to 5_000_000 raw euros, NOT 5). Also test "over 10m" -> `market_value_min == 10000000`.
    - Test 2 (position synonyms, keyword subset): "centre back"/"center back"/"CB"/"defender(centre)" -> `position == "CB"`; "left-back"/"left back"/"LB" -> `position == "LB"`; "striker"/"forward"/"FWD" -> `position == "FWD"`; "keeper"/"goalkeeper"/"GK" -> `position == "GK"`. Synonyms map onto ONLY the 10 real position codes.
    - Test 3 (league substring, keyword subset): "plays in the Premier League" -> `league == "Premier League (England)"`; an unambiguous full-name match works.
    - Test 4 (ambiguous league): "bundesliga" alone (matches 3 real leagues: Austria/Germany/Bundesliga 2) MUST NOT guess one arbitrarily — either omit `league` entirely or only set it when a country qualifier disambiguates (e.g. "german bundesliga" -> "Bundesliga (Germany)"). Assert the ambiguous bare "bundesliga" does NOT set a single guessed league. Same principle for bare "serie" (Serie A Brazil / Serie A Italy / Serie B).
    - Test 5 (nothing usable): `keyword_extract("asdfghjkl nonsense")` -> `{}` (empty dict, never an exception).
    - Test 6 (combined): "young left backs under 5m in ligue 1" -> `{position: "LB", market_value_max: 5000000, league: "Ligue 1 (France)"}` (age term "young" may be ignored or mapped to age_max — document which; NOT required to map "young").
  </behavior>
  <action>
    Create `get-scouted-be/players/ai/fallback.py` with `def keyword_extract(query: str) -> dict:` using only stdlib `re`. Build:
    1. `POSITION_SYNONYMS`: a dict mapping lowercased synonym phrases to one of the 10 real codes (AM/CB/CM/DM/FWD/GK/LB/LW/RB/RW). Match longest/most-specific phrases first (e.g. "centre back" before "back") to avoid mis-tagging "left back" as "back".
    2. A money regex: match a number (optionally with decimal) followed by an m/million or k/thousand suffix and/or a preceding €/$, plus a direction word ("under"/"below"/"less than" -> `market_value_max`; "over"/"above"/"more than" -> `market_value_min`). Convert `Xm`/`X million` -> `int(X * 1_000_000)`, `Xk` -> `int(X * 1_000)`. Default an amount with no direction word to `market_value_max` (a bare "€5M" reads as a budget ceiling — document this choice).
    3. A league matcher: case-insensitively match the query against the 25 real league strings. Only assign `league` when the match is UNAMBIGUOUS (exactly one real league contains the matched phrase). For shared base terms ("bundesliga", "serie", "la liga", "ligue", "efl") require the country/qualifier to also appear before assigning; otherwise leave `league` unset (never guess — mirrors this project's "degrade, never fabricate" principle).
    4. Return only the keys it confidently extracted; `{}` if none. Keys MUST be exact PlayerFilter whitelist keys.
    Then write `get-scouted-be/players/tests/test_ai_fallback.py` implementing behavior tests 1-6. Ensure the "keyword" subset (Tests 1-3) is selectable via `pytest -k keyword` by naming those tests with `keyword` in the function name (the validation map references `-k keyword`).
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_fallback.py -x -q && .venv/bin/pytest players/tests/test_ai_fallback.py -k keyword -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def keyword_extract' get-scouted-be/players/ai/fallback.py` succeeds
    - `grep -q 'import re' get-scouted-be/players/ai/fallback.py` succeeds (stdlib only, no new dependency)
    - `grep -q '1_000_000\|1000000' get-scouted-be/players/ai/fallback.py` succeeds (money scaled to raw euros, not 5)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_fallback.py -q` — all 6 behavior tests pass
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_fallback.py -k keyword -q` selects and passes the money/position/league keyword tests
    - A test explicitly asserts bare "bundesliga" does NOT get assigned a single guessed league (ambiguity handled)
  </acceptance_criteria>
  <done>keyword_extract turns real free text into correct whitelist filters (money scaled to euros, positions mapped to the 10 real codes, leagues matched only when unambiguous) and returns {} rather than erroring when nothing is extractable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: search_players(filters, request) service — compose Phase 7 primitives + manual style step</name>
  <files>get-scouted-be/players/services.py, get-scouted-be/players/tests/test_services_search.py</files>
  <read_first>
    - get-scouted-be/players/filters.py (PlayerFilter — accepts data=dict; SILENTLY IGNORES club__ keys, so style filters CANNOT go through it)
    - get-scouted-be/players/views.py (PlayerListView — note ordering=["-impact_score"]; do NOT reuse its bound filter_queryset/get_queryset methods, they expect request.query_params)
    - get-scouted-be/core/pagination.py (IdsBypassPagination.paginate_queryset(queryset, request, view=None) — pass the REAL request object, not the filters dict)
    - get-scouted-be/players/serializers.py (PlayerListSerializer — the reused list shape)
    - get-scouted-be/clubs/filters.py (the 8 real style field names)
  </read_first>
  <behavior>
    - Test 1 (field-name transform regression — the flagged trap): calling the service (or a small helper it exposes) with `filters={"club__control_possession_min": 15}` produces an ORM filter equivalent to `.filter(club__control_possession__gte=15)` and does NOT raise `FieldError`. Assert by inspecting `qs.query` for `control_possession` + `gte`, OR by asserting no exception is raised against the real DB (skip-if-empty). The transform is: strip trailing `_min`, append `__gte`.
    - Test 2 (PlayerFilter path): `filters={"position": "CB", "age_max": 21}` yields a queryset restricted to CB players aged <= 21 (real-data test, `real_data_available` skip fixture).
    - Test 3 (style keys are NOT smuggled through PlayerFilter): confirm that passing `club__control_possession_min` inside the PlayerFilter `data` dict alone does nothing (documents WHY the manual step exists) — i.e. the service must apply it via the separate `.filter(...__gte=...)` step, and Test 1 proves that step fires.
    - Test 4 (paginated envelope): the return value is a dict containing `results` and `count` keys (same envelope shape as GET /api/players/), with `results` serialized via PlayerListSerializer.
    - Test 5 (empty filters): `filters={}` returns the full (paginated) player list, `count` > 0 (real-data test).
  </behavior>
  <action>
    Create `get-scouted-be/players/services.py` with `def search_players(filters: dict, request) -> dict:` composing the primitives DIRECTLY (do NOT invoke PlayerListView's bound methods — 09-RESEARCH.md Pattern 3):
    ```python
    from core.pagination import IdsBypassPagination
    from players.filters import PlayerFilter
    from players.models import Player
    from players.serializers import PlayerListSerializer

    STYLE_FIELDS = {
        "control_possession", "gegenpressing", "direct_play", "tiki_taka",
        "counter_attack", "wing_play", "low_block", "defensive_counter_attack",
    }

    def search_players(filters: dict, request) -> dict:
        # Split style filters (club__<field>_min) out — PlayerFilter has no club__
        # filters and would SILENTLY IGNORE them (no error). They must be applied
        # as a separate manual ORM step.
        style_filters, player_filter_data = {}, {}
        for key, value in filters.items():
            if key.startswith("club__") and key.removeprefix("club__").removesuffix("_min") in STYLE_FIELDS:
                style_filters[key] = value
            else:
                player_filter_data[key] = value

        qs = PlayerFilter(data=player_filter_data, queryset=Player.objects.all()).qs
        for key, value in style_filters.items():
            field = key.removesuffix("_min")            # e.g. "club__control_possession"
            qs = qs.filter(**{f"{field}__gte": value})   # -> club__control_possession__gte=value
        qs = qs.order_by("-impact_score")                # match PlayerListView's deterministic default

        paginator = IdsBypassPagination()
        page = paginator.paginate_queryset(qs, request, view=None)
        serialized = PlayerListSerializer(page, many=True).data
        return paginator.get_paginated_response(serialized).data
    ```
    Note the two separate params: `filters` (dict from the parser/fallback) drives PlayerFilter; `request` (the real DRF request, untouched) drives pagination — never conflate them (09-RESEARCH.md Pitfall 3). Then write `get-scouted-be/players/tests/test_services_search.py` implementing behavior tests 1-5. Name the field-name-transform test `test_style_filter_field_name_transform` (the validation map references it by that exact name). Build the DRF `request` via `rest_framework.test.APIRequestFactory().post("/api/players/search/")` and wrap with DRF's `Request` if needed for `.query_params`.
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest players/tests/test_services_search.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def search_players' get-scouted-be/players/services.py` succeeds
    - `grep -q 'PlayerFilter(data=' get-scouted-be/players/services.py` succeeds
    - `grep -Eq 'club__.*__gte|__gte' get-scouted-be/players/services.py` succeeds (manual style step present)
    - `grep -q 'order_by("-impact_score")' get-scouted-be/players/services.py` succeeds (matches PlayerListView default)
    - The service does NOT import or call `PlayerListView` (`! grep -q 'PlayerListView' get-scouted-be/players/services.py`)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_services_search.py::test_style_filter_field_name_transform -q` passes (no FieldError)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_services_search.py -q` — all tests pass (real-data tests skip cleanly on empty test DB)
  </acceptance_criteria>
  <done>search_players composes PlayerFilter + a separate manual club__<field>__gte style step + IdsBypassPagination + PlayerListSerializer into the standard paginated envelope; the style field-name transform is regression-tested; PlayerListView's request-bound methods are never reused.</done>
</task>

</tasks>

<verification>
- `keyword_extract` deterministically maps free text to real whitelist filters and returns {} (not an error) when nothing is extractable; ambiguous leagues are never guessed.
- `search_players` returns the standard paginated envelope, applies style filters via a separate manual `club__<field>__gte` step, and never raises FieldError.
- `cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_fallback.py players/tests/test_services_search.py -q` is green.
</verification>

<success_criteria>
- AI-02: a deterministic tier-2 extractor exists that can always produce a (possibly empty) filter set without erroring.
- AI-01: the search composition reuses Phase 7's tested filter/serializer/pagination surface rather than reinventing it, with the two research-flagged traps (silent club__ ignore; PlayerListView reuse) explicitly avoided.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-provider-interface-natural-language-search/09-03-SUMMARY.md`.
</output>

---
phase: 09-ai-provider-interface-natural-language-search
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - get-scouted-be/players/ai/anthropic_parser.py
  - get-scouted-be/players/ai/factory.py
  - get-scouted-be/players/tests/test_ai_anthropic_parser.py
  - get-scouted-be/players/tests/test_ai_factory.py
autonomous: true
requirements: [AI-01, AI-05]

must_haves:
  truths:
    - "AnthropicNLQueryParser.parse() maps a (mocked) tool-use response into a ParsedQuery whose filters are restricted to the real field whitelist"
    - "AnthropicNLQueryParser.parse() raises NLQueryParserError when the Anthropic call raises anthropic.APIError (rate-limit, timeout, 5xx, auth, etc.) OR when the tool response is malformed/empty"
    - "Invalid/hallucinated position or league values from the LLM are dropped, not passed into the ORM"
    - "get_nl_query_parser() returns an AnthropicNLQueryParser when LLM_PROVIDER=anthropic and raises ValueError for an unknown provider — with zero changes required in calling code to swap providers"
  artifacts:
    - path: "get-scouted-be/players/ai/anthropic_parser.py"
      provides: "AnthropicNLQueryParser(NLQueryParser) — forced tool-use structured extraction + defensive whitelist validation"
      contains: "class AnthropicNLQueryParser"
    - path: "get-scouted-be/players/ai/factory.py"
      provides: "get_nl_query_parser() provider selector reading settings.LLM_PROVIDER"
      contains: "def get_nl_query_parser"
  key_links:
    - from: "players/ai/anthropic_parser.py"
      to: "anthropic.APIError"
      via: "except anthropic.APIError -> raise NLQueryParserError"
      pattern: "except anthropic\\.APIError"
    - from: "players/ai/factory.py"
      to: "settings.LLM_PROVIDER"
      via: "provider dispatch"
      pattern: "settings\\.LLM_PROVIDER"
---

<objective>
Build the first concrete provider implementation behind the Plan 01 interface: `AnthropicNLQueryParser` (Anthropic Messages API, forced tool-use for schema-constrained extraction) plus the `get_nl_query_parser()` factory that selects it from `settings.LLM_PROVIDER`. This is the piece that proves the provider-agnostic interface (AI-05) actually works end-to-end, and does the real natural-language -> structured-filter mapping (AI-01).

Purpose: Tool-use gives reliable, schema-constrained output so parsed filters stay inside the fixed real-field whitelist and never invent filter dimensions. The factory means swapping providers later = one new class + one env var, zero changes to the search view/service.
Output: `anthropic_parser.py`, `factory.py`, and two fully-mocked test files (never a real API call).
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-provider-interface-natural-language-search/09-CONTEXT.md
@.planning/phases/09-ai-provider-interface-natural-language-search/09-RESEARCH.md
@.planning/phases/09-ai-provider-interface-natural-language-search/09-VALIDATION.md

<interfaces>
<!-- The contract this plan implements (from Plan 01, players/ai/base.py): -->
```python
@dataclass
class ParsedQuery:
    filters: dict
    raw_query: str

class NLQueryParserError(Exception): ...

class NLQueryParser(ABC):
    @abstractmethod
    def parse(self, query: str) -> ParsedQuery: ...
```

<!-- The EXACT whitelist keys the tool schema + validation must use (verified against players/filters.py + clubs/filters.py). -->
<!-- Player fields (PlayerFilter): -->
position, league, age_min, age_max, market_value_min, market_value_max,
impact_score_min, compatibility_score_min, financial_fit_score_min, transfer_probability_score_min
<!-- Style fields — emitted as club__<field>_min keys (the 8 ClubFilter style fields): -->
club__control_possession_min, club__gegenpressing_min, club__direct_play_min, club__tiki_taka_min,
club__counter_attack_min, club__wing_play_min, club__low_block_min, club__defensive_counter_attack_min

<!-- Real enum values to hardcode into the tool schema (verified live against the dev DB, 09-RESEARCH.md Code Examples): -->
<!-- position (10): --> AM, CB, CM, DM, FWD, GK, LB, LW, RB, RW
<!-- league (25): --> Allsvenskan (Sweden), Bundesliga (Austria), Bundesliga (Germany), Bundesliga 2, Challenger Pro League, EFL Championship, EFL League One, Eerste Divisie, Eliteserien, Eredivisie (Netherlands), La Liga (Spain), La Liga 2, Liga Portugal 2, Ligue 1 (France), Ligue 2 (France), MLS (USA), Premier League (England), Primeira Liga (Portugal), Pro League (Belgium), SPL, Serie A (Brazil), Serie A (Italy), Serie B, Super Lig (Turkey), Superliga (Denmark)

<!-- Anthropic SDK exception hierarchy (verified from SDK source, 09-RESEARCH.md): -->
<!-- AnthropicError -> APIError -> {APIStatusError -> [BadRequest, Authentication, PermissionDenied, NotFound, RateLimit, InternalServer, Overloaded, ...], APIConnectionError -> APITimeoutError} -->
<!-- => catching anthropic.APIError covers ALL real failure modes. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnthropicNLQueryParser — forced tool-use extraction + defensive whitelist validation</name>
  <files>get-scouted-be/players/ai/anthropic_parser.py, get-scouted-be/players/tests/test_ai_anthropic_parser.py</files>
  <read_first>
    - get-scouted-be/players/ai/base.py (the NLQueryParser ABC / ParsedQuery / NLQueryParserError contract you implement)
    - get-scouted-be/players/filters.py (the EXACT PlayerFilter field names — position, league, age_min/max, market_value_min/max, 4 score-threshold mins — the tool schema must match these)
    - get-scouted-be/clubs/filters.py (the 8 style field names that become club__<field>_min keys)
    - get-scouted-be/players/tests/conftest.py (the autouse `_block_real_anthropic_calls` guard from Plan 01 — your tests inject a fake client rather than relying on it, but understand it is active)
    - get-scouted-be/players/tests/test_views.py (the "patch the caller's own import binding" mocking convention)
  </read_first>
  <behavior>
    - Test 1 (happy path, mocked): a fake client whose `messages.create()` returns a response with a `tool_use` content block whose `.input == {"position": "LB", "market_value_max": 5000000}` -> `parse()` returns `ParsedQuery(filters={"position": "LB", "market_value_max": 5000000}, raw_query=<query>)`.
    - Test 2 (style field): `.input == {"club__control_possession_min": 15}` -> that key survives into `ParsedQuery.filters` unchanged.
    - Test 3 (invalid enum dropped): `.input == {"position": "STRIKER", "age_max": 21}` -> `"position"` is DROPPED (not in the 10 real values), `"age_max": 21` is kept. Filters must never contain a hallucinated position/league.
    - Test 4 (anthropic.APIError -> NLQueryParserError): fake client `messages.create()` raises `anthropic.RateLimitError` (a subclass of APIError) -> `parse()` raises `NLQueryParserError`. Repeat asserting the same for `anthropic.APITimeoutError` and `anthropic.APIConnectionError`.
    - Test 5 (malformed/empty response -> NLQueryParserError): response `.content` has NO `tool_use` block (only text) -> `parse()` raises `NLQueryParserError` (defensive `except (StopIteration, KeyError, TypeError, AttributeError)`).
    - Test 6 (no real network): assert that constructing `AnthropicNLQueryParser()` with no injected client and calling parse never reaches the network — the autouse guard would raise RuntimeError; the test injects a fake client so the guard is not triggered. (Documents the DI contract.)
  </behavior>
  <action>
    Create `get-scouted-be/players/ai/anthropic_parser.py` implementing `AnthropicNLQueryParser(NLQueryParser)`:
    1. Constructor supports dependency injection for testability: `def __init__(self, client=None): self._client = client`. Do NOT construct `anthropic.Anthropic(...)` in `__init__` — construct it lazily inside `parse()` only if `self._client is None`, as `anthropic.Anthropic(api_key=settings.ANTHROPIC_API_KEY)`. (Lazy construction + the injected-client seam is what lets tests avoid the autouse guard.)
    2. Define a module-level `FILTER_TOOL` dict: `{"name": "extract_player_filters", "description": "...omit fields the query does not address...", "input_schema": {"type": "object", "properties": {...}, "additionalProperties": False}}`. The `properties` MUST include every whitelist key from the interfaces block above: `position` (enum of the 10 real values), `league` (enum of the 25 real values), `age_min`/`age_max`/`market_value_min`/`market_value_max` (integer), the 4 `*_score_min` (number), and the 8 `club__<field>_min` (number). Hardcode the exact enum value lists from the interfaces block.
    3. In `parse(self, query)`:
       ```python
       import anthropic
       from django.conf import settings
       client = self._client or anthropic.Anthropic(api_key=settings.ANTHROPIC_API_KEY)
       try:
           response = client.messages.create(
               model=settings.ANTHROPIC_MODEL,
               max_tokens=512,
               tools=[FILTER_TOOL],
               tool_choice={"type": "tool", "name": "extract_player_filters"},
               messages=[{"role": "user", "content": query}],
           )
           tool_block = next(b for b in response.content if b.type == "tool_use")
           raw_filters = dict(tool_block.input)
       except anthropic.APIError as exc:
           raise NLQueryParserError("Anthropic API call failed") from exc
       except (StopIteration, KeyError, TypeError, AttributeError) as exc:
           raise NLQueryParserError("Anthropic returned an unusable/malformed response") from exc
       return ParsedQuery(filters=self._validate(raw_filters), raw_query=query)
       ```
       CRITICAL: catch the PARENT `anthropic.APIError` (NOT a narrow `APITimeoutError`) — it is the common ancestor of RateLimitError/InternalServerError/APIConnectionError/APITimeoutError/all APIStatusError subclasses, so every real failure mode routes to tier-2 fallback (09-RESEARCH.md Pitfall 1).
    4. Implement `_validate(raw_filters)`: treat LLM output as UNTRUSTED. Build module-level constants `REAL_POSITIONS = {the 10 values}` and `REAL_LEAGUES = {the 25 values}`. Drop any `position` value not in `REAL_POSITIONS` and any `league` value not in `REAL_LEAGUES`. Drop any key not in the known whitelist key set. Return only the surviving valid key/value pairs. (A hallucinated position must NOT reach the ORM — it would silently return zero results instead of degrading gracefully.)
    5. Write `get-scouted-be/players/tests/test_ai_anthropic_parser.py` implementing behavior tests 1-6. Build fake client + fake response objects with `unittest.mock` (e.g. a `SimpleNamespace`/`MagicMock` whose `content` is a list of objects with `.type`/`.input`). Import the real `anthropic` module only for its exception classes when simulating failures.
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_anthropic_parser.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class AnthropicNLQueryParser(NLQueryParser)' get-scouted-be/players/ai/anthropic_parser.py` succeeds
    - `grep -q 'except anthropic.APIError' get-scouted-be/players/ai/anthropic_parser.py` succeeds (parent class, not a narrow subclass)
    - `grep -q 'tool_choice' get-scouted-be/players/ai/anthropic_parser.py` succeeds (forced tool-use, not free-text prompting)
    - `grep -q 'settings.ANTHROPIC_MODEL' get-scouted-be/players/ai/anthropic_parser.py` succeeds (env-driven model, never hardcoded)
    - The tool schema `position` enum contains exactly the 10 real values and `league` enum contains exactly the 25 real values (verify by reading the file)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_anthropic_parser.py -q` — all 6 behavior tests pass, and NO test makes a real network call (the injected fake client + Plan 01 autouse guard both hold)
  </acceptance_criteria>
  <done>AnthropicNLQueryParser maps mocked tool-use output into a whitelist-restricted ParsedQuery, drops hallucinated enum values, and converts every anthropic.APIError plus malformed-response case into NLQueryParserError — all verified with a fully mocked client, zero real API calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_nl_query_parser() factory — provider selection from settings.LLM_PROVIDER</name>
  <files>get-scouted-be/players/ai/factory.py, get-scouted-be/players/tests/test_ai_factory.py</files>
  <read_first>
    - get-scouted-be/players/ai/base.py (returns an NLQueryParser instance)
    - get-scouted-be/players/ai/anthropic_parser.py (the concrete class the factory returns for provider "anthropic")
    - get-scouted-be/config/settings/base.py (the LLM_PROVIDER setting added in Plan 01)
  </read_first>
  <behavior>
    - Test 1: with `settings.LLM_PROVIDER == "anthropic"` (override via `settings` fixture / `override_settings`), `get_nl_query_parser()` returns an instance of `AnthropicNLQueryParser` (and of `NLQueryParser`).
    - Test 2: with `LLM_PROVIDER == "bogus"`, `get_nl_query_parser()` raises `ValueError` mentioning the unknown provider.
    - Test 3 (AI-05 swappability proof): the factory is the ONLY import needed by calling code — assert `get_nl_query_parser` is importable from `players.ai.factory` and returns something satisfying the `NLQueryParser` interface, without the test importing any concrete class name in its call path.
  </behavior>
  <action>
    Create `get-scouted-be/players/ai/factory.py`:
    ```python
    from django.conf import settings
    from players.ai.base import NLQueryParser


    def get_nl_query_parser() -> NLQueryParser:
        """Provider-agnostic factory (AI-05). Calling code imports ONLY this;
        swapping providers = add a branch + set LLM_PROVIDER, no caller changes."""
        if settings.LLM_PROVIDER == "anthropic":
            from players.ai.anthropic_parser import AnthropicNLQueryParser
            return AnthropicNLQueryParser()
        raise ValueError(f"Unknown LLM_PROVIDER: {settings.LLM_PROVIDER!r}")
    ```
    Import the concrete class lazily INSIDE the branch (mirrors the deferred-import pattern in the codebase's service modules and avoids importing anthropic at module import time). Then write `get-scouted-be/players/tests/test_ai_factory.py` implementing behavior tests 1-3 using `django.test.override_settings(LLM_PROVIDER=...)` (or the pytest `settings` fixture).
  </action>
  <verify>
    <automated>cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_factory.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def get_nl_query_parser' get-scouted-be/players/ai/factory.py` succeeds
    - `grep -q 'settings.LLM_PROVIDER == "anthropic"' get-scouted-be/players/ai/factory.py` succeeds
    - `grep -q 'raise ValueError' get-scouted-be/players/ai/factory.py` succeeds
    - The `AnthropicNLQueryParser` import is INSIDE the `if` branch (lazy), not at module top (verify by reading the file)
    - `cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_factory.py -q` — all 3 tests pass
  </acceptance_criteria>
  <done>get_nl_query_parser() returns the Anthropic implementation for LLM_PROVIDER=anthropic and raises ValueError otherwise; the concrete class is imported lazily; swapping providers requires no calling-code change (proven by the factory being the sole import).</done>
</task>

</tasks>

<verification>
- `AnthropicNLQueryParser.parse()` (fully mocked) returns whitelist-restricted ParsedQuery, drops hallucinated enums, and raises NLQueryParserError on every anthropic.APIError + malformed response.
- `get_nl_query_parser()` selects the Anthropic parser from settings and errors on an unknown provider.
- `cd get-scouted-be && .venv/bin/pytest players/tests/test_ai_anthropic_parser.py players/tests/test_ai_factory.py -q` is green with zero real network calls.
</verification>

<success_criteria>
- AI-01: free text is mapped to structured filters constrained to the real whitelist via forced tool-use.
- AI-05: a concrete provider now sits behind the factory; swapping it is a one-class + one-env-var change with no caller edits.
- No test can make a billed Anthropic call (injected fake client + Plan 01 autouse guard).
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-provider-interface-natural-language-search/09-02-SUMMARY.md`.
</output>

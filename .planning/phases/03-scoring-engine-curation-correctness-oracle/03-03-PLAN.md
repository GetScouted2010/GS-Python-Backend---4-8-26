---
phase: 03-scoring-engine-curation-correctness-oracle
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-scouted-be/scoring/docs/ESCALATION_REVIEW.md
autonomous: false
requirements: []   # none directly owned — completes Phase 3 Success Criterion 1 for the escalation cases

must_haves:
  truths:
    - "The four definitions of prepare_team_and_transfer_signal (7041/11427/11702/12192) and the four of player_transfer_history (7093/11488/11870/12329) are extracted with side-by-side diffs into a review doc"
    - "The user has explicitly chosen the authoritative version for each of the two functions, and that decision + reason is recorded in ESCALATION_REVIEW.md — NOT resolved silently by the mechanical last-wins rule"
  artifacts:
    - path: "get-scouted-be/scoring/docs/ESCALATION_REVIEW.md"
      provides: "4-way diffs for both escalated functions + recorded user decision and reason"
      contains: "prepare_team_and_transfer_signal"
      min_lines: 40
  key_links:
    - from: "get-scouted-be/scoring/docs/ESCALATION_REVIEW.md"
      to: "get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md"
      via: "escalation handoff reference"
      pattern: "DUPLICATE_FUNCTIONS"
---

<objective>
Resolve the two escalation-required duplicate functions that CONTEXT.md's locked policy
forbids resolving mechanically: `prepare_team_and_transfer_signal` (4 definitions) and
`player_transfer_history` (4 definitions). Extract all four versions of each with diffs,
present them to the user, and record their authoritative-version decision + reason.

Purpose: These two names each have 4 divergent definitions with genuine body-length
differences (not whitespace) — the last-wins rule is explicitly NOT trusted here.
Phase 4's port needs a human-signed-off decision on which version is authoritative before
it can port these safely. (03-RESEARCH.md confirmed neither function feeds the 4 oracle
scores, so this does not block Plans 04/05/06 — but it IS required to complete Success
Criterion 1 and is consumed by Plan 07's curation-map finalization.)

Output: `ESCALATION_REVIEW.md` with diffs + the recorded user decision.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-CONTEXT.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-RESEARCH.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the 4-way diffs for both escalated functions</name>
  <files>get-scouted-be/scoring/docs/ESCALATION_REVIEW.md</files>
  <read_first>
    - 03-CONTEXT.md "Duplicate-Function Resolution Policy" (the >2-definitions escalation trigger; policy is LOCKED as escalate, the mechanics are Claude's discretion)
    - 03-VALIDATION.md "Manual-Only Verifications" (the escalation review row — present line numbers with diffs, record decision + reason in the curation map)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 7041-7092 (prepare_team_and_transfer_signal def #1), 11427-11488 (#2), 11702-11760 (#3), 12192-12256 (#4)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 7093-7150 (player_transfer_history def #1), 11488-11577 (#2, note multi-line signature), 11870-11941 (#3), 12329-12399 (#4)
  </read_first>
  <action>
    Extract each function's four full bodies from `impact_model_v4.1.py` at the exact line ranges above (read the actual bodies; the ranges above are approximate end-lines — extend each to the next top-level `def`/end of body). Create `get-scouted-be/scoring/docs/ESCALATION_REVIEW.md` with two sections:

    Section A — `prepare_team_and_transfer_signal` (4 defs: 7041 / 11427 / 11702 / 12192):
      - Note the verified fact: all 4 share an identical first ~9 lines (Team_within_selected_timeframe → Team_Final fallback) then diverge; body lengths 51/60/58/64.
      - Show each version's body (fenced code) and a pairwise diff summary highlighting where they diverge (what each later version adds/changes vs. version 1).

    Section B — `player_transfer_history` (4 defs: 7093 / 11488 / 11870 / 12329):
      - Note the verified fact: body lengths 57/89/71/70; the 2nd def (11488) uses a multi-line signature `def player_transfer_history(\n    df, player_name, season=None, team=None\n):` vs. single-line signatures on the other three — a possible signal of a more substantially reworked version.
      - Show each body + pairwise diff summary.

    For each function, add a "Claude's recommendation (non-binding)" line: state what last-wins WOULD pick (12192 and 12329 respectively) and whether the diffs suggest the last version is genuinely the most complete or whether an earlier version looks more correct — so the user has an informed starting point. End each section with an empty `DECISION: ` and `REASON: ` line to be filled by the checkpoint.
  </action>
  <verify>
    <automated>grep -c "7041\|11427\|11702\|12192\|7093\|11488\|11870\|12329\|DECISION:\|REASON:" get-scouted-be/scoring/docs/ESCALATION_REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/docs/ESCALATION_REVIEW.md` contains all 8 line-number anchors: `7041`, `11427`, `11702`, `12192`, `7093`, `11488`, `11870`, `12329`
    - The file contains two `DECISION:` markers and two `REASON:` markers (one pair per function)
    - The file contains `prepare_team_and_transfer_signal` and `player_transfer_history`
  </acceptance_criteria>
  <done>Both escalated functions' four versions are extracted with diffs and a non-binding recommendation; DECISION/REASON placeholders await the user.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User picks the authoritative version for each escalated function</name>
  <decision>Which of the four definitions of `prepare_team_and_transfer_signal` and which of the four of `player_transfer_history` is authoritative for the Phase 4 port?</decision>
  <context>
    CONTEXT.md's locked policy forbids resolving these two 4-definition functions via the
    mechanical last-wins rule — they must have explicit human sign-off. The diffs and a
    non-binding recommendation are in get-scouted-be/scoring/docs/ESCALATION_REVIEW.md
    (open it before deciding). Neither function feeds the 4 oracle scores, so this choice
    does not affect the oracle numbers — it affects which body Phase 4 ports for these
    transfer-signal/history utilities.
  </context>
  <options>
    <option id="last-wins">
      <name>Accept last-wins default (12192 and 12329)</name>
      <pros>Consistent with the policy applied to the other 18 names; the last version is usually the most-recently-edited.</pros>
      <cons>Body-length divergence suggests these are genuinely different implementations, not just revisions — the last one may not be the most correct.</cons>
    </option>
    <option id="per-function-choice">
      <name>Choose per function after reading the diffs</name>
      <pros>Honors the exact reason these were escalated — lets you pick, e.g., the 89-line player_transfer_history (11488) if it's the most complete.</pros>
      <cons>Requires reading the diffs.</cons>
    </option>
  </options>
  <files>get-scouted-be/scoring/docs/ESCALATION_REVIEW.md</files>
  <action>
    Present the diffs in ESCALATION_REVIEW.md to the user and obtain, for EACH of the two
    functions, the chosen authoritative line number and a reason. On resume, write the chosen
    line + reason into the DECISION:/REASON: fields for that function in ESCALATION_REVIEW.md.
    Do NOT resolve either function via the mechanical last-wins rule without the user's explicit
    choice (CONTEXT.md locked escalation policy).
  </action>
  <verify>
    <automated>grep -A1 "DECISION:" get-scouted-be/scoring/docs/ESCALATION_REVIEW.md | grep -qE "[0-9]{4}" && echo OK</automated>
  </verify>
  <done>Both functions have a user-chosen authoritative line number + reason recorded in ESCALATION_REVIEW.md's DECISION:/REASON: fields.</done>
  <resume-signal>
    State, for EACH function, the chosen line number and a one-line reason (e.g.
    "prepare_team_and_transfer_signal: 12192 — most complete team-fallback logic;
     player_transfer_history: 11488 — the reworked 89-line version handles season+team filters").
    On resume, the executor writes each chosen line + reason into ESCALATION_REVIEW.md's
    DECISION:/REASON: fields for that function.
  </resume-signal>
</task>

</tasks>

<verification>
- ESCALATION_REVIEW.md exists with both functions' diffs and both DECISION/REASON fields filled after the checkpoint resolves.
</verification>

<success_criteria>
Success Criterion 1 is fully met including the escalation cases: the authoritative version of
every one of the 20 duplicated functions is now identified — 18 mechanically (Plan 02) and 2
by explicit user decision (this plan). Plan 07 merges this into the master curation map.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engine-curation-correctness-oracle/03-03-SUMMARY.md`
</output>

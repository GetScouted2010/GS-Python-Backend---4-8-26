---
phase: 03-scoring-engine-curation-correctness-oracle
plan: 07
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - get-scouted-be/scoring/management/commands/generate_scoring_oracle.py
  - get-scouted-be/scoring/oracle/MANIFEST.md
  - get-scouted-be/scoring/docs/CURATION_MAP.md
  - get-scouted-be/scoring/tests/test_oracle_snapshot.py
autonomous: true
requirements: []   # none directly owned — completes Phase 3 Success Criteria 2 & 4 (oracle snapshot + written curation map)

must_haves:
  truths:
    - "A single versioned CSV oracle snapshot exists with one row per real player (~41,708), columns player_id, player_name, main_position, rmm, cs, tfm, transfer_probability, generated by running the curated calculators over the full reconstructed population"
    - "A master CURATION_MAP.md exists showing, per score (RMM/CS/TFM/Transfer Probability), which original functions + columns feed it, with line numbers and whole-dataset-dependency notes"
    - "The curation map records the resolved TFM/Transfer-Probability mapping, the duplicate-function decisions (linking DUPLICATE_FUNCTIONS.md + the resolved ESCALATION_REVIEW.md decisions), and every fix applied (aggregated APPLIED_FIXES from Plans 04/05/06)"
  artifacts:
    - path: "get-scouted-be/scoring/oracle/MANIFEST.md"
      provides: "Snapshot version, git commit, row count, per-score population/context definitions"
      contains: "snapshot_version"
    - path: "get-scouted-be/scoring/docs/CURATION_MAP.md"
      provides: "Master curation map: per-score function/column map + duplicate decisions + fixes + resolutions"
      contains: "Transfer Probability"
      min_lines: 80
  key_links:
    - from: "get-scouted-be/scoring/management/commands/generate_scoring_oracle.py"
      to: "impact.compute_rmm_column / deterministic_scores.compute_cs_tp_for_pairs / tfm_model artifact"
      via: "import + call over reconstructed full population"
      pattern: "compute_rmm_column"
    - from: "get-scouted-be/scoring/docs/CURATION_MAP.md"
      to: "DUPLICATE_FUNCTIONS.md + ESCALATION_REVIEW.md"
      via: "cross-reference of duplicate decisions"
      pattern: "DUPLICATE_FUNCTIONS"
---

<objective>
Assemble the two headline deliverables of Phase 3:
1. The **correctness-oracle snapshot** (Success Criterion 2): a single versioned CSV of
   RMM/CS/TFM/Transfer Probability for the full real player population, the ground truth
   Phase 5 diffs the port against.
2. The **master curation map** (Success Criterion 4): a per-score function/column map that
   also folds in the duplicate-function decisions (Plans 02+03), the resolved
   TFM/Transfer-Probability mapping, and every fix applied (Plans 04/05/06).

Purpose: This is the convergence point — RMM (Plan 04), CS+Transfer Probability (Plan 05),
and the TFM artifact (Plan 06) are run together over the reconstructed full population to emit
one oracle CSV; and the audit-trail docs (DUPLICATE_FUNCTIONS.md, ESCALATION_REVIEW.md, the
per-module APPLIED_FIXES) are consolidated into the single curation map CONTEXT.md requires.

Output: `generate_scoring_oracle` command, the versioned oracle CSV + MANIFEST.md, and the
finalized CURATION_MAP.md.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-CONTEXT.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-RESEARCH.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-VALIDATION.md

# Everything this plan consolidates
@get-scouted-be/scoring/characterization/reconstruct.py
@get-scouted-be/scoring/characterization/impact.py
@get-scouted-be/scoring/characterization/role_fit.py
@get-scouted-be/scoring/characterization/deterministic_scores.py
@get-scouted-be/scoring/characterization/tfm_model.py
@get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md
@get-scouted-be/scoring/docs/ESCALATION_REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate_scoring_oracle command → full-population versioned CSV + manifest</name>
  <files>get-scouted-be/scoring/management/commands/generate_scoring_oracle.py, get-scouted-be/scoring/oracle/MANIFEST.md, get-scouted-be/scoring/tests/test_oracle_snapshot.py</files>
  <read_first>
    - get-scouted-be/scoring/characterization/impact.py (compute_rmm_column)
    - get-scouted-be/scoring/characterization/deterministic_scores.py (compute_cs_tp_for_pairs — per-player-vs-current-club context)
    - get-scouted-be/scoring/characterization/role_fit.py (compatibility component)
    - get-scouted-be/scoring/characterization/tfm_model.py (trained artifact predict path)
    - get-scouted-be/scoring/characterization/reconstruct.py (all four builders)
    - get-scouted-be/core/management/commands/import_all.py (command style precedent)
    - 03-CONTEXT.md "Oracle Snapshot Scope" (full population ~41,708; flat CSV versioned in repo; corrected ground truth, single oracle) and 03-RESEARCH.md "Oracle Snapshot — Recommended Format"
  </read_first>
  <action>
    Create `get-scouted-be/scoring/management/commands/generate_scoring_oracle.py` as a Django `BaseCommand` that:
    1. Reconstructs the population once: `players_df = build_players_df()`, `role_scores_wide = build_role_scores_wide()`, `team_styles_df = build_team_styles_df()`.
    2. Computes RMM for all players: `rmm = compute_rmm_column(players_df)` (per-player, context-free).
    3. Computes CS + Transfer Probability per player against the player's OWN current club (Player.club FK) via `compute_cs_tp_for_pairs(...)` — one row per player; NaN (not 0) where the player has no club or the club has all-NaN styles.
    4. Computes TFM: load the joblib artifact from `scoring/ml_artifacts/tfm_value_model_v1.joblib` (fail with a clear message if absent, instructing to run `train_tfm_model` first) and predict `predicted_fee` per player from reconstructed features (current club as buying-club context); NaN where features are unavailable.
    5. Assembles one DataFrame with columns EXACTLY: `player_id` (UUID join key back to Postgres), `player_name`, `main_position`, `rmm`, `cs`, `tfm`, `transfer_probability`. One row per real player.
    6. Writes it to `scoring/oracle/scoring_oracle_v1_<YYYY-MM-DD>.csv` (accept `--out` to override; default the dated v1 path).
    7. Writes/updates `scoring/oracle/MANIFEST.md` recording: `snapshot_version: v1`, generation date, current git commit hash (`git rev-parse HEAD`), row count, sklearn version, the TFM artifact filename used, and a "Per-score population & context" section stating: RMM = context-free per player; CS/Transfer Probability = per player vs. their current club (Player.club); TFM = predicted_fee per player with current club as buying context; and how many players were NaN/excluded per score (never zero-filled).
    Do the full-population run inside the command (NOT inside a request) — this is the whole-dataset build the caching phase (Phase 6) later optimizes.

    Create `get-scouted-be/scoring/tests/test_oracle_snapshot.py` with `test_snapshot_coverage` (uses `real_data_available` + `@pytest.mark.django_db`): if the oracle CSV exists, load it and assert columns == the exact 7 above, that row count is > 40000 (documented deviation allowed — assert within 5% of the live Player count), that `rmm` is non-null for >50% of rows and all non-null rmm ∈ [0,100], and that no score column is 100% null. If the CSV does not exist yet, skip with a message to run `manage.py generate_scoring_oracle`. Add `test_manifest_records_context` asserting MANIFEST.md contains `snapshot_version` and the per-score context descriptions.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_oracle_snapshot.py -x -q && ./.venv/bin/python manage.py generate_scoring_oracle --help</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/management/commands/generate_scoring_oracle.py` contains `class Command` and `compute_rmm_column`
    - The command references all 7 oracle columns: grep the file for `player_id`, `player_name`, `main_position`, `rmm`, `cs`, `tfm`, `transfer_probability`
    - `get-scouted-be/scoring/oracle/MANIFEST.md` contains `snapshot_version`
    - test_oracle_snapshot.py contains `def test_snapshot_coverage` and `def test_manifest_records_context`
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_oracle_snapshot.py -x -q` exits 0
    - `cd get-scouted-be && ./.venv/bin/python manage.py generate_scoring_oracle --help` exits 0
  </acceptance_criteria>
  <done>A single versioned oracle CSV with the 7 exact columns is produced over the full real population, with a manifest recording version/commit/row-count and each score's population+context; coverage test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize master CURATION_MAP.md (per-score map + decisions + fixes + resolutions)</name>
  <files>get-scouted-be/scoring/docs/CURATION_MAP.md</files>
  <read_first>
    - get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md (Plan 02 — the 20-name catalogue)
    - get-scouted-be/scoring/docs/ESCALATION_REVIEW.md (Plan 03 — the resolved 4-def decisions)
    - get-scouted-be/scoring/characterization/impact.py, role_fit.py, deterministic_scores.py, tfm_model.py (their APPLIED_FIXES lists + which functions/lines each ported)
    - 03-CONTEXT.md "Score-to-Artifact Mapping Resolution" (the LOCKED TFM/Transfer-Probability resolution to write up)
    - 03-RESEARCH.md "Core Score Computation Map" + "Curation Map — Recommended Format" and "Related, Lower-Stakes Ambiguities" (CS fork; RMM Player Impact vs Performance Score)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/docs/CURATION_MAP.md` — the single master curation map (Success Criterion 4). It must contain:
    1. **Per-score sections** (one each for RMM, Compatibility Score, Financial Fit/TFM, Transfer Probability). Each lists: the authoritative function(s) that feed it with source line numbers (RMM: add_player_impact@2725 + 8 _calc_*_impact_raw@1413-2007 + _build_std_lookup@1392; CS: calculate_subjective_role_fit_for_player_to_team@2446 + assembly@3798-3802; TFM: build_transfer_value_dataset@4935 + train_transfer_value_model@5569 + RandomForestRegressor@5638; Transfer Probability: deterministic formula@3809-3814), the exact input columns each reads, whether it needs whole-dataset context (RMM yes via std_lookup; CS yes via team_styles; TFM yes via club aggregates; Transfer Probability no — row-local), and where the port lives (scoring/characterization/*.py).
    2. **Resolved escalations subsection** (clearly flagged, not buried): (a) the TFM/Transfer-Probability mapping resolution — RF@5638 = TFM (predicts log_fee), the "Transfer Probability %" column = the deterministic formula; copy the resolution from 03-CONTEXT.md verbatim with a one-line rationale. (b) The two 4-definition function decisions from ESCALATION_REVIEW.md (chosen line + reason for each). Link to DUPLICATE_FUNCTIONS.md for the full 20-name catalogue rather than duplicating it.
    3. **Fixes applied subsection**: aggregate every non-empty `APPLIED_FIXES` from impact.py/role_fit.py/deterministic_scores.py/tfm_model.py into one table (module, column, what the source did, what changed, why) — the "no silent fixes" audit trail. If all APPLIED_FIXES were empty, state explicitly "No distorting missing-column silent-defaults were found in the ported score paths."
    4. **Open forks for Phase 4** (documented, not resolved here): the CS source fork (legacy PlayerClubCompatibility.score vs. fresh role-fit — the oracle uses the fresh script computation) and the RMM primitive choice (Player Impact context-free vs. Performance Score club-contextual — the oracle uses Player Impact). Reference 03-RESEARCH.md Open Questions 2 & 3.
    5. A short header linking the oracle (scoring/oracle/) and the artifact (scoring/ml_artifacts/) as the phase's other deliverables.

    Add the three per-score assertions to the existing curation test (extend scoring/tests/test_curation_map.py from Plan 02 is owned by Plan 02 — instead add a lightweight check here): NOT NEEDED as a new test file since test_oracle_snapshot.py + test_curation_map.py already cover artifacts. Just ensure CURATION_MAP.md content satisfies the acceptance greps below.
  </action>
  <verify>
    <automated>grep -c "RMM\|Compatibility Score\|Financial Fit\|Transfer Probability\|add_player_impact\|RandomForestRegressor\|3809\|Fixes applied\|DUPLICATE_FUNCTIONS" get-scouted-be/scoring/docs/CURATION_MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/docs/CURATION_MAP.md` contains a section for each score: `RMM`, `Compatibility Score`, `Financial Fit` (TFM), `Transfer Probability`
    - The file contains `add_player_impact`, `RandomForestRegressor`, and `3809` (the Transfer Probability formula line) with function/line references
    - The file contains the resolved mapping statement (grep for both `TFM` and `NOT` / `not Transfer Probability`)
    - The file references both `DUPLICATE_FUNCTIONS` and `ESCALATION_REVIEW` for the duplicate decisions
    - The file contains a `Fixes applied` section (or the explicit "No distorting ... found" statement)
    - The file references the CS source fork and the RMM primitive choice as open Phase-4 forks
  </acceptance_criteria>
  <done>A single master curation map documents every score's feeding functions/columns, the resolved TFM/Transfer-Probability mapping, the duplicate-function decisions (linked), every applied fix, and the open forks for Phase 4 — satisfying Success Criterion 4.</done>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest scoring/tests/ -q` (full phase suite) passes.
- Oracle CSV exists with 7 columns and ~41,708 rows; MANIFEST.md records version + context.
- CURATION_MAP.md covers all 4 scores + decisions + fixes + forks.
</verification>

<success_criteria>
All four Phase 3 Success Criteria are satisfied: (1) duplicate catalogue with authoritative
versions (Plans 02+03), (2) versioned full-population oracle snapshot (this plan), (3) sklearn
TFM component separated and versioned (Plan 06), (4) written curation map showing functions/columns
per score (this plan). Phases 4-6 have their ground truth, artifact, and curation map.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engine-curation-correctness-oracle/03-07-SUMMARY.md`
</output>

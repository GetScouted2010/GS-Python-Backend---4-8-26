---
phase: 03-scoring-engine-curation-correctness-oracle
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md
  - get-scouted-be/scoring/tests/test_curation_map.py
autonomous: true
requirements: []   # none directly owned — satisfies Phase 3 Success Criterion 1 (duplicate catalogue), prerequisite for SCORE-01..07

must_haves:
  truths:
    - "All 20 top-level function names defined more than once in impact_model_v4.1.py are catalogued with exact line numbers, definition counts, the kept (authoritative) version, every rejected version, and a reason per version"
    - "The 18 two-definition names are mechanically resolved to their LAST definition (last-wins policy); the 2 four-definition names are explicitly flagged ESCALATION PENDING, not resolved"
    - "Nested/local duplicate helpers (e.g. financial_fit_label) are documented as 'duplicated logic, not duplicated definition' and NOT subjected to the last-wins/escalation machinery"
  artifacts:
    - path: "get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md"
      provides: "The 20-name duplicate-function catalogue with kept/rejected/reason audit trail"
      contains: "prepare_team_and_transfer_signal"
      min_lines: 60
  key_links:
    - from: "get-scouted-be/scoring/tests/test_curation_map.py"
      to: "get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md"
      via: "file read + string assertions"
      pattern: "DUPLICATE_FUNCTIONS"
---

<objective>
Produce the duplicate-function catalogue (Phase 3 Success Criterion 1): every function
defined more than once in the 15,747-line `impact_model_v4.1.py`, with the authoritative
version identified and every rejected version recorded with a reason — the audit trail
CONTEXT.md requires for debugging any future scoring discrepancy.

Purpose: Phase 4's port must not blindly copy all 15,747 lines (ARCHITECTURE Anti-Pattern
3); it needs to know exactly which definition of each duplicated name is authoritative.
The two 4-definition functions are deliberately left UNRESOLVED here and handed to Plan 03's
human escalation — this plan mechanically resolves only the 18 two-definition cases.

Output: `DUPLICATE_FUNCTIONS.md` + a test asserting all 20 names are present.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-CONTEXT.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-RESEARCH.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-VALIDATION.md

<interfaces>
<!-- The verified 20-name inventory (03-RESEARCH.md "Full Duplicate-Function Inventory"). -->
<!-- Line numbers directly verified against the repo file 2026-07-21. Kept = LAST definition. -->

VERIFIED DUPLICATE INVENTORY (name | def-count | line numbers | kept line | escalate?):
- prepare_team_and_transfer_signal | 4 | 7041,11427,11702,12192 | (ESCALATE) | YES
- player_transfer_history          | 4 | 7093,11488,11870,12329 | (ESCALATE) | YES
- season_to_year        | 2 | 4783,13705  | 13705 | no
- safe_div              | 2 | 4762,13699  | 13699 | no (feeds sklearn mv_to_fee_ratio — verify zero/NaN-div handling)
- player_transfer_summary | 2 | 11577,12399 | 12399 | no
- pick_first_existing   | 2 | 4768,13733  | 13733 | no (SIGNATURE DIFFERS: required=True vs required=False default)
- parse_money_to_numeric | 2 | 4738,13668 | 13668 | no (feeds sklearn fee-cleaning)
- get_role_scores_from_dataset | 2 | 1055,3133 | 3133 | no (2nd reads ROLE_COLUMNS_BY_POSITION wide cols)
- get_position_target_avg_cols | 2 | 13,104   | 104  | no (byte-identical, "EXPORT HELPERS" cell pasted twice)
- get_position_delta_cols | 2 | 16,107    | 107  | no (trivial redundant duplicate)
- get_position_component_cols | 2 | 10,101 | 101  | no (trivial redundant duplicate)
- get_export_columns_for_position | 2 | 19,110 | 110 | no (byte-identical body; this is the cross-check anchor — both copies list same column order)
- get_2425_transfers    | 2 | 11598,12412 | 12412 | no
- format_financial      | 2 | 1305,13742  | 13742 | no (SIGNATURE DIFFERS: value= vs x= first param)
- export_team_shortlist_xlsx | 2 | 67,158  | 158  | no (~90 lines each — DIFF the two bodies, not a trivial helper)
- contract_to_years_left | 2 | 4793,13716 | 13716 | no (feeds sklearn contract_years_left)
- classify_age_fit      | 2 | 1325,2698   | 2698 | no (2698 in effect at the shortlist scoring block line 3719)
- build_wim_player_list_from_players_df | 2 | 10406,10670 | 10670 | no
- build_transfer_value_dataset | 2 | 4887,4935 | 4935 | no (FIRST def 4887 is TRUNCATED/dead code — strong evidence last-wins is correct)
- build_general_market_shortlist | 2 | 12776,13263 | 13263 | no

NESTED (NOT part of the 20; do NOT apply escalation policy):
- financial_fit_label — locally defined inside 3 different outer functions (7454, 8473, 9108); each correctly scoped, no collision.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 20-name duplicate-function catalogue (mechanical resolution of the 18)</name>
  <files>get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md</files>
  <read_first>
    - 03-RESEARCH.md "Full Duplicate-Function Inventory (verified, all 20 names)" — the authoritative source table (also inlined in this plan's <interfaces>)
    - 03-CONTEXT.md "Duplicate-Function Resolution Policy" (locked: last-wins for exactly-2-defs, cross-checked against get_export_columns_for_position; escalate >2 defs; record BOTH kept and rejected with a reason)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 19-30 and 110-121 (the two get_export_columns_for_position copies — confirm they list the same column order, so the cross-check is copy-invariant)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 67-160 (export_team_shortlist_xlsx both copies — actually diff these two ~90-line bodies, since research did not diff them)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 4887-4970 (build_transfer_value_dataset — confirm the first def at 4887 is truncated/never returns before the second def at 4935)
  </read_first>
  <action>
    First re-verify the top-level duplicate count with the anchored grep (must equal 20; if not, the grep pattern is wrong — see 03-RESEARCH.md Pitfall 4):
    `grep -n "^def " pixel-perfect-clone-60729/docs/impact_model_v4.1.py | sed 's/(.*//;s/^[0-9]*:def //' | sort | uniq -d` should list exactly 20 names; and `... | sort | uniq -c | awk '$1>2'` must show exactly `prepare_team_and_transfer_signal` and `player_transfer_history` (count 4 each).

    Create `get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md` with:
    1. A header stating the policy in force (last-wins for exactly-2-definition names, cross-checked against get_export_columns_for_position's column order at line 110; escalate >2-definition names to a human — deferred to ESCALATION_REVIEW.md / Plan 03).
    2. One Markdown table row per duplicated name (all 20) with columns: `Function`, `Def count`, `Line numbers`, `Kept line`, `Rejected line(s)`, `Reason`, `Escalated? (Y/N)`. Populate from the verified inventory in this plan's <interfaces> block. For the 18 two-definition names, `Kept line` = the last definition; `Reason` must be specific (e.g. build_transfer_value_dataset: "first def at 4887 is truncated/orphaned — cut off mid-body by the 4935 def, never reaches a return; last-wins is not just convention but demonstrably correct here"; get_export_columns_for_position: "byte-identical body except one blank line, redundant"). For the 2 four-definition names, set `Kept line` = "ESCALATION PENDING", `Escalated?` = Y.
    3. Record the signature-difference notes explicitly for `pick_first_existing` (required=True→required=False default flip) and `format_financial` (value= vs x= param rename) — these are exactly the subtle-default bug class CONTEXT.md's fix-threshold targets; note which default is in effect at call sites.
    4. A short "Nested duplicates (out of scope for last-wins)" section documenting `financial_fit_label` (defined locally inside outer functions at 7454, 8473, 9108) as "duplicated logic, not duplicated definition — each closure is correctly scoped; not a name-collision."
    5. An "Escalation handoff" note pointing to Plan 03 / ESCALATION_REVIEW.md for the two 4-definition functions.
  </action>
  <verify>
    <automated>grep -c "prepare_team_and_transfer_signal\|player_transfer_history\|build_transfer_value_dataset\|get_export_columns_for_position" get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md` exists and contains all 20 names — grep for each of these returns a match: `prepare_team_and_transfer_signal`, `player_transfer_history`, `season_to_year`, `safe_div`, `player_transfer_summary`, `pick_first_existing`, `parse_money_to_numeric`, `get_role_scores_from_dataset`, `get_position_target_avg_cols`, `get_position_delta_cols`, `get_position_component_cols`, `get_export_columns_for_position`, `get_2425_transfers`, `format_financial`, `export_team_shortlist_xlsx`, `contract_to_years_left`, `classify_age_fit`, `build_wim_player_list_from_players_df`, `build_transfer_value_dataset`, `build_general_market_shortlist`
    - The file contains the string `ESCALATION PENDING` (for the two 4-def functions)
    - The file contains `13733` and `required` (the pick_first_existing signature-flip note)
    - The file contains `financial_fit_label` under a nested/out-of-scope section
  </acceptance_criteria>
  <done>All 20 duplicated top-level names catalogued with kept/rejected/reason; 18 mechanically resolved to last-wins; 2 flagged ESCALATION PENDING; nested duplicates documented as out-of-scope.</done>
</task>

<task type="auto">
  <name>Task 2: Automated catalogue-completeness test</name>
  <files>get-scouted-be/scoring/tests/test_curation_map.py</files>
  <read_first>
    - get-scouted-be/scoring/docs/DUPLICATE_FUNCTIONS.md (the artifact under test — created in Task 1)
    - get-scouted-be/scoring/tests/conftest.py (fixtures available)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py (the test independently recomputes the duplicate set from this file)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/tests/test_curation_map.py` with:
    1. `test_all_20_names_present`: read DUPLICATE_FUNCTIONS.md and assert every one of the 20 expected names (hardcode the list) appears in the text. Assert the count of expected names is exactly 20.
    2. `test_duplicate_set_matches_source`: independently parse `impact_model_v4.1.py` (regex `^def (\w+)` over lines), compute the set of names with >1 definition, and assert it equals the hardcoded 20-name set — so if the source file ever changes, the test catches a drift between file and catalogue. Locate the script via a path relative to the repo root (walk up from `__file__` to find `pixel-perfect-clone-60729/docs/impact_model_v4.1.py`); skip with a clear message if not found.
    3. `test_two_escalations_flagged`: assert DUPLICATE_FUNCTIONS.md contains `ESCALATION PENDING` and that both `prepare_team_and_transfer_signal` and `player_transfer_history` appear on a line also containing the escalation marker (or in an escalation section).
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_curation_map.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/tests/test_curation_map.py` contains `def test_all_20_names_present` and `def test_duplicate_set_matches_source` and `def test_two_escalations_flagged`
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_curation_map.py -x -q` exits 0
  </acceptance_criteria>
  <done>Tests prove the catalogue lists all 20 duplicated names, that the set matches what's actually in the source file, and that the two 4-definition functions are flagged for escalation.</done>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest scoring/tests/test_curation_map.py -q` passes.
- DUPLICATE_FUNCTIONS.md lists all 20 names with kept/rejected/reason and flags the 2 escalations.
</verification>

<success_criteria>
Phase 3 Success Criterion 1 is met for the 18 mechanically-resolved names; the 2 escalation
cases are cleanly handed off to Plan 03. Phase 4 has an authoritative, tested list of which
definition of each duplicated function to port.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engine-curation-correctness-oracle/03-02-SUMMARY.md`
</output>

---
phase: 03-scoring-engine-curation-correctness-oracle
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-scouted-be/scoring/characterization/impact.py
  - get-scouted-be/scoring/tests/test_impact.py
autonomous: true
requirements: []   # none directly owned — characterizes RMM ahead of SCORE-01 (Phase 4)

must_haves:
  truths:
    - "The 8 position-specific _calc_*_impact_raw calculators, _build_std_lookup, and add_player_impact are extracted verbatim (faithful) into a clean importable module and run against the reconstructed full-population players_df"
    - "add_player_impact produces a per-player, position-relative 0-100 'Player Impact' (RMM) column for every player, built from a std_lookup computed once over the whole population"
    - "Any missing-column silent-default that mathematically distorts the score (e.g. Minutes defaulting to 0) is fixed to exclude/null the player instead, and the fix is recorded for the curation map"
  artifacts:
    - path: "get-scouted-be/scoring/characterization/impact.py"
      provides: "_build_std_lookup, 8 _calc_*_impact_raw, add_player_impact — faithful port"
      contains: "def add_player_impact"
      min_lines: 120
    - path: "get-scouted-be/scoring/tests/test_impact.py"
      provides: "RMM coverage test against real data"
      contains: "def test"
  key_links:
    - from: "get-scouted-be/scoring/characterization/impact.py"
      to: "scoring.characterization.reconstruct.build_players_df"
      via: "import + call"
      pattern: "build_players_df"
---

<objective>
Characterize RMM (Player Score) by extracting the one genuinely clean, non-duplicated part
of the script — the 8 `_calc_*_impact_raw` position calculators, `_build_std_lookup`, and
`add_player_impact` — into an importable module and running them against the reconstructed
full-population `players_df` to produce the per-player 0-100 "Player Impact" column.

Purpose: RMM is the least-ambiguous of the 4 scores (03-RESEARCH.md: `add_player_impact` is
single-definition, context-free, population-relative — the recommended RMM primitive over the
club-context-dependent "Performance Score"). This is the anchor column of the oracle. Faithful
port, not redesign — hardcoded weights and the league_weights dict are preserved untouched
(CONTEXT.md "don't touch non-bug quirks"); only missing-column silent-defaults that distort the
score are fixed.

Output: `impact.py` (faithful calculators) + a test proving RMM coverage over real data.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-CONTEXT.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-RESEARCH.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-VALIDATION.md

# Reconstruction layer this plan calls
@get-scouted-be/scoring/characterization/reconstruct.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract std_lookup + the 8 position impact calculators</name>
  <files>get-scouted-be/scoring/characterization/impact.py, get-scouted-be/scoring/tests/test_impact.py</files>
  <behavior>
    - Each _calc_*_impact_raw(row, row_idx, std_lookup) returns a 4-tuple (raw_impact, positive, negative, components_dict) as in the source.
    - _build_std_lookup(df, metrics_needed, position_col="Main_Position") returns per-metric-per-position mean/std used for z-scoring; computed once over the full df.
    - A column required by a calculator but ABSENT from the reconstructed df raises (via assert_columns_present) rather than silently zero-filling.
  </behavior>
  <read_first>
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 1392-1412 (_build_std_lookup)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 1413-2007 (the 8 calculators: _calc_gk_impact_raw 1413, _calc_cb_impact_raw 1473, _calc_fb_impact_raw 1539, _calc_cmf_impact_raw 1616, _calc_dmf_impact_raw 1694, _calc_amf_impact_raw 1773, _calc_winger_impact_raw 1851, _calc_cf_impact_raw 1928)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 4677+ (league_weights dict — port as-is, do NOT touch)
    - get-scouted-be/scoring/characterization/reconstruct.py (build_players_df, assert_columns_present)
    - .planning/codebase/CONCERNS.md §"Fragile Area: Complex Python Scoring Engine" (the missing-Minutes→0 silent-default bug class)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/characterization/impact.py`. Copy `_build_std_lookup` and all 8 `_calc_*_impact_raw` functions FAITHFULLY from the line ranges above (preserve every threshold, weight, and the `league_weights` dict verbatim — these are deliberate tuning choices per CONTEXT.md, NOT bugs to improve). Also copy any tiny helper each calculator calls (e.g. a `_std`/z-score helper) that lives near them.

    Apply the phase's narrow fix-threshold ONLY where the source silently defaults a MISSING/ABSENT column in a way that mathematically distorts the score (the CONCERNS.md pattern, e.g. `if "Minutes" not in df.columns: df["Minutes"] = 0`): replace that with a call to `assert_columns_present(df, [...], "players_df")` at the top of the scoring path OR null/exclude the affected player — do NOT zero-fill. Add a module-level docstring list `APPLIED_FIXES = [...]` recording each such fix (what column, what the source did, what you changed, why) — Plan 07 copies these into the curation map's "Fixes applied" section. If NO qualifying silent-default exists in this code path, set `APPLIED_FIXES = []` and note "none found in RMM path".

    Write the first half of `get-scouted-be/scoring/tests/test_impact.py`: a unit test that builds a tiny synthetic 3-row DataFrame (two players of the same position, one outlier) and asserts `_build_std_lookup` returns finite mean/std for the metrics, and that a `_calc_*_impact_raw` for that position returns a 4-tuple whose first element is a float. Assert that calling the scoring path with a df MISSING a required column raises ValueError (proving no silent zero-fill).
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_impact.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/characterization/impact.py` contains `def _build_std_lookup` and all 8 names: `_calc_gk_impact_raw`, `_calc_cb_impact_raw`, `_calc_fb_impact_raw`, `_calc_cmf_impact_raw`, `_calc_dmf_impact_raw`, `_calc_amf_impact_raw`, `_calc_winger_impact_raw`, `_calc_cf_impact_raw`
    - `impact.py` contains `APPLIED_FIXES` (a list, possibly empty with a "none found" note)
    - `impact.py` contains `league_weights` (preserved verbatim)
    - test_impact.py asserts a ValueError is raised when a required column is missing
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_impact.py -x -q` exits 0
  </acceptance_criteria>
  <done>The 8 calculators + std_lookup are faithfully extracted, quirks preserved, any distorting missing-column default fixed and recorded in APPLIED_FIXES, and unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: add_player_impact wrapper + full-population RMM smoke over real data</name>
  <files>get-scouted-be/scoring/characterization/impact.py, get-scouted-be/scoring/tests/test_impact.py</files>
  <behavior>
    - add_player_impact(df) adds a "Player Impact" column: per-position percentile (0-100) of raw impact via _position_percentile, using std_lookup built once over df.
    - Run over the real reconstructed players_df, every player gets a Player Impact value OR is explicitly excluded/nulled (never zero-defaulted due to a missing column).
  </behavior>
  <read_first>
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 2725-2900 (add_player_impact 2725, _position_percentile ~2822)
    - get-scouted-be/scoring/characterization/impact.py (Task 1 output)
    - get-scouted-be/scoring/characterization/reconstruct.py (build_players_df)
    - get-scouted-be/scoring/tests/conftest.py (real_data_available fixture)
  </read_first>
  <action>
    Add `add_player_impact(df)` and its helper `_position_percentile` to impact.py, copied faithfully from lines 2725-~2900 (preserve the percentile method exactly). It must build the std_lookup once over the passed df (whole-population), then compute per-position percentiles — confirming the "no isolated per-player scoring" architectural finding.

    Add a convenience `compute_rmm_column(players_df) -> pd.Series` (indexed by `player_id`) that returns the "Player Impact" (RMM) value per player — this is what Plan 07's oracle generator imports.

    Extend `get-scouted-be/scoring/tests/test_impact.py` with `test_rmm_full_population_coverage` (uses `real_data_available` + `@pytest.mark.django_db`): call `build_players_df()` → `compute_rmm_column(df)`; assert the result is non-empty, all values are in [0, 100] or NaN (never a spurious 0 for a missing-column player), and that the fraction of non-NaN RMM values is > 0.5 (most players score). Group-check: assert at least the main position groups present in the data each have ≥1 scored player. Skip if DB empty.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_impact.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `impact.py` contains `def add_player_impact` and `def compute_rmm_column`
    - `impact.py` contains `Player Impact` (the exact output column string)
    - test_impact.py contains `def test_rmm_full_population_coverage`
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_impact.py -x -q` exits 0
  </acceptance_criteria>
  <done>add_player_impact produces a whole-population, position-relative 0-100 RMM per player; compute_rmm_column exposes it keyed by player_id for the oracle; coverage test passes on real data.</done>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest scoring/tests/test_impact.py -q` passes.
- RMM values are in [0,100] or NaN, never spurious 0 from a missing column.
</verification>

<success_criteria>
The RMM calculator is faithfully characterized, quirks preserved, distorting defaults fixed and
recorded, and produces a per-player oracle-ready RMM column keyed by the Postgres UUID. Feeds
Plan 07's oracle assembly and Phase 4's SCORE-01 port.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engine-curation-correctness-oracle/03-04-SUMMARY.md`
</output>

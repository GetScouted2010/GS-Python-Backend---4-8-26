---
phase: 03-scoring-engine-curation-correctness-oracle
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - get-scouted-be/scoring/characterization/role_fit.py
  - get-scouted-be/scoring/characterization/deterministic_scores.py
  - get-scouted-be/scoring/tests/test_deterministic_scores.py
autonomous: true
requirements: []   # none directly owned — characterizes CS and Transfer Probability ahead of SCORE-02 / SCORE-04

must_haves:
  truths:
    - "calculate_subjective_role_fit_for_player_to_team and the Compatibility Score assembly are faithfully extracted, producing a per-player-vs-club CS from role-fit + team-style vectors (with NaN club styles left as NaN, not zero-filled)"
    - "Transfer Probability is characterized as the DETERMINISTIC weighted formula tp = (0.30*compat + 0.20*performance + 0.20*financial + 0.30*contract_fit)*100 — NO ML, no sklearn — per the resolved TFM/Transfer-Probability mapping"
    - "Performance Score (the 20% term of Transfer Probability) takes Player Impact (RMM, Plan 04's output) as an EXPLICIT input — compute_cs_tp_for_pairs accepts a player_impact series and returns per-player compatibility_score/performance_score/role_pct columns for Plan 06's TFM recipe and Plan 07's oracle; a player with NaN player_impact yields NaN (not 0) performance/TP"
    - "The CS source fork (legacy PlayerClubCompatibility.score vs. fresh script role-fit) is documented for Phase 4 to resolve, and the oracle uses the script's fresh computation"
  artifacts:
    - path: "get-scouted-be/scoring/characterization/role_fit.py"
      provides: "calculate_subjective_role_fit_for_player_to_team faithful port + team-style vector helpers"
      contains: "def calculate_subjective_role_fit_for_player_to_team"
      min_lines: 40
    - path: "get-scouted-be/scoring/characterization/deterministic_scores.py"
      provides: "compatibility_score(), financial_score(), performance_score(player_impact,...), contract_fit(), transfer_probability() faithful ports + compute_cs_tp_for_pairs(..., player_impact)"
      contains: "def transfer_probability"
      min_lines: 60
  key_links:
    - from: "get-scouted-be/scoring/characterization/deterministic_scores.py"
      to: "get-scouted-be/scoring/characterization/role_fit.py"
      via: "import for compatibility component"
      pattern: "role_fit"
    - from: "get-scouted-be/scoring/characterization/deterministic_scores.py::compute_cs_tp_for_pairs"
      to: "get-scouted-be/scoring/characterization/impact.py::compute_rmm_column (Plan 04)"
      via: "explicit player_impact series parameter feeding performance_score"
      pattern: "player_impact"
---

<objective>
Characterize two of the four scores as the ORIGINAL script computes them:
- **Compatibility Score (CS)** via `calculate_subjective_role_fit_for_player_to_team` +
  the `"Compatibility Score"` assembly (role-fit + team-style overlap).
- **Transfer Probability** via the deterministic, non-ML weighted formula at lines 3809-3814
  (`tp = (0.30*compatibility + 0.20*performance + 0.20*financial + 0.30*contract_fit)*100`).

Purpose: The TFM/Transfer-Probability mapping was RESOLVED this session (03-CONTEXT.md
"Score-to-Artifact Mapping Resolution"): the RandomForestRegressor is the TFM artifact (Plan
06); Transfer Probability is this deterministic arithmetic — no sklearn, no artifact, trivially
reproducible. This plan ports that arithmetic faithfully. CS has an open source-fork (legacy
per-pair data vs. fresh role-fit) that 03-RESEARCH.md leaves for Phase 4 to decide; this phase
characterizes the SCRIPT's fresh role-fit computation regardless (ARCHITECTURE already plans
scoring/services/role_fit.py) and documents the fork.

Cross-plan wiring note: Transfer Probability's Performance Score term (20% of the formula) is a
blend of **Player Impact (RMM)** — which is Plan 04's output (`compute_rmm_column`) — with
team/target percentiles. This plan does NOT recompute RMM; it takes it as an explicit
`player_impact` parameter so the runners (Plans 06 & 07) pass in Plan 04's real RMM series. The
per-player CS/performance/role_pct columns this plan emits are also the exact upstream features
Plan 06's `build_transfer_value_dataset` recipe and Plan 07's oracle merge in.

Output: `role_fit.py` + `deterministic_scores.py` (CS + Transfer Probability) + tests.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-CONTEXT.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-RESEARCH.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-VALIDATION.md

# Reconstruction layer this plan calls
@get-scouted-be/scoring/characterization/reconstruct.py

<interfaces>
<!-- Verified formula anchors (03-RESEARCH.md "Core Score Computation Map" + Critical Finding). -->
Transfer Probability (deterministic, line 3809-3814, inside external_target_shortlist_absolute_vectorized def@3275):
    tp = (0.30*compatibility_score/100 + 0.20*performance_score/100
          + 0.20*financial_score/100 + 0.30*contract_fit) * 100
  where contract_fit is a contract-expiry band: 1.0 / 0.7 / 0.4 / 0.1 by years left.
Compatibility Score (line 3798-3802): _avg_non_null([role_fit_score, similarity_pct, 100_or_70_bonus]).
Financial Score (line 3804-3807): avg(age_fit_score, mv_fit_score)  # the cheap deterministic TFM candidate.
Performance Score (line ~3836): blends Player Impact (RMM) with team/target percentiles — REQUIRES the
  RMM value as an input; it CANNOT be derived from players_df alone. RMM is produced by Plan 04
  (impact.compute_rmm_column / add_player_impact), so it must be passed in, not recomputed here.
calculate_subjective_role_fit_for_player_to_team(player_row, target_team, team_styles_df): def@2446.

Cross-plan contract this plan DEFINES (consumed by Plans 06 & 07):
  compute_cs_tp_for_pairs(players_df, team_styles_df, role_scores_wide, club_context, player_impact) -> pd.DataFrame
    - player_impact: pd.Series keyed by player_id == Plan 04's compute_rmm_column(players_df) output.
    - returns a DataFrame keyed by player_id with columns:
        club_context, compatibility_score, financial_score, performance_score,
        contract_fit, role_pct, transfer_probability
    - compatibility_score / performance_score / role_pct are the exact upstream feature columns
      Plan 06's build_transfer_value_dataset (33-feature recipe) and Plan 07's oracle merge in.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Faithful role-fit port (Compatibility Score component)</name>
  <files>get-scouted-be/scoring/characterization/role_fit.py, get-scouted-be/scoring/tests/test_deterministic_scores.py</files>
  <behavior>
    - calculate_subjective_role_fit_for_player_to_team(player_row, target_team, team_styles_df) returns a role-fit score using the player's role-score columns and the club's playing-style vector.
    - A club with all-NaN playing styles yields NaN/excluded role-fit (NOT a zero-filled misleadingly-low fit).
  </behavior>
  <read_first>
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 2400-2726 (weighted_overlap_score + calculate_subjective_role_fit_for_player_to_team@2446 — read in full to capture the exact team_styles_df column shape it expects; 03-RESEARCH.md Open Question 4)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 3798-3802 (the "Compatibility Score" assembly = _avg_non_null([role_fit, similarity_pct, bonus]))
    - get-scouted-be/scoring/characterization/reconstruct.py (build_role_scores_wide, build_team_styles_df, assert_columns_present)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/characterization/role_fit.py`. Faithfully copy `weighted_overlap_score` (or whatever helper role-fit calls) and `calculate_subjective_role_fit_for_player_to_team` (def@2446) from the source, plus any team-style-vector builder they depend on (e.g. `build_team_style_vector` referenced in ARCHITECTURE — confirm its actual name/location by reading 2400-2726). Preserve all weights/thresholds verbatim.

    Guard the team-style path: if the target club's style vector is entirely NaN (the ~77%-null case from FIELD_MAPPING.md §3), return NaN for role-fit rather than treating missing styles as zeros — record this in a module `APPLIED_FIXES` list if the source silently zero-filled. Use `assert_columns_present` for role-score columns.

    Add `compatibility_score(role_fit_score, similarity_pct, bonus)` to role_fit.py implementing the line 3798-3802 `_avg_non_null` assembly faithfully.

    Start `get-scouted-be/scoring/tests/test_deterministic_scores.py`: a unit test with a synthetic player row (known role scores) and a synthetic club style vector giving a hand-computable role-fit; assert the function returns the expected value within 1e-6. Add a test that an all-NaN club style vector yields NaN role-fit (no zero-fill).
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_deterministic_scores.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/characterization/role_fit.py` contains `def calculate_subjective_role_fit_for_player_to_team` and `def compatibility_score`
    - `role_fit.py` contains `APPLIED_FIXES`
    - test asserts all-NaN club styles → NaN role-fit (grep test for `isnan` or `is None`)
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_deterministic_scores.py -x -q` exits 0
  </acceptance_criteria>
  <done>Role-fit + Compatibility Score assembly faithfully ported, null-style clubs excluded rather than zero-filled, unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic Transfer Probability + Financial/Performance components</name>
  <files>get-scouted-be/scoring/characterization/deterministic_scores.py, get-scouted-be/scoring/tests/test_deterministic_scores.py</files>
  <behavior>
    - transfer_probability(compatibility, performance, financial, contract_fit) returns (0.30*compat/100 + 0.20*perf/100 + 0.20*fin/100 + 0.30*contract_fit)*100 exactly.
    - contract_fit(years_left) returns the banded 1.0/0.7/0.4/0.1 value matching the source.
    - financial_score(age_fit, mv_fit) returns their average (the cheap deterministic score).
    - performance_score(player_impact, ...) consumes the player's Player Impact (RMM) value as an explicit input; a NaN player_impact yields NaN performance (never silently 0).
    - compute_cs_tp_for_pairs(players_df, team_styles_df, role_scores_wide, club_context, player_impact) returns per-player CS/financial/performance/contract_fit/role_pct/transfer_probability using the passed-in player_impact series; NaN (not 0) where player_impact is NaN or club context is missing.
    - No sklearn import anywhere in this module.
  </behavior>
  <read_first>
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 3275-3345 (external_target_shortlist_absolute_vectorized header — how Performance/Financial/contract_fit are computed before the tp line)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 3798-3860 (Compatibility/Financial/Performance/"Transfer Probability %" assignments, tp formula@3809-3814, round@3840; and the contract_fit banding)
    - 03-CONTEXT.md "Score-to-Artifact Mapping Resolution" (Transfer Probability = deterministic formula, NOT the RandomForestRegressor)
    - get-scouted-be/scoring/characterization/role_fit.py (Task 1 output — compatibility_score)
    - get-scouted-be/scoring/characterization/impact.py (Plan 04 — compute_rmm_column produces the player_impact series performance_score consumes; read its return shape: pd.Series keyed by player_id)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/characterization/deterministic_scores.py`. Faithfully port, from the line ranges above:
    - `contract_fit(years_left)` — the banded lookup (1.0 / 0.7 / 0.4 / 0.1 by contract years left). Copy the exact band boundaries from the source, do not guess.
    - `financial_score(age_fit_score, mv_fit_score)` — the avg at line 3804-3807 (the cheap deterministic quantity; note in a docstring this is the "cheap TFM candidate" whose ML sibling is Plan 06).
    - `performance_score(player_impact, ...)` — copy the line-3836 Performance Score blend as the source computes it (Player Impact + team/target percentiles). Take the player's Player Impact (RMM) value as an EXPLICIT first argument rather than recomputing it — it is produced by Plan 04's `compute_rmm_column`. If the `player_impact` value is NaN, or a strictly-required target-club context is missing, return NaN (no zero-fill) and document the exact inputs it needs in the docstring.
    - `transfer_probability(compatibility_score, performance_score, financial_score, contract_fit_value)` — implement the exact weighted formula from line 3809-3814, then `round(float(tp), 1)` as at line 3840.

    Add a module docstring stating the RESOLVED mapping: "Transfer Probability is this deterministic formula (no ML). The RandomForestRegressor is TFM (Plan 06). Source: 03-CONTEXT.md Score-to-Artifact Mapping Resolution." Add a top-of-file assertion-in-comment that this module imports NO sklearn.

    Add `compute_cs_tp_for_pairs(players_df, team_styles_df, role_scores_wide, club_context, player_impact) -> pd.DataFrame`. **`player_impact` is a required `pd.Series` keyed by `player_id`** — the RMM output of Plan 04's `compute_rmm_column(players_df)`. This is the explicit cross-plan wiring: Performance Score is 20% of the Transfer Probability formula and needs Player Impact, which lives in Plan 04 — so it MUST be passed in; do NOT recompute or zero-fill it here. For each player against a given `club_context` (default `None` = the player's OWN current club from Player.club FK), compute CS, Financial, Performance (using that player's `player_impact` value), contract_fit, `role_pct`, and Transfer Probability, returning a DataFrame keyed by `player_id` with a `club_context` column plus the `compatibility_score`, `financial_score`, `performance_score`, `contract_fit`, `role_pct`, and `transfer_probability` columns. These `compatibility_score`/`performance_score`/`role_pct` columns are the exact upstream features Plan 06's `build_transfer_value_dataset` recipe and Plan 07's oracle merge back onto `players_df` — emit them with these exact names. Where a player has no club, the club has all-NaN styles, or the player's `player_impact` is NaN, set the dependent CS/Performance/TP to NaN (not 0) and record the exclusion count. Document the per-player-vs-current-club context choice AND the `player_impact` input requirement in the docstring.

    Extend test_deterministic_scores.py: `test_transfer_probability_exact` (hand-compute tp for known inputs, assert within 1e-9), `test_contract_fit_bands` (assert each band boundary maps to the expected value), `test_no_sklearn_import` (assert `sklearn` is not in this module's imports — read the file text and assert `import sklearn` and `from sklearn` are both absent), and `test_performance_needs_player_impact` (call `performance_score` / `compute_cs_tp_for_pairs` for a player whose `player_impact` is NaN and assert the resulting performance_score and transfer_probability are NaN — proving the RMM input is wired, not zero-filled).
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_deterministic_scores.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/characterization/deterministic_scores.py` contains `def transfer_probability`, `def contract_fit`, `def financial_score`, `def performance_score`, `def compute_cs_tp_for_pairs`
    - `compute_cs_tp_for_pairs`'s signature contains a `player_impact` parameter (grep the def line for `player_impact`)
    - `deterministic_scores.py` output columns include `compatibility_score`, `performance_score`, and `role_pct` (the upstream features Plans 06/07 consume)
    - `deterministic_scores.py` does NOT contain `import sklearn` or `from sklearn` (grep returns nothing)
    - `deterministic_scores.py` contains `0.30` and `0.20` (the exact formula weights)
    - test_deterministic_scores.py contains `def test_transfer_probability_exact`, `def test_contract_fit_bands`, `def test_no_sklearn_import`, `def test_performance_needs_player_impact`
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_deterministic_scores.py -x -q` exits 0
  </acceptance_criteria>
  <done>Transfer Probability characterized as the exact deterministic formula (no ML); Performance Score takes Plan 04's RMM as an explicit player_impact input (NaN, not 0, when missing); compute_cs_tp_for_pairs emits per-player compatibility_score/performance_score/role_pct for Plans 06/07; tests prove exactness, the no-sklearn property, and the player_impact wiring.</done>
</task>

</tasks>

<verification>
- `./.venv/bin/pytest scoring/tests/test_deterministic_scores.py -q` passes.
- Transfer Probability reproduces the source formula to 1e-9; contains no sklearn.
- compute_cs_tp_for_pairs accepts player_impact and emits compatibility_score/performance_score/role_pct.
</verification>

<success_criteria>
CS and Transfer Probability are faithfully characterized; Transfer Probability is confirmed
deterministic (matching the resolved mapping); Performance Score is explicitly wired to Plan 04's
RMM; the CS source-fork is documented for Phase 4. Feeds Plan 06's TFM feature recipe, Plan 07's
oracle assembly, and Phase 4's SCORE-02 / SCORE-04 ports.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engine-curation-correctness-oracle/03-05-SUMMARY.md`
</output>

---
phase: 03-scoring-engine-curation-correctness-oracle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-scouted-be/requirements/base.txt
  - get-scouted-be/pyproject.toml
  - get-scouted-be/config/settings/base.py
  - get-scouted-be/scoring/__init__.py
  - get-scouted-be/scoring/apps.py
  - get-scouted-be/scoring/characterization/__init__.py
  - get-scouted-be/scoring/characterization/reconstruct.py
  - get-scouted-be/scoring/tests/__init__.py
  - get-scouted-be/scoring/tests/conftest.py
  - get-scouted-be/scoring/tests/test_reconstruct.py
autonomous: true
requirements: []   # none directly owned — prerequisite foundation for SCORE-01..07 (Phases 4-6)

must_haves:
  truths:
    - "scikit-learn and joblib are installed in get-scouted-be/.venv and pinned in requirements/base.txt"
    - "A `scoring` Django app exists, is in INSTALLED_APPS, and its tests/ dir is collected by pytest"
    - "reconstruct.py can turn the Django ORM (Player, PlayerRoleScore, Club, Transfer) into the four DataFrame shapes the original script expects, using FIELD_MAPPING.md's reverse rename, without silently zero-filling absent columns"
  artifacts:
    - path: "get-scouted-be/scoring/characterization/reconstruct.py"
      provides: "build_players_df(), build_role_scores_wide(), build_team_styles_df(), build_transfers_df()"
      contains: "def build_players_df"
      min_lines: 80
    - path: "get-scouted-be/scoring/apps.py"
      provides: "ScoringConfig app config"
      contains: "class ScoringConfig"
    - path: "get-scouted-be/requirements/base.txt"
      provides: "scikit-learn + joblib pins"
      contains: "scikit-learn"
  key_links:
    - from: "get-scouted-be/config/settings/base.py"
      to: "scoring app"
      via: "INSTALLED_APPS entry"
      pattern: "\"scoring\"|'scoring'"
    - from: "get-scouted-be/scoring/characterization/reconstruct.py"
      to: "players/clubs/transfers ORM"
      via: "Model.objects.values()"
      pattern: "\\.objects\\.(values|all)"
---

<objective>
Lay the foundation every other Phase 3 plan builds on: install the two missing
data-science dependencies (scikit-learn, joblib), create a minimal `scoring/` Django
app (used properly starting Phase 4, seeded now), wire its tests into pytest, and build
the ORM→script-shaped-DataFrame reconstruction layer that the oracle-generation and
model-training plans all call.

Purpose: The original `impact_model_v4.1.py` can only ever run as extracted functions
against reconstructed DataFrames (it reads hardcoded absolute Excel paths that do not
exist here and uses Jupyter `display()` before importing it — see 03-RESEARCH.md
"Script Structure & Execution Model"). This plan produces the clean DataFrame bridge
from Phase 1's real migrated Postgres data into the exact column shapes those functions
read.

Output: Installed deps, a `scoring` app skeleton, and a tested `reconstruct.py`.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-CONTEXT.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-RESEARCH.md
@.planning/phases/03-scoring-engine-curation-correctness-oracle/03-VALIDATION.md

# The canonical column-name mapping (Django field ↔ script-internal literal). SOURCE OF TRUTH — do not re-derive.
@get-scouted-be/docs/FIELD_MAPPING.md
# Model field names to query from
@get-scouted-be/players/models.py
@get-scouted-be/clubs/models.py
@get-scouted-be/transfers/models.py
# Settings to extend (split base/local/production, django-environ)
@get-scouted-be/config/settings/base.py
# pytest config to extend (testpaths)
@get-scouted-be/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: scikit-learn/joblib deps + scoring app skeleton + pytest wiring</name>
  <files>get-scouted-be/requirements/base.txt, get-scouted-be/pyproject.toml, get-scouted-be/config/settings/base.py, get-scouted-be/scoring/__init__.py, get-scouted-be/scoring/apps.py, get-scouted-be/scoring/characterization/__init__.py, get-scouted-be/scoring/tests/__init__.py, get-scouted-be/scoring/tests/conftest.py</files>
  <read_first>
    - get-scouted-be/requirements/base.txt (current pins — pandas>=2.2,<3.0 already present, scikit-learn/joblib absent)
    - get-scouted-be/pyproject.toml (current [tool.pytest.ini_options]: testpaths = ["clubs","players","transfers","core","accounts"])
    - get-scouted-be/config/settings/base.py (INSTALLED_APPS shape — see how accounts/clubs/players/transfers/core are registered)
    - get-scouted-be/accounts/apps.py (AppConfig precedent to mirror)
    - get-scouted-be/conftest.py (root conftest re-exporting fixture_dir)
  </read_first>
  <action>
    1. Append to get-scouted-be/requirements/base.txt exactly:
       `scikit-learn>=1.5,<2.0`
       `joblib>=1.4,<2.0`
    2. Install into the existing venv: `cd get-scouted-be && ./.venv/bin/pip install -r requirements/base.txt`
    3. Create the `scoring` Django app package (do NOT run `startapp`; create files directly):
       - `get-scouted-be/scoring/__init__.py` (empty)
       - `get-scouted-be/scoring/apps.py` containing:
         ```python
         from django.apps import AppConfig

         class ScoringConfig(AppConfig):
             default_auto_field = "django.db.models.BigAutoField"
             name = "scoring"
         ```
       - `get-scouted-be/scoring/characterization/__init__.py` (empty)
       - `get-scouted-be/scoring/tests/__init__.py` (empty)
    4. Register the app: add `"scoring"` to INSTALLED_APPS in config/settings/base.py (in the same local-apps grouping as `"players"`, `"clubs"`, `"transfers"`, `"accounts"`, `"core"`).
    5. Add `"scoring"` to the `testpaths` list in pyproject.toml so it reads:
       `testpaths = ["clubs", "players", "transfers", "core", "accounts", "scoring"]`
    6. Create `get-scouted-be/scoring/tests/conftest.py` with a pytest fixture `real_data_available` that skips (pytest.skip) with a clear message if `Player.objects.count() == 0`, so real-data characterization tests degrade gracefully when the dev DB is empty. Mark it `@pytest.fixture` and use `@pytest.mark.django_db`. This phase's tests query REAL migrated Phase 1 data (not factory_boy fixtures) per 03-VALIDATION.md "Wave 0 Requirements".
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/python -c "import sklearn, joblib; print(sklearn.__version__, joblib.__version__)" && ./.venv/bin/python manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/requirements/base.txt` contains `scikit-learn` and contains `joblib`
    - `cd get-scouted-be && ./.venv/bin/python -c "import sklearn, joblib"` exits 0
    - `get-scouted-be/scoring/apps.py` contains `class ScoringConfig`
    - `get-scouted-be/config/settings/base.py` contains the string `scoring` inside INSTALLED_APPS
    - `get-scouted-be/pyproject.toml` testpaths line contains `scoring`
    - `get-scouted-be/scoring/tests/conftest.py` contains `real_data_available`
    - `cd get-scouted-be && ./.venv/bin/python manage.py check` exits 0
  </acceptance_criteria>
  <done>scikit-learn + joblib installed and pinned; scoring app registered and passing `manage.py check`; pytest collects scoring/tests/; real-data fixture available.</done>
</task>

<task type="auto">
  <name>Task 2: ORM→script-shaped DataFrame reconstruction module</name>
  <files>get-scouted-be/scoring/characterization/reconstruct.py, get-scouted-be/scoring/tests/test_reconstruct.py</files>
  <read_first>
    - get-scouted-be/docs/FIELD_MAPPING.md (SOURCE OF TRUTH for Django-field ↔ script-literal column names; §1 player stats, §1a Team_within_selected_timeframe→Team note, §3 club playing-style coverage 239/1059, §4 transfer fields)
    - get-scouted-be/players/models.py (Player: id UUID, unique_id int, player name, season, main_position, position, Minutes_played, club FK; PlayerRoleScore: player FK, position_group, role_name, role_name_raw, score — LONG format, 230,139 rows)
    - get-scouted-be/clubs/models.py (Club: 8 playing-style FloatFields control_possession, gegenpressing, direct_play, defensive_counter_attack, tiki_taka, counter_attack, wing_play, low_block — null for ~77% of clubs)
    - get-scouted-be/transfers/models.py (Transfer: player FK, player_name_raw, age_at_transfer, position, market_value_at_transfer, dealing_club, fee, is_loan, league_name, year)
    - pixel-perfect-clone-60729/docs/impact_model_v4.1.py lines 824-1055 (ROLE_COLUMNS_BY_POSITION definition), 1191-1197 (_rename_columns_safe), 2400-2726 (calculate_subjective_role_fit_for_player_to_team + weighted_overlap_score — to confirm exact team_styles_df column names/shape it expects; 03-RESEARCH.md Open Question 4 flags this as not-yet-fully-traced)
  </read_first>
  <action>
    Create `get-scouted-be/scoring/characterization/reconstruct.py` exposing four functions. Each returns a pandas DataFrame whose column NAMES match the script's literal expectations (spaces/commas/%/mixed-case), produced by renaming Django `.values()` output via the REVERSE of FIELD_MAPPING.md's table. Do NOT re-derive column names from scratch — read them off FIELD_MAPPING.md.

    1. `build_players_df() -> pd.DataFrame`:
       - `Player.objects.all().values(...)` → DataFrame, then `.rename(columns=<reverse of FIELD_MAPPING.md §1>)` so e.g. Django `Successful_defensive_actions_per_90` → `"Successful defensive actions per 90"`, `Market_value` → `"Market value"`, `Minutes_played` → `"Minutes"`, and the `Team_within_selected_timeframe`→`Team` rename (§1a) is applied.
       - Carry `id` (UUID) as a column named `player_id` — this is the stable join key back to Postgres (the script has no stable ID, only the non-unique `Player` name string).
       - Keep `main_position` available under whatever literal the calculators key on (`"Main_Position"` per 03-RESEARCH.md).
    2. `build_role_scores_wide() -> pd.DataFrame`:
       - Pivot `PlayerRoleScore` (long: one row per player×role) WIDE: index=player (UUID), columns=`role_name`, values=`score`. Validate the resulting columns cover the role names listed in ROLE_COLUMNS_BY_POSITION (impact_model_v4.1.py line ~824) and log any missing role columns rather than fabricating them.
    3. `build_team_styles_df() -> pd.DataFrame`:
       - `Club.objects.all().values("name", <8 style fields>)` → DataFrame, renamed to the exact column names `calculate_subjective_role_fit_for_player_to_team()` (line 2446) reads (confirm by reading lines 2400-2726 first).
       - Leave null playing-style values as NaN — do NOT `.fillna(0)`. Zero-filling a genuinely-absent style vector distorts role-fit (this is the CONCERNS.md silent-default bug class the phase exists to guard against; ~77% of clubs have null styles per FIELD_MAPPING.md §3).
    4. `build_transfers_df() -> pd.DataFrame`:
       - `Transfer.objects.all().values(...)` → DataFrame renamed to the literals `build_transfer_value_dataset()` reads: `Player`, `Year`, `Position`, `Age`, `Fee`, `Dealing_Club` (from Django `dealing_club`), `Club` (buying club name), `League` (from Django `league_name`), `is_loan` (verified list, 03-RESEARCH.md "Data Reconstruction").

    Add a module-level helper `assert_columns_present(df, required, name)` that raises `ValueError(f"{name} missing columns: {missing}")` if any required column is absent — every downstream calculator should call this before reading columns, so a reconstruction bug surfaces as a loud error, not a silent zero-filled score (03-RESEARCH.md Pitfall 3).

    Write `get-scouted-be/scoring/tests/test_reconstruct.py` (uses the `real_data_available` fixture + `@pytest.mark.django_db`) asserting: `build_players_df()` returns a DataFrame with a `player_id` column and a `"Minutes"` column; `build_role_scores_wide()` has one row per distinct player and more than one role column; `build_team_styles_df()` has at least one NaN style value (proving no silent zero-fill); `build_transfers_df()` contains columns `Player`, `Year`, `Fee`, `Dealing_Club`. If the dev DB is empty the fixture skips.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_reconstruct.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get-scouted-be/scoring/characterization/reconstruct.py` contains `def build_players_df`, `def build_role_scores_wide`, `def build_team_styles_df`, `def build_transfers_df`, and `def assert_columns_present`
    - `reconstruct.py` contains no `.fillna(0)` on the team-styles path (grep: `build_team_styles_df` block must not zero-fill)
    - `reconstruct.py` contains `player_id` (the UUID join-key column name)
    - `cd get-scouted-be && ./.venv/bin/pytest scoring/tests/test_reconstruct.py -x -q` exits 0
  </acceptance_criteria>
  <done>reconstruct.py produces all four script-shaped DataFrames from the ORM using FIELD_MAPPING.md's reverse rename, preserves NaN for absent club styles, exposes assert_columns_present, and its tests pass against real migrated data (or skip cleanly if DB empty).</done>
</task>

</tasks>

<verification>
- `./.venv/bin/python manage.py check` passes with `scoring` registered.
- `./.venv/bin/pytest scoring/tests/ -q` passes (or skips on empty DB).
- scikit-learn + joblib importable in the venv.
</verification>

<success_criteria>
Downstream plans (04 RMM, 05 CS/TP, 06 TFM, 07 oracle) can `from scoring.characterization.reconstruct import build_players_df, build_role_scores_wide, build_team_styles_df, build_transfers_df` and get script-shaped DataFrames from real Postgres data, with absent columns raising rather than silently defaulting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engine-curation-correctness-oracle/03-01-SUMMARY.md`
</output>

---
phase: 06-scoring-performance-caching-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-scouted-be/scoring/services/population.py
  - get-scouted-be/scoring/tests/test_services_population.py
autonomous: true
requirements: [SCORE-07]
must_haves:
  truths:
    - "Calling reconstruct_population() N times in one process queries Postgres and rebuilds the DataFrames only ONCE (subsequent calls return the memoized result)"
    - "The expensive own-club whole-population scoring aggregate is computed at most once per process until explicitly invalidated"
    - "There is a single clear_scoring_caches() function that invalidates every memoized aggregate so a data refresh forces a fresh rebuild"
  artifacts:
    - path: "get-scouted-be/scoring/services/population.py"
      provides: "lru_cache-memoized reconstruct_population + get_scored_population aggregate + clear_scoring_caches invalidator"
      contains: "def clear_scoring_caches"
    - path: "get-scouted-be/scoring/tests/test_services_population.py"
      provides: "tests proving the DB reconstruction is memoized and clear_scoring_caches forces a rebuild"
      contains: "cache_clear"
  key_links:
    - from: "get-scouted-be/scoring/services/population.py::reconstruct_population"
      to: "functools.lru_cache"
      via: "@lru_cache(maxsize=1) decorator (same precedent as get_tfm_pipeline)"
      pattern: "@lru_cache\\(maxsize=1\\)"
    - from: "get-scouted-be/scoring/services/population.py::clear_scoring_caches"
      to: "reconstruct_population.cache_clear / get_scored_population.cache_clear"
      via: "explicit .cache_clear() calls"
      pattern: "\\.cache_clear\\(\\)"
---

<objective>
Extend the in-process memoization pattern this codebase already established (`@lru_cache(maxsize=1)` on `get_tfm_pipeline()`) to the two expensive whole-population operations — `reconstruct_population()` (the ~3.5s ORM to DataFrame rebuild) and the own-club whole-population scoring aggregate — and add a single `clear_scoring_caches()` invalidator the recompute command (Plan 03) calls after a data refresh.

Purpose: Success Criterion 2 — "Precomputed aggregates (std_lookup, team-style vectors, team-position references) are rebuilt on data change, not per request." After this plan, a live arbitrary-club request reuses the cached population/team-styles instead of re-querying all 41,708 players from Postgres every time; and the own-club aggregate the denormalization pipeline depends on is computed once, not per request.
Output: memoized reconstruct_population + get_scored_population + clear_scoring_caches, plus cache-behavior tests.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-scoring-performance-caching-layer/06-CONTEXT.md

<interfaces>
<!-- The exact existing memoization precedent to extend. From get-scouted-be/scoring/services/population.py -->

The pattern to replicate (population.py:142-155):
  @lru_cache(maxsize=1)
  def get_tfm_pipeline() -> tuple[object, list[str]]: ...

The two functions being memoized (population.py:54-114) — both currently un-cached, re-run on every request:
  def reconstruct_population() -> Population:   # ~3.5s: 4x build_* ORM queries
  def score_population(pop, club_name):         # ~74-115s full-population scoring; RMM (add_player_impact) is club-independent

Confirmed safe to memoize reconstruct_population: every caller treats the returned Population read-only — score_population does pop.players_df.copy(); get_summary/get_compatibility do pop.players_df.merge(...) (returns a new frame); financial_fit's _merge_tfm_feature_columns does base_players_df.copy(). No caller mutates pop in place.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memoize reconstruct_population + add get_scored_population aggregate + clear_scoring_caches</name>
  <files>get-scouted-be/scoring/services/population.py</files>
  <read_first>
    - get-scouted-be/scoring/services/population.py  (the WHOLE file — you are extending its existing @lru_cache(maxsize=1) precedent at line 142 and modifying reconstruct_population at line 54 + adding functions after score_population at line 114)
    - get-scouted-be/scoring/services/summary.py  (confirm get_summary treats pop read-only via .merge — so memoizing reconstruct_population cannot corrupt it)
    - get-scouted-be/scoring/services/financial_fit.py  (confirm _merge_tfm_feature_columns does base_players_df.copy() — same read-only guarantee)
  </read_first>
  <behavior>
    - reconstruct_population() called twice in the same process performs the underlying build_*() queries only once (the second call is a cache hit; cache_info().hits increments).
    - get_scored_population() returns the same (scored, cs_tp) tuple on repeat calls without re-running the ~74-115s scoring.
    - clear_scoring_caches() resets both caches so the next call rebuilds fresh (each cache_info().currsize drops to 0).
  </behavior>
  <action>
Edit get-scouted-be/scoring/services/population.py:

STEP 1 — Add `@lru_cache(maxsize=1)` DIRECTLY above the existing `def reconstruct_population() -> Population:` (line 54). It takes no arguments, so lru_cache memoizes its single output — identical mechanics to the existing `get_tfm_pipeline`. Update its docstring's first line to note it is now memoized and invalidated via `clear_scoring_caches()`.

STEP 2 — Add a NEW memoized own-club aggregate function immediately AFTER `score_population` (after line 114), copied verbatim:

    @lru_cache(maxsize=1)
    def get_scored_population() -> tuple[pd.DataFrame, pd.DataFrame]:
        """Memoized own-club (club_context=None) whole-population scoring.

        Runs score_population(reconstruct_population(), None) at most once per
        process — the ~74-115s full-population pass — and hands back the cached
        (scored, cs_tp) every subsequent call. This is the precomputed
        aggregate SCORE-07 Success Criterion 2 requires: the recompute_scores
        command (writes the denormalized Player fields) and any own-club live
        path both read this instead of re-scoring 41,708 players per request.

        Invalidated by clear_scoring_caches() after a data refresh. Callers
        MUST treat the returned DataFrames as read-only (copy/merge before
        mutating), exactly as reconstruct_population's callers already do.
        """
        return score_population(reconstruct_population(), None)

STEP 3 — Add a single invalidator function at the end of the file, copied verbatim:

    def clear_scoring_caches() -> None:
        """Invalidate every in-process scoring aggregate cache so the next
        call rebuilds against fresh data. Call this after any data refresh
        (Phase 1's import_all) and at the end of the recompute_scores command.
        Does NOT clear get_tfm_pipeline — the joblib artifact only changes via
        train_tfm_model, not a data refresh."""
        reconstruct_population.cache_clear()
        get_scored_population.cache_clear()

Do NOT change score_population's internal logic, the RMM-first ordering, or any scoring math — this plan only adds a caching layer around the existing proven functions. Do NOT memoize score_population itself (its `pop` argument is a tuple of unhashable DataFrames — lru_cache would raise TypeError); the arbitrary-club live path keeps calling `score_population(reconstruct_population(), club_name)` and simply benefits from the now-cached reconstruction.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "src=open('scoring/services/population.py').read(); assert src.count('@lru_cache(maxsize=1)')==3, src.count('@lru_cache(maxsize=1)'); assert 'def get_scored_population' in src; assert 'def clear_scoring_caches' in src; assert 'reconstruct_population.cache_clear()' in src; assert 'get_scored_population.cache_clear()' in src; print('OK')" && python manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@lru_cache(maxsize=1)" get-scouted-be/scoring/services/population.py` returns 3 (get_tfm_pipeline + reconstruct_population + get_scored_population)
    - `grep -n "def get_scored_population" get-scouted-be/scoring/services/population.py` returns exactly one line
    - `grep -n "def clear_scoring_caches" get-scouted-be/scoring/services/population.py` returns exactly one line
    - `grep -c ".cache_clear()" get-scouted-be/scoring/services/population.py` returns 2
    - `grep -c "def score_population" get-scouted-be/scoring/services/population.py` still returns 1 and its body is unchanged (no @lru_cache above `def score_population`)
    - `cd get-scouted-be && python manage.py check` exits 0
  </acceptance_criteria>
  <done>reconstruct_population and a new get_scored_population are both @lru_cache(maxsize=1)-memoized, clear_scoring_caches() invalidates both, score_population's math/ordering is untouched, and manage.py check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add cache-behavior tests (memoization proven + invalidation proven)</name>
  <files>get-scouted-be/scoring/tests/test_services_population.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/test_services_population.py  (existing test file — extend it; note it already imports from scoring.services.population and uses the real_data_available fixture pattern for DB-touching tests)
    - get-scouted-be/scoring/tests/conftest.py  (the real_data_available fixture that skips cleanly when the dev DB is empty — DB-touching cache tests must depend on it)
    - get-scouted-be/scoring/services/population.py  (post-Task-1 — the functions under test)
  </read_first>
  <action>
Add tests to get-scouted-be/scoring/tests/test_services_population.py:

TEST A (no DB needed — pure memoization mechanics via mock): patch `scoring.services.population.build_players_df`, `build_role_scores_wide`, `build_team_styles_df`, `build_transfers_df` to return trivial empty DataFrames using `unittest.mock.patch`, then:
  - call `clear_scoring_caches()` first (defensive — other tests may have warmed the cache),
  - call `reconstruct_population()` twice,
  - assert each of the 4 patched build_* mocks was called EXACTLY once (memoization proven — the second reconstruct_population() call was a cache hit),
  - call `clear_scoring_caches()` then `reconstruct_population()` once more,
  - assert each build_* mock has now been called exactly twice (invalidation proven — the cache was rebuilt).
  Use `reconstruct_population.cache_clear()` in a try/finally (or a fixture) so this test cannot leak a poisoned mock-backed cache into other tests.

TEST B (cache_info sanity, no DB): after `clear_scoring_caches()`, assert `reconstruct_population.cache_info().currsize == 0` and `get_scored_population.cache_info().currsize == 0`.

Import the new symbols at the top: add `get_scored_population` and `clear_scoring_caches` to the existing `from scoring.services.population import (...)` block.

Do NOT write a test that actually runs the ~74-115s get_scored_population() against real data here — that heavy real-data timing assertion is Plan 04's job. These tests are fast, mock-backed cache-mechanics tests only.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -m pytest scoring/tests/test_services_population.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cache_clear\|clear_scoring_caches\|cache_info" get-scouted-be/scoring/tests/test_services_population.py` is >= 3
    - The test file imports `get_scored_population` and `clear_scoring_caches` from scoring.services.population (grep confirms both names present in an import block)
    - `cd get-scouted-be && python -m pytest scoring/tests/test_services_population.py -q` exits 0 with the two new tests passing (mock-backed, so they pass even against an empty dev DB)
    - There is a mock-based assertion that a build_* function is called exactly once across two reconstruct_population() calls (grep for `call_count` or `assert_called_once`)
  </acceptance_criteria>
  <done>Two new fast tests prove reconstruct_population memoizes (build_* called once across repeat calls) and clear_scoring_caches forces a rebuild (currsize 0 / build_* called again), both green under pytest without needing real data.</done>
</task>

</tasks>

<verification>
- `python -m pytest scoring/tests/test_services_population.py -q` green.
- population.py has exactly 3 @lru_cache(maxsize=1) sites and a clear_scoring_caches() that resets both new caches.
- No scoring math or RMM-first ordering changed — diff of score_population's body is empty.
</verification>

<success_criteria>
- reconstruct_population()'s ~3.5s DB rebuild happens once per process, not per request (memoization proven by test).
- get_scored_population() is the reusable own-club aggregate the recompute command and own-club paths read once.
- clear_scoring_caches() gives the recompute command (Plan 03) a single, tested way to force a fresh rebuild after a data refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-performance-caching-layer/06-02-SUMMARY.md`
</output>

---
phase: 06-scoring-performance-caching-layer
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - get-scouted-be/scoring/tests/test_scoring_performance.py
autonomous: true
requirements: [SCORE-07]
must_haves:
  truths:
    - "An automated test measures single-player score retrieval on the O(1) denormalized-field path and proves it is at least an order of magnitude faster than a cold full-population recomputation"
    - "The test measures the warm-cache aggregate path vs the cold (cache-cleared) path and asserts the warm path is dramatically faster"
    - "The timing check is a real assertion (pytest fails if the fast path is not fast), not an eyeballed print"
  artifacts:
    - path: "get-scouted-be/scoring/tests/test_scoring_performance.py"
      provides: "automated timing test proving flat/O(1) per-entity retrieval vs linear full-population recompute"
      contains: "time.perf_counter"
  key_links:
    - from: "get-scouted-be/scoring/tests/test_scoring_performance.py"
      to: "Player.objects.only(...).get(id=...) denormalized read"
      via: "timed O(1) field read compared against a timed reconstruct+score baseline"
      pattern: "impact_score"
---

<objective>
Add the automated timing test that proves Success Criterion 4 — "A timing check confirms per-entity score retrieval stays flat as dataset size grows, rather than scaling linearly with player count." It measures the O(1) denormalized-field read path (Plan 01/03) and the warm-cache aggregate path (Plan 02) against a cold full-population recomputation baseline, and asserts an order-of-magnitude speedup as a hard pytest assertion.

Purpose: turn the phase's core performance claim into a real, repeatable test rather than a manual/eyeballed measurement — the last of the 4 ROADMAP success criteria.
Output: test_scoring_performance.py with a passing timing assertion against the real dev DB.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-scoring-performance-caching-layer/06-CONTEXT.md

<interfaces>
<!-- What to time, and the measured baselines from Phase 5 research. -->

Cold baseline (Phase 5 research, confirmed live-timed): reconstruct_population() ~3.5s alone; full score_population() ~74-115s over 41,708 players. This is the "scales linearly with player count" path.

Fast paths introduced by this phase:
  - O(1) denormalized read (Plan 01/03): Player.objects.only("id","impact_score","compatibility_score","financial_fit_score","transfer_probability_score").get(id=pid) — a single indexed row fetch, independent of population size.
  - Warm-cache aggregate (Plan 02): after get_scored_population() has been called once, subsequent calls are a memoized dict lookup (~microseconds) vs the cold ~74-115s.

Plan 02 gives: reconstruct_population(), get_scored_population(), clear_scoring_caches() in scoring.services.population.
Plan 03's recompute_scores must have been run so the denormalized fields hold real values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timing test — O(1) denormalized read + warm-cache aggregate vs cold full recompute</name>
  <files>get-scouted-be/scoring/tests/test_scoring_performance.py</files>
  <read_first>
    - get-scouted-be/scoring/tests/conftest.py  (real_data_available skip fixture)
    - get-scouted-be/scoring/tests/test_parity_bulk.py  (lines 49-60 — module-scope django_db_blocker.unblock() heavy-real-data fixture pattern; this timing test must run against the populated dev DB, not pytest's empty test DB)
    - get-scouted-be/scoring/services/population.py  (post-Plan-02 — reconstruct_population/get_scored_population/clear_scoring_caches under test)
    - get-scouted-be/players/models.py  (the denormalized fields the O(1) path reads)
  </read_first>
  <action>
Create get-scouted-be/scoring/tests/test_scoring_performance.py, following the real-data test pattern (module-scope django_db_blocker.unblock() + real_data_available skip so it degrades cleanly on an empty test DB and is live-verified via the project .venv against the real 41,708-player dev DB).

Use `time.perf_counter()` for all timing. Structure:

TEST A — warm aggregate vs cold recompute (order-of-magnitude assertion):
  1. `clear_scoring_caches()` to guarantee a cold start.
  2. Time a COLD `get_scored_population()` call -> `t_cold` (this pays the full reconstruct + ~74-115s scoring once). Record it.
  3. Time a WARM `get_scored_population()` call (immediately after, cache now populated) -> `t_warm` (a memoized return).
  4. Assert `t_warm < t_cold / 10` (warm path at least 10x faster) AND `t_warm < 0.5` seconds (a memoized return is sub-second regardless). Use these concrete thresholds. Include an informative assertion message printing both timings.

TEST B — O(1) denormalized read is flat and fast:
  1. Pick a real player id: `pid = Player.objects.filter(impact_score__isnull=False).values_list("id", flat=True).first()` (requires recompute_scores to have run — if it returns None, skip with a message telling the operator to run `manage.py recompute_scores` first).
  2. Time 100 iterations of `Player.objects.only("id","impact_score","compatibility_score","financial_fit_score","transfer_probability_score").get(id=pid)` -> total, then per-read = total/100.
  3. Assert per-read is < 0.05s (50ms) — a single indexed-row fetch, independent of the 41,708-row population size. This is the concrete "stays flat as dataset size grows" evidence: it reads one row by PK regardless of N.
  4. Assert the fetched row's impact_score is not None (proves the O(1) path returns a real precomputed score, not a placeholder).

TEST C — flatness across growing subsets (the literal "flat as N grows" check):
  1. For sample sizes N in [100, 1000, 10000] (capped at Player.objects.count()), time fetching one player's denormalized fields by PK while that many rows exist in the table (the table already holds all rows; you are timing the same single-row PK fetch and asserting it does NOT grow with N).
  2. Assert the per-fetch time for N=10000-context is within 3x of the N=100-context time (i.e. it does not scale linearly with N). Since it is a PK lookup this should hold trivially; the test documents and enforces the O(1) property.

Set the thresholds as module-level constants (WARM_SPEEDUP_FACTOR=10, MAX_WARM_SECONDS=0.5, MAX_PER_READ_SECONDS=0.05, MAX_FLATNESS_RATIO=3.0) with a comment noting these are Claude's-discretion thresholds grounded in Phase 5's measured ~74-115s cold baseline vs a memoized/PK-indexed fast path.

Do NOT mock the DB or the scoring functions for TEST A/B/C — the whole point is a real timing measurement against real data. TEST A legitimately takes ~1-2 minutes for its one cold call; mark the module with `@pytest.mark.integration` (the existing marker registered in pyproject.toml) so it can be selected/deselected, matching the project's convention for slow real-data tests.
  </action>
  <verify>
    <automated>cd get-scouted-be && python -c "src=open('scoring/tests/test_scoring_performance.py').read(); assert 'perf_counter' in src; assert 'get_scored_population' in src; assert 'clear_scoring_caches' in src; assert 'impact_score' in src; assert 'WARM_SPEEDUP_FACTOR' in src or 't_cold / 10' in src; assert '.only(' in src; assert 'pytest.mark.integration' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "perf_counter" get-scouted-be/scoring/tests/test_scoring_performance.py` >= 2 (cold and warm timings)
    - `grep -c "get_scored_population" get-scouted-be/scoring/tests/test_scoring_performance.py` >= 2 (cold + warm call)
    - `grep -c "clear_scoring_caches" get-scouted-be/scoring/tests/test_scoring_performance.py` >= 1 (cold-start guarantee)
    - `grep -E "t_warm < t_cold / 10|WARM_SPEEDUP_FACTOR" get-scouted-be/scoring/tests/test_scoring_performance.py` present (the order-of-magnitude assertion)
    - `grep -c ".only(" get-scouted-be/scoring/tests/test_scoring_performance.py` >= 1 (O(1) denormalized-field read path)
    - `grep -c "pytest.mark.integration" get-scouted-be/scoring/tests/test_scoring_performance.py` >= 1
    - `cd get-scouted-be && python -m pytest scoring/tests/test_scoring_performance.py -q -m integration` exits 0 against the real dev DB (or skips cleanly on empty DB — never errors)
    - VERIFICATION EVIDENCE: the executor pastes the actual measured t_cold, t_warm, and per-read timings into the SUMMARY, showing the warm/denormalized path is >=10x faster than the cold full-population recompute
  </acceptance_criteria>
  <done>test_scoring_performance.py hard-asserts (1) the warm cached aggregate is >=10x faster than the cold ~74-115s recompute, (2) a single denormalized-field read is <50ms and returns a real score, and (3) that read time stays flat (within 3x) as the effective N grows — all green against the real dev DB, with measured timings recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `python -m pytest scoring/tests/test_scoring_performance.py -q -m integration` green against the real dev DB.
- Measured timings recorded: cold full recompute (tens of seconds) vs warm memoized (<0.5s) vs O(1) denormalized read (<50ms).
- The order-of-magnitude speedup is a real assertion, not a printed observation.
</verification>

<success_criteria>
- Success Criterion 4 is satisfied by an automated, repeatable timing test — per-entity retrieval proven flat/O(1) vs the linear full-population baseline.
- Thresholds are explicit constants grounded in Phase 5's measured baselines.
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-performance-caching-layer/06-04-SUMMARY.md`
</output>

---
phase: 02-auth-access-control
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - get-scouted-be/accounts/permissions.py
  - get-scouted-be/accounts/serializers.py
  - get-scouted-be/accounts/views.py
  - get-scouted-be/accounts/urls.py
  - get-scouted-be/accounts/tests/test_permissions.py
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "A reusable MinimumRole(role) permission factory encodes the additive role hierarchy (scout=analyst < director < admin) in one place"
    - "An authenticated user can view and edit their OWN profile at /api/auth/me/ (display_name), but cannot change their own role (self-escalation blocked)"
    - "An unauthenticated request to a write endpoint returns 401/403, never 200 (deny-by-default proven on a real endpoint)"
    - "A scout/analyst attempting an admin-only action (changing another user's role) returns 403"
    - "A director can list/view all user accounts read-only (org-wide visibility) but cannot change roles or deactivate anyone"
    - "An admin can list users, change any user's role, and deactivate (soft, is_active=False) an account; accounts are never hard-deleted"
    - "A role change or deactivation performed by an admin takes effect on the target user's very next request (no re-login required)"
  artifacts:
    - path: "get-scouted-be/accounts/permissions.py"
      provides: "ROLE_RANK, MinimumRole(role) factory, IsSelfOrAdmin"
      contains: "def MinimumRole"
    - path: "get-scouted-be/accounts/views.py"
      provides: "ProfileView (self RetrieveUpdate), AdminUserViewSet (director read-only, admin read/write user management)"
      contains: "class AdminUserViewSet"
    - path: "get-scouted-be/accounts/serializers.py"
      provides: "ProfileSerializer (role read-only), AdminUserSerializer (role/is_active writable by admin)"
      contains: "class ProfileSerializer"
  key_links:
    - from: "get-scouted-be/accounts/views.py"
      to: "accounts.permissions.MinimumRole"
      via: "AdminUserViewSet.get_permissions()"
      pattern: "MinimumRole"
    - from: "get-scouted-be/accounts/permissions.py"
      to: "request.user.role"
      via: "DB-fresh role read (never the JWT claim)"
      pattern: "user.role"
    - from: "get-scouted-be/accounts/urls.py"
      to: "accounts.views.AdminUserViewSet"
      via: "DefaultRouter under admin/users/"
      pattern: "AdminUserViewSet"
---

<objective>
Implement the role-based permission enforcement (AUTH-02) with two concrete Phase-2-owned
write endpoints that prove the pattern before Phase 7/8 reuse it: a self-service profile
endpoint (/api/auth/me/) and an org-wide user-management surface
(/api/auth/admin/users/) that is read-only for director and read/write for admin. The
additive role hierarchy lives in ONE reusable MinimumRole(role) factory. Role/deactivation
changes take effect on the target's next request for free (default JWTAuthentication
re-fetches the user row per request; permission checks read request.user.role from the DB,
never the JWT claim).

Purpose: Satisfy "every write path has an explicit, testable permission check; no
allow-by-default" and give Phases 7/8 a single role-gating primitive to import, plus a
concrete first instance of the "director gets read-only org-wide visibility, admin gets
read/write" shape they will replicate on Watchlist/Shortlist/SquadPlan.

Output: permissions.py factory + profile + admin user-management endpoints, all role-gated
and tested.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-access-control/02-CONTEXT.md
@.planning/phases/02-auth-access-control/02-RESEARCH.md
@.planning/phases/02-auth-access-control/02-VALIDATION.md

# Plan 01/02 outputs this plan builds on
@get-scouted-be/accounts/models.py
@get-scouted-be/accounts/views.py
@get-scouted-be/accounts/serializers.py
@get-scouted-be/accounts/urls.py
@get-scouted-be/accounts/tests/conftest.py

<interfaces>
<!-- Executor MUST use these exact shapes. Copied from 02-RESEARCH.md Code Examples. -->

accounts/permissions.py — rank-based factory + IsSelfOrAdmin (the ROLE_RANK table is the
single source of truth for the additive hierarchy; scout and analyst share rank 1 per the
locked decision):

    from rest_framework.permissions import BasePermission

    ROLE_RANK = {
        "scout": 1,
        "analyst": 1,
        "director": 2,
        "admin": 3,
    }


    def MinimumRole(role: str):
        required_rank = ROLE_RANK[role]

        class _MinimumRole(BasePermission):
            message = "You do not have the required role for this action."

            def has_permission(self, request, view):
                user = request.user
                if not user or not user.is_authenticated:
                    return False
                # Always re-read from the DB-fetched request.user, never a JWT claim.
                return ROLE_RANK.get(user.role, 0) >= required_rank

        return _MinimumRole


    class IsSelfOrAdmin(BasePermission):
        def has_object_permission(self, request, view, obj):
            return obj == request.user or request.user.role == "admin"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MinimumRole/IsSelfOrAdmin permissions + self-service profile endpoint (/api/auth/me/)</name>
  <files>get-scouted-be/accounts/permissions.py, get-scouted-be/accounts/serializers.py, get-scouted-be/accounts/views.py, get-scouted-be/accounts/urls.py, get-scouted-be/accounts/tests/test_permissions.py</files>
  <read_first>
    - get-scouted-be/accounts/models.py (User.Role values, role field)
    - get-scouted-be/accounts/views.py (existing views; extend, do not clobber Plan 02 views)
    - get-scouted-be/accounts/serializers.py (existing serializers; add ProfileSerializer)
    - get-scouted-be/accounts/urls.py (existing routes; add me/)
    - get-scouted-be/accounts/tests/conftest.py (authenticated_client(role) fixture)
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Pattern 2 rank factory; Pitfall 4 — role freshness for free; Anti-pattern: never trust JWT claim / never use JWTStatelessUserAuthentication)
  </read_first>
  <behavior>
    - GET /api/auth/me/ with a valid token -> 200 returning the requesting user's own email, display_name, role.
    - PATCH /api/auth/me/ {display_name:"New"} -> 200; display_name updated.
    - PATCH /api/auth/me/ {role:"admin"} by a scout -> role is NOT changed (role is read-only on ProfileSerializer); response still 200 with role unchanged (self-escalation blocked).
    - GET/PATCH /api/auth/me/ with NO Authorization header -> 401 (deny-by-default via global IsAuthenticated).
    - Role-freshness: given a director client, if an admin sets that user's role to "scout" in the DB, the SAME user's next request that requires director rank is denied (403) without re-login — because JWTAuthentication re-fetches the user row and MinimumRole reads request.user.role from the DB.
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    Create accounts/permissions.py exactly per the <interfaces> block (ROLE_RANK, MinimumRole
    factory, IsSelfOrAdmin). Do NOT read the JWT claim anywhere; always use request.user.role.

    Add ProfileSerializer to accounts/serializers.py: a ModelSerializer over User with
    fields = ["id", "email", "display_name", "role"] and read_only_fields = ["id", "email", "role"]
    (role read-only enforces the no-self-role-change locked decision; email is the identity, also
    read-only here).

    Add ProfileView to accounts/views.py: generics.RetrieveUpdateAPIView with
    serializer_class = ProfileSerializer; permission_classes = [IsAuthenticated] (the global
    default already covers this, but set it explicitly for clarity); override get_object to
    return self.request.user (so /me/ always targets the caller, no pk in the URL).

    Add to accounts/urls.py: path("me/", ProfileView.as_view(), name="profile").

    test_permissions.py (this task's subset): test_write_requires_auth (unauthenticated PATCH /me/
    -> 401), test_profile_self_edit (display_name updates), test_profile_role_read_only (scout
    PATCH role=admin leaves role unchanged), test_role_change_takes_effect_immediately (director
    client; flip the user's role to scout in the DB via ORM; a follow-up request through the
    admin endpoint from Task 2 — GET /api/auth/admin/users/, which requires at least director
    rank — is denied 403 without re-login). Since Task 2's endpoint does not exist until that
    task lands, write this test in this file but it may run/pass only once Task 2 is complete;
    if you need an immediacy assertion sooner, additionally assert directly that
    MinimumRole("director")().has_permission(request, view) flips to False after refreshing
    request.user from the DB mid-test — but the primary assertion MUST exercise a real endpoint,
    not just the permission class in isolation.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_permissions.py -x -q -k "auth or profile"</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_permissions.py -x -q -k "auth or profile"` exits 0
    - `grep -q "def MinimumRole" get-scouted-be/accounts/permissions.py` succeeds
    - `grep -q "ROLE_RANK" get-scouted-be/accounts/permissions.py` succeeds
    - `grep -q "class IsSelfOrAdmin" get-scouted-be/accounts/permissions.py` succeeds
    - `grep -q "class ProfileSerializer" get-scouted-be/accounts/serializers.py` succeeds
    - `grep -q "read_only_fields" get-scouted-be/accounts/serializers.py` succeeds
    - `grep -q "class ProfileView" get-scouted-be/accounts/views.py` succeeds
    - `grep -q 'path("me/"' get-scouted-be/accounts/urls.py` succeeds
    - `grep -q "def test_profile_role_read_only" get-scouted-be/accounts/tests/test_permissions.py` succeeds
  </acceptance_criteria>
  <done>ROLE_RANK/MinimumRole/IsSelfOrAdmin exist in permissions.py; /api/auth/me/ lets a user view/edit their own display_name but never their own role; unauthenticated access is 401; tests for auth-requirement and self-edit/role-read-only are green (immediacy test completes once Task 2 lands).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminUserViewSet — director read-only, admin read/write user management (org-wide visibility + role change + soft deactivation)</name>
  <files>get-scouted-be/accounts/serializers.py, get-scouted-be/accounts/views.py, get-scouted-be/accounts/urls.py, get-scouted-be/accounts/tests/test_permissions.py</files>
  <read_first>
    - get-scouted-be/accounts/permissions.py (MinimumRole factory from Task 1)
    - get-scouted-be/accounts/models.py (User fields: id, email, display_name, role, is_active, date_joined)
    - get-scouted-be/accounts/urls.py (routes so far; this task adds a router-based admin/users/ prefix)
    - .planning/phases/02-auth-access-control/02-CONTEXT.md (Role Permission Matrix: director = org-wide READ visibility only; admin = director + user management, including role change and soft deactivation; accounts never hard-deleted)
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Pattern 2 — MinimumRole composes per-action; "Don't Hand-Roll" table — no bespoke revocation table needed, freshness is free via JWTAuthentication)
  </read_first>
  <behavior>
    - GET /api/auth/admin/users/ (list) and GET /api/auth/admin/users/{id}/ (retrieve) by a director -> 200, full user list/detail visible (org-wide read visibility).
    - GET (list/retrieve) by a scout/analyst -> 403 (below director rank).
    - PATCH /api/auth/admin/users/{id}/ {role:"director"} by a director (not admin) -> 403 (write requires admin rank; director is view-only oversight per the locked decision).
    - PATCH /api/auth/admin/users/{id}/ {role:"director"} by an admin -> 200; the target user's role is updated.
    - POST /api/auth/admin/users/{id}/deactivate/ by an admin -> 200; target user's is_active becomes False; the user row still exists (no hard delete, ever — no destroy route/action is exposed on this viewset at all).
    - POST .../deactivate/ by a director -> 403.
    - Immediacy: after an admin PATCHes another user's role (or deactivates them), that target user's very next request through this same admin/users/ endpoint (if their new role still qualifies) or any director-gated endpoint reflects the new role/is_active immediately — no re-login, because JWTAuthentication re-fetches the user row per request and MinimumRole reads request.user.role fresh. This completes Task 1's test_role_change_takes_effect_immediately.
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    Add AdminUserSerializer to accounts/serializers.py: ModelSerializer over User with
    fields = ["id", "email", "display_name", "role", "is_active", "date_joined"] and
    read_only_fields = ["id", "email", "date_joined"] (role and is_active ARE writable here —
    this serializer is only ever reached through admin-rank-gated actions, unlike
    ProfileSerializer).

    Add AdminUserViewSet to accounts/views.py as a
    viewsets.GenericViewSet with mixins.ListModelMixin, mixins.RetrieveModelMixin,
    mixins.UpdateModelMixin ONLY (deliberately no CreateModelMixin — registration is the only
    creation path — and no DestroyModelMixin — accounts are never hard-deleted):
      - queryset = User.objects.all().order_by("email"); serializer_class = AdminUserSerializer
      - permission_classes default to [MinimumRole("director")] (covers list/retrieve — org-wide
        read visibility for director and admin alike)
      - override get_permissions(self): for self.action in ("update", "partial_update",
        "deactivate") return [MinimumRole("admin")()]; otherwise return
        [MinimumRole("director")()]
      - add a custom action:
            from rest_framework.decorators import action
            from rest_framework.response import Response

            @action(detail=True, methods=["post"])
            def deactivate(self, request, pk=None):
                user = self.get_object()
                user.is_active = False
                user.save(update_fields=["is_active"])
                return Response(self.get_serializer(user).data)

    Wire into accounts/urls.py using a DRF router:
        from rest_framework.routers import DefaultRouter
        from .views import AdminUserViewSet

        router = DefaultRouter()
        router.register("admin/users", AdminUserViewSet, basename="admin-users")

        urlpatterns = [
            ...,  # existing paths from Plans 02/03 Task 1
        ] + router.urls

    test_permissions.py (this task's subset): test_role_gated_403 (scout GET list -> 403),
    test_director_read_only_visibility (director GET list -> 200 sees all users; director
    PATCH role -> 403; director POST deactivate -> 403), test_admin_can_change_role (admin PATCH
    role -> 200, role persisted), test_admin_can_deactivate_soft (admin POST deactivate/ -> 200,
    is_active False, row still exists via User.objects.filter(pk=...).exists() True),
    test_no_hard_delete_route (DELETE /api/auth/admin/users/{id}/ -> 405, proving no destroy
    action is registered). Finish test_role_change_takes_effect_immediately from Task 1 here:
    create a director-authenticated client, have an admin (via ORM, not the API) flip that
    user's role to "scout", then re-issue a GET /api/auth/admin/users/ request with the SAME
    original access token/client -> now 403 (rank dropped below director) without any re-login —
    proving DB-fresh role reads.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_permissions.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_permissions.py -x -q` exits 0 (full file, including Task 1's tests)
    - `grep -q "class AdminUserSerializer" get-scouted-be/accounts/serializers.py` succeeds
    - `grep -q "class AdminUserViewSet" get-scouted-be/accounts/views.py` succeeds
    - `grep -q "def get_permissions" get-scouted-be/accounts/views.py` succeeds
    - `grep -q 'MinimumRole("admin")' get-scouted-be/accounts/views.py` succeeds
    - `grep -q 'MinimumRole("director")' get-scouted-be/accounts/views.py` succeeds
    - `grep -q "def deactivate" get-scouted-be/accounts/views.py` succeeds
    - `grep -q "DestroyModelMixin" get-scouted-be/accounts/views.py` returns nothing (no hard delete mixin) — verify via `! grep -q "DestroyModelMixin" get-scouted-be/accounts/views.py`
    - `grep -q "router.register" get-scouted-be/accounts/urls.py` succeeds
    - `grep -q "def test_role_change_takes_effect_immediately" get-scouted-be/accounts/tests/test_permissions.py` succeeds
    - `grep -q "def test_no_hard_delete_route" get-scouted-be/accounts/tests/test_permissions.py` succeeds
  </acceptance_criteria>
  <done>AdminUserViewSet exposes org-wide user visibility (director+) and role-change/soft-deactivation (admin-only) with no hard-delete route ever registered; the full test_permissions.py file is green, including the immediacy test proving role/deactivation changes apply on the very next request with no re-login.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && ./.venv/bin/pytest accounts -x -q` exits 0 (Plans 01/02/03 combined).
- `cd get-scouted-be && ./.venv/bin/python manage.py check` still exits 0.
- Manual sanity (optional): a director JWT can GET /api/auth/admin/users/ but a PATCH/deactivate from that same token is rejected 403; an admin JWT can do both.
</verification>

<success_criteria>
- ROLE_RANK/MinimumRole(role)/IsSelfOrAdmin exist as the single reusable role-gating primitive in accounts/permissions.py.
- /api/auth/me/ lets any authenticated user view/edit their own profile but never their own role.
- /api/auth/admin/users/ gives director+ read-only org-wide visibility and admin-only write access (role change, soft deactivation); no hard-delete route exists anywhere.
- A role/deactivation change made by an admin is visible on the target user's very next request, without re-login, proven by an integration test.
- Unauthenticated and under-ranked requests to every endpoint in this plan return 401/403, never 200.
- All Plan 03 tests green; full `accounts` suite (Plans 01-03) green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-access-control/02-03-SUMMARY.md`.
</output>

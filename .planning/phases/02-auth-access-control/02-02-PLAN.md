---
phase: 02-auth-access-control
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - get-scouted-be/accounts/serializers.py
  - get-scouted-be/accounts/views.py
  - get-scouted-be/accounts/urls.py
  - get-scouted-be/config/urls.py
  - get-scouted-be/accounts/tests/test_registration.py
  - get-scouted-be/accounts/tests/test_auth_flow.py
  - get-scouted-be/accounts/tests/test_password_reset.py
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "A user can POST to /api/auth/register/ with email, password, display_name, and role in {scout, analyst, director} and get an active account; role=admin is rejected"
    - "A user can POST to /api/auth/login/ with correct credentials and receive access + refresh tokens; the access token carries a role claim"
    - "Login with wrong credentials returns a generic 401 that does not reveal whether the email or password was wrong"
    - "POST to /api/auth/token/refresh/ rotates the refresh token and blacklists the previously-used one"
    - "POST to /api/auth/logout/ blacklists the submitted refresh token so it can no longer be refreshed"
    - "Only rest_framework_simplejwt.JWTAuthentication is a configured DRF auth class (no legacy JWT/Supabase path)"
    - "A user can request a password reset link (console-emailed) and confirm it to set a new password"
  artifacts:
    - path: "get-scouted-be/accounts/serializers.py"
      provides: "RegisterSerializer (admin-excluded role choices), RoleTokenObtainPairSerializer (role claim), password-reset serializers"
      contains: "class RegisterSerializer"
    - path: "get-scouted-be/accounts/views.py"
      provides: "RegisterView, LoginView, PasswordResetRequestView, PasswordResetConfirmView"
      contains: "class RegisterView"
    - path: "get-scouted-be/accounts/urls.py"
      provides: "register/login/token-refresh/logout/password-reset routes"
      contains: "TokenBlacklistView"
  key_links:
    - from: "get-scouted-be/config/urls.py"
      to: "accounts.urls"
      via: "include under api/auth/"
      pattern: "accounts.urls"
    - from: "get-scouted-be/accounts/views.py"
      to: "RoleTokenObtainPairSerializer"
      via: "LoginView.serializer_class"
      pattern: "RoleTokenObtainPairSerializer"
---

<objective>
Build the account lifecycle endpoints: registration (role-restricted, admin excluded),
login (JWT with a role claim embedded for client UX), token refresh with rotation +
blacklisting, real server-side logout via token blacklist, and a console-email password
reset flow. This is the public surface of the single Django auth system (AUTH-01) and
proves logout revocation + single-auth-backend enforcement (AUTH-03).

Purpose: Turn the User model + settings from Plan 01 into a working auth API a client can
register/login/refresh/logout against.

Output: /api/auth/* endpoints with passing registration, auth-flow, and password-reset tests.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-access-control/02-CONTEXT.md
@.planning/phases/02-auth-access-control/02-RESEARCH.md
@.planning/phases/02-auth-access-control/02-VALIDATION.md

# Plan 01 outputs this plan builds on
@get-scouted-be/accounts/models.py
@get-scouted-be/config/settings/base.py
@get-scouted-be/accounts/tests/conftest.py
# URL root to extend
@get-scouted-be/config/urls.py

<interfaces>
<!-- Executor MUST use these exact shapes. Copied from 02-RESEARCH.md Code Examples. -->

accounts/serializers.py — RegisterSerializer + RoleTokenObtainPairSerializer:

    from rest_framework import serializers
    from django.contrib.auth.password_validation import validate_password
    from rest_framework_simplejwt.serializers import TokenObtainPairSerializer
    from accounts.models import User


    class RegisterSerializer(serializers.ModelSerializer):
        password = serializers.CharField(write_only=True, validators=[validate_password])
        role = serializers.ChoiceField(choices=[
            (User.Role.SCOUT, User.Role.SCOUT.label),
            (User.Role.ANALYST, User.Role.ANALYST.label),
            (User.Role.DIRECTOR, User.Role.DIRECTOR.label),
        ])  # admin deliberately excluded from self-service

        class Meta:
            model = User
            fields = ["email", "password", "display_name", "role"]

        def create(self, validated_data):
            return User.objects.create_user(**validated_data)


    class RoleTokenObtainPairSerializer(TokenObtainPairSerializer):
        @classmethod
        def get_token(cls, user):
            token = super().get_token(user)
            token["role"] = user.role
            token["email"] = user.email
            return token

accounts/urls.py — route wiring (mounted under api/auth/ by config/urls.py):

    from django.urls import path
    from rest_framework_simplejwt.views import TokenRefreshView, TokenBlacklistView
    from .views import (RegisterView, LoginView, PasswordResetRequestView,
                        PasswordResetConfirmView)

    urlpatterns = [
        path("register/", RegisterView.as_view(), name="register"),
        path("login/", LoginView.as_view(), name="login"),
        path("token/refresh/", TokenRefreshView.as_view(), name="token_refresh"),
        path("logout/", TokenBlacklistView.as_view(), name="logout"),
        path("password-reset/", PasswordResetRequestView.as_view(), name="password_reset"),
        path("password-reset/confirm/", PasswordResetConfirmView.as_view(), name="password_reset_confirm"),
    ]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registration + login (role-claim JWT) + refresh/logout wiring</name>
  <files>get-scouted-be/accounts/serializers.py, get-scouted-be/accounts/views.py, get-scouted-be/accounts/urls.py, get-scouted-be/config/urls.py, get-scouted-be/accounts/tests/test_registration.py, get-scouted-be/accounts/tests/test_auth_flow.py</files>
  <read_first>
    - get-scouted-be/accounts/models.py (User.Role choices, create_user manager)
    - get-scouted-be/config/settings/base.py (SIMPLE_JWT TOKEN_OBTAIN_SERIALIZER points at accounts.serializers.RoleTokenObtainPairSerializer; DEFAULT_AUTHENTICATION_CLASSES)
    - get-scouted-be/config/urls.py (current urlpatterns to extend)
    - get-scouted-be/accounts/tests/conftest.py (UserFactory, authenticated_client fixtures)
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Pattern 3 role claims; generic-login-failure note; blacklist app usage)
  </read_first>
  <behavior>
    - Register: POST /api/auth/register/ {email, password, display_name, role=scout} -> 201, account is_active True, role persisted.
    - Register accepts role in {scout, analyst, director}; role=admin -> 400 (not a valid choice).
    - Register with a weak password (e.g. "123") -> 400 from Django validators.
    - Login: POST /api/auth/login/ {email, password} (correct) -> 200 with "access" and "refresh" tokens; decoding the access token payload shows a "role" claim equal to the user's role.
    - Login with wrong password OR unknown email -> 401 with the same generic detail (no enumeration).
    - Refresh: POST /api/auth/token/refresh/ {refresh} -> 200 new access+refresh; the OLD refresh token is now blacklisted (a second refresh with it -> 401).
    - Logout: POST /api/auth/logout/ {refresh} -> 200; that refresh token afterward -> 401 on refresh.
    - Single auth backend: settings REST_FRAMEWORK DEFAULT_AUTHENTICATION_CLASSES == exactly ["rest_framework_simplejwt.authentication.JWTAuthentication"].
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    Implement in accounts/serializers.py: RegisterSerializer and RoleTokenObtainPairSerializer
    exactly per the <interfaces> block.

    Implement in accounts/views.py:
      - RegisterView(generics.CreateAPIView): serializer_class = RegisterSerializer;
        permission_classes = [AllowAny] (public — explicitly relax the global IsAuthenticated default).
      - LoginView(TokenObtainPairView): serializer_class = RoleTokenObtainPairSerializer;
        permission_classes = [AllowAny]. Do NOT override the error path — simplejwt's default
        AuthenticationFailed already returns the generic "No active account found with the given
        credentials" message that satisfies the no-enumeration requirement.
      (TokenRefreshView and TokenBlacklistView are used directly from
       rest_framework_simplejwt.views in urls.py — no custom subclass needed. They are AllowAny
       by default at the view level; do not add IsAuthenticated to them.)

    Create accounts/urls.py exactly per the <interfaces> block.

    In config/urls.py add: path("api/auth/", include("accounts.urls")) (import include from
    django.urls). Keep the existing admin/ route.

    Test files:
      - test_registration.py: covers the register behaviors above (scout/analyst/director success,
        admin rejected 400, weak password 400).
      - test_auth_flow.py with named tests: test_login_flow, test_refresh_rotation,
        test_logout_blacklist, test_single_auth_backend. Use the API via
        rest_framework.test.APIClient. Decode the access token with
        rest_framework_simplejwt.tokens.AccessToken to assert the role claim. For
        test_single_auth_backend, assert
        django.conf.settings.REST_FRAMEWORK["DEFAULT_AUTHENTICATION_CLASSES"] equals the single
        JWTAuthentication entry.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_registration.py accounts/tests/test_auth_flow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_registration.py accounts/tests/test_auth_flow.py -x -q` exits 0
    - `grep -q "class RegisterSerializer" get-scouted-be/accounts/serializers.py` succeeds
    - `grep -q "class RoleTokenObtainPairSerializer" get-scouted-be/accounts/serializers.py` succeeds
    - `grep -q "token\[.role.\] = user.role" get-scouted-be/accounts/serializers.py` succeeds (role claim embedded)
    - `grep -q "AllowAny" get-scouted-be/accounts/views.py` succeeds (public endpoints explicitly relax the default)
    - `grep -q "TokenBlacklistView" get-scouted-be/accounts/urls.py` succeeds
    - `grep -q "api/auth/" get-scouted-be/config/urls.py` succeeds
    - `grep -q "def test_single_auth_backend" get-scouted-be/accounts/tests/test_auth_flow.py` succeeds
    - `grep -q "def test_refresh_rotation" get-scouted-be/accounts/tests/test_auth_flow.py` succeeds
    - `grep -q "def test_logout_blacklist" get-scouted-be/accounts/tests/test_auth_flow.py` succeeds
  </acceptance_criteria>
  <done>Register/login/refresh/logout endpoints live under /api/auth/; login returns tokens with a role claim; refresh rotates+blacklists; logout revokes; generic 401 on bad login; single JWT auth backend asserted — all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Password reset request + confirm (console email, Django token generator)</name>
  <files>get-scouted-be/accounts/serializers.py, get-scouted-be/accounts/views.py, get-scouted-be/accounts/urls.py, get-scouted-be/accounts/tests/test_password_reset.py</files>
  <read_first>
    - get-scouted-be/accounts/views.py (current views to extend; AllowAny import already present)
    - get-scouted-be/accounts/serializers.py (existing serializers)
    - get-scouted-be/config/settings/base.py (EMAIL_BACKEND console; PASSWORD_RESET_TIMEOUT default 3 days)
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Password reset code example — default_token_generator + urlsafe_base64_encode/decode; no-enumeration 200 response)
  </read_first>
  <behavior>
    - POST /api/auth/password-reset/ {email} for an existing active user -> 200 generic detail; a reset message is sent via the console email backend (capture with Django's mail.outbox in tests, or assert 200 + token generation).
    - POST /api/auth/password-reset/ {email} for a NON-existent email -> still 200 with the same generic detail (no user enumeration); no error.
    - POST /api/auth/password-reset/confirm/ {uid, token, new_password} with a valid uid+token -> 200; the user's password is changed (old password no longer authenticates, new one does).
    - Confirm with a tampered/invalid token -> 400 "Invalid or expired reset link.".
    - Confirm with a new_password that fails Django validators (e.g. "123") -> 400 with validator messages.
    - After a successful reset, reusing the same token again -> 400 (token is single-use because the password hash changed).
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    Implement PasswordResetRequestView and PasswordResetConfirmView in accounts/views.py as
    APIView subclasses with permission_classes = [AllowAny], following the 02-RESEARCH.md
    "Password reset" code example verbatim in structure:
      - Request view: look up User by email__iexact + is_active=True; if found, build
        uid = urlsafe_base64_encode(force_bytes(user.pk)) and token =
        default_token_generator.make_token(user), then send_mail(...) with the reset link in the
        body (console backend). ALWAYS return 200 with a generic detail regardless of whether the
        email matched.
      - Confirm view: decode uid via urlsafe_base64_decode, fetch the user, validate the token
        with default_token_generator.check_token, run
        django.contrib.auth.password_validation.validate_password(new_password, user=user), then
        user.set_password(new_password) + user.save(update_fields=["password"]). Return 400 on
        invalid link/token or validator failure; 200 on success.
    (Serializers for these are optional — the research example reads request.data directly; if
     you add PasswordResetRequestSerializer/PasswordResetConfirmSerializer for field validation,
     place them in accounts/serializers.py. Either approach is acceptable.)

    The routes password-reset/ and password-reset/confirm/ are already declared in accounts/urls.py
    (Task 1's <interfaces> urls block) — confirm they resolve to these views.

    test_password_reset.py: cover all behaviors above. Use Django's mail.outbox (the console/locmem
    backend populates it under test) to grab the generated uid/token, or generate them directly via
    default_token_generator in the test to drive the confirm endpoint.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_password_reset.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_password_reset.py -x -q` exits 0
    - `grep -q "class PasswordResetRequestView" get-scouted-be/accounts/views.py` succeeds
    - `grep -q "class PasswordResetConfirmView" get-scouted-be/accounts/views.py` succeeds
    - `grep -q "default_token_generator" get-scouted-be/accounts/views.py` succeeds
    - `grep -q "set_password" get-scouted-be/accounts/views.py` succeeds
    - test_password_reset.py contains a test asserting a non-existent email still returns 200 (no enumeration)
  </acceptance_criteria>
  <done>Password reset request + confirm endpoints work via Django's built-in token generator and console email backend; non-existent emails return a generic 200; tokens are single-use; all tests green.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && ./.venv/bin/pytest accounts -x -q` exits 0 (registration + auth flow + password reset + Plan 01 smoke).
- Manual sanity (optional): `curl -X POST localhost:8000/api/auth/register/` shape returns 201/400 as specified.
</verification>

<success_criteria>
- Registration, login (role-claim JWT), refresh (rotate+blacklist), logout (revoke), and password reset endpoints are live under /api/auth/.
- Bad login returns a generic 401; admin role cannot be self-registered.
- Only JWTAuthentication is the configured DRF auth backend (asserted in a test).
- All Plan 02 tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-access-control/02-02-SUMMARY.md`.
</output>

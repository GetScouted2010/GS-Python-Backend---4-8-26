---
phase: 02-auth-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-scouted-be/requirements/base.txt
  - get-scouted-be/config/settings/base.py
  - get-scouted-be/pyproject.toml
  - get-scouted-be/accounts/__init__.py
  - get-scouted-be/accounts/apps.py
  - get-scouted-be/accounts/models.py
  - get-scouted-be/accounts/admin.py
  - get-scouted-be/accounts/migrations/__init__.py
  - get-scouted-be/accounts/migrations/0001_initial.py
  - get-scouted-be/accounts/tests/__init__.py
  - get-scouted-be/accounts/tests/conftest.py
  - get-scouted-be/accounts/tests/test_user_model.py
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "A custom accounts.User model exists with a role field (scout/analyst/director/admin), email as the login identifier, and a UUID primary key"
    - "AUTH_USER_MODEL points to accounts.User and the dev DB was reset so accounts_user (not stock auth_user) is the origin user table"
    - "The dev Postgres DB is repopulated with the full Phase 1 dataset after the reset (Player=41708, Club=1060, Transfer=47201, PlayerRoleScore=230139, PlayerClubCompatibility=8188712)"
    - "Only JWTAuthentication is configured as the DRF auth class and IsAuthenticated is the global default permission (deny-by-default)"
    - "A UserFactory + authenticated_client(role) test fixture exists for downstream auth tests"
  artifacts:
    - path: "get-scouted-be/accounts/models.py"
      provides: "User(AbstractBaseUser, PermissionsMixin) + UserManager with email login and role TextChoices"
      contains: "class User"
      min_lines: 40
    - path: "get-scouted-be/config/settings/base.py"
      provides: "AUTH_USER_MODEL, REST_FRAMEWORK, SIMPLE_JWT, EMAIL_BACKEND, INSTALLED_APPS additions"
      contains: "AUTH_USER_MODEL"
    - path: "get-scouted-be/accounts/migrations/0001_initial.py"
      provides: "Origin migration creating accounts_user"
      contains: "migrations.CreateModel"
    - path: "get-scouted-be/accounts/tests/conftest.py"
      provides: "UserFactory + authenticated_client(role) fixture"
      contains: "authenticated_client"
  key_links:
    - from: "get-scouted-be/config/settings/base.py"
      to: "accounts.User"
      via: "AUTH_USER_MODEL setting"
      pattern: "AUTH_USER_MODEL"
    - from: "get-scouted-be/config/settings/base.py"
      to: "rest_framework_simplejwt.authentication.JWTAuthentication"
      via: "DEFAULT_AUTHENTICATION_CLASSES"
      pattern: "JWTAuthentication"
---

<objective>
Establish the identity foundation for Phase 2: a fully custom `accounts.User` model
(AbstractBaseUser + PermissionsMixin, UUID PK, email login, role field), the DRF +
simplejwt settings wiring, the one-time dev-DB reset that the AUTH_USER_MODEL swap
requires in THIS codebase, and the test fixtures every downstream auth plan reuses.

Purpose: Everything in Plans 02 and 03 (registration, login, permissions, admin
management) depends on this User model, these settings, and these fixtures existing.
This plan replaces the 3 legacy auth models (JWT / disabled API-key / Supabase) with
one Django system of record — the AUTH_USER_MODEL swap IS the reconciliation (AUTH-03),
and no legacy user data is migrated (clean re-registration only).

Output: A migrated `accounts_user` table, a repopulated dev dataset, deny-by-default
DRF settings, and a green smoke test proving the User model works.
</objective>

<execution_context>
@/Users/MAC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/MAC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-access-control/02-CONTEXT.md
@.planning/phases/02-auth-access-control/02-RESEARCH.md
@.planning/phases/02-auth-access-control/02-VALIDATION.md

# UUID-PK precedent to mirror
@get-scouted-be/clubs/models.py
# Settings to extend (split base/local/production, django-environ)
@get-scouted-be/config/settings/base.py
# Full-dataset re-import orchestrator run after the DB reset
@get-scouted-be/core/management/commands/import_all.py

<interfaces>
<!-- Executor MUST use these exact shapes. Copied from 02-RESEARCH.md Code Examples. -->

accounts/models.py — User model (AbstractBaseUser + PermissionsMixin, UUID PK, email login):

    import uuid
    from django.contrib.auth.base_user import AbstractBaseUser, BaseUserManager
    from django.contrib.auth.models import PermissionsMixin
    from django.db import models


    class UserManager(BaseUserManager):
        def create_user(self, email, password=None, **extra_fields):
            if not email:
                raise ValueError("Users must have an email address")
            extra_fields.setdefault("role", User.Role.SCOUT)
            user = self.model(email=self.normalize_email(email), **extra_fields)
            user.set_password(password)
            user.save(using=self._db)
            return user

        def create_superuser(self, email, password=None, **extra_fields):
            extra_fields["role"] = User.Role.ADMIN
            extra_fields.setdefault("is_staff", True)
            extra_fields.setdefault("is_superuser", True)
            return self.create_user(email, password, **extra_fields)


    class User(AbstractBaseUser, PermissionsMixin):
        class Role(models.TextChoices):
            SCOUT = "scout", "Scout"
            ANALYST = "analyst", "Analyst"
            DIRECTOR = "director", "Director"
            ADMIN = "admin", "Admin"

        id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
        email = models.EmailField(unique=True)
        display_name = models.CharField(max_length=150)
        role = models.CharField(max_length=20, choices=Role.choices, default=Role.SCOUT)
        is_active = models.BooleanField(default=True)
        is_staff = models.BooleanField(default=False)
        date_joined = models.DateTimeField(auto_now_add=True)

        objects = UserManager()

        USERNAME_FIELD = "email"
        REQUIRED_FIELDS = ["display_name"]

        def __str__(self):
            return self.email

config/settings/base.py — REST_FRAMEWORK (deny-by-default) + SIMPLE_JWT:

    REST_FRAMEWORK = {
        "DEFAULT_AUTHENTICATION_CLASSES": [
            "rest_framework_simplejwt.authentication.JWTAuthentication",
        ],
        "DEFAULT_PERMISSION_CLASSES": [
            "rest_framework.permissions.IsAuthenticated",
        ],
    }

    # add near top imports: from datetime import timedelta
    SIMPLE_JWT = {
        "ACCESS_TOKEN_LIFETIME": timedelta(minutes=15),
        "REFRESH_TOKEN_LIFETIME": timedelta(days=7),
        "ROTATE_REFRESH_TOKENS": True,
        "BLACKLIST_AFTER_ROTATION": True,
        "UPDATE_LAST_LOGIN": True,
        "AUTH_HEADER_TYPES": ("Bearer",),
        "USER_ID_FIELD": "id",
        "USER_ID_CLAIM": "user_id",
        "TOKEN_OBTAIN_SERIALIZER": "accounts.serializers.RoleTokenObtainPairSerializer",
    }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install simplejwt, wire DRF/JWT/email settings, scaffold accounts app + User model</name>
  <files>get-scouted-be/requirements/base.txt, get-scouted-be/config/settings/base.py, get-scouted-be/pyproject.toml, get-scouted-be/accounts/__init__.py, get-scouted-be/accounts/apps.py, get-scouted-be/accounts/models.py</files>
  <read_first>
    - get-scouted-be/config/settings/base.py (current INSTALLED_APPS, env pattern, DEFAULT_AUTO_FIELD)
    - get-scouted-be/requirements/base.txt (current pins)
    - get-scouted-be/pyproject.toml (current testpaths)
    - get-scouted-be/clubs/models.py (UUID PK pattern to mirror)
    - get-scouted-be/clubs/apps.py (AppConfig pattern)
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Code Examples — SIMPLE_JWT dict, INSTALLED_APPS additions, User model)
  </read_first>
  <action>
    Do NOT run makemigrations/migrate in this task (that is Task 2, after the DB reset).

    1. Install the pinned JWT lib into the project venv:
       cd get-scouted-be && ./.venv/bin/pip install "djangorestframework-simplejwt>=5.5.1,<5.6"
       (5.5.1 is the floor that patches CVE-2024-22513 — do not accept an older cached wheel.)

    2. Append to get-scouted-be/requirements/base.txt exactly this line:
       djangorestframework-simplejwt>=5.5,<5.6

    3. Create the accounts app package:
       - get-scouted-be/accounts/__init__.py (empty file)
       - get-scouted-be/accounts/apps.py with class AccountsConfig(AppConfig):
         default_auto_field = "django.db.models.BigAutoField"; name = "accounts"

    4. Create get-scouted-be/accounts/models.py with the EXACT User + UserManager code from
       the <interfaces> block above (AbstractBaseUser + PermissionsMixin, UUID PK, email
       USERNAME_FIELD, role TextChoices scout/analyst/director/admin, display_name
       CharField(max_length=150), is_active/is_staff/date_joined, UserManager.create_user +
       create_superuser).

    5. In get-scouted-be/config/settings/base.py:
       - Add to INSTALLED_APPS (after the existing "core" entry): "rest_framework",
         "rest_framework_simplejwt.token_blacklist", "accounts".
       - Add AUTH_USER_MODEL = "accounts.User" after INSTALLED_APPS.
       - Add the REST_FRAMEWORK dict exactly as in the <interfaces> block (JWTAuthentication
         as the ONLY auth class; IsAuthenticated as the ONLY default permission — the
         deny-by-default posture AUTH-02/03 require).
       - Add `from datetime import timedelta` to the top imports and the SIMPLE_JWT dict
         exactly as in the <interfaces> block.
       - Add EMAIL_BACKEND = env("EMAIL_BACKEND", default="django.core.mail.backends.console.EmailBackend")
         (console backend per the locked password-reset decision).

    6. In get-scouted-be/pyproject.toml change the testpaths line from
       ["clubs", "players", "transfers", "core"] to
       ["clubs", "players", "transfers", "core", "accounts"].

    Preserve as code comments: SIMPLE_JWT lifetimes 15min/7days are a deliberate discretionary
    bump over simplejwt defaults (5min/1day) for a demo SPA; BLACKLIST_AFTER_ROTATION=True is
    required for ROTATE_REFRESH_TOKENS to actually revoke the prior refresh token.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/python manage.py check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/python manage.py check` exits 0 ("System check identified no issues")
    - `grep -c "djangorestframework-simplejwt>=5.5" get-scouted-be/requirements/base.txt` returns >= 1
    - `grep -q 'AUTH_USER_MODEL = "accounts.User"' get-scouted-be/config/settings/base.py` succeeds
    - `grep -q "rest_framework_simplejwt.token_blacklist" get-scouted-be/config/settings/base.py` succeeds
    - `grep -q "JWTAuthentication" get-scouted-be/config/settings/base.py` succeeds
    - `grep -q "IsAuthenticated" get-scouted-be/config/settings/base.py` succeeds
    - `grep -q "console.EmailBackend" get-scouted-be/config/settings/base.py` succeeds
    - `grep -q '"accounts"' get-scouted-be/pyproject.toml` succeeds (testpaths)
    - `grep -q "class User" get-scouted-be/accounts/models.py` and `grep -q 'USERNAME_FIELD = "email"' get-scouted-be/accounts/models.py` both succeed
    - `grep -q "ROTATE_REFRESH_TOKENS" get-scouted-be/config/settings/base.py` succeeds
  </acceptance_criteria>
  <done>manage.py check passes with AUTH_USER_MODEL=accounts.User, DRF/simplejwt/email settings present, User model defined, testpaths includes accounts — no migrations run yet.</done>
</task>

<task type="auto">
  <name>Task 2: Reset dev DB, create+apply accounts migration, re-import full dataset, register User in admin</name>
  <files>get-scouted-be/accounts/admin.py, get-scouted-be/accounts/migrations/__init__.py, get-scouted-be/accounts/migrations/0001_initial.py</files>
  <read_first>
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Pitfall 1 — the exact DB-reset procedure and why it is mandatory in this codebase)
    - get-scouted-be/core/management/commands/import_all.py (the re-import orchestrator; --skip-compatibility exists but must NOT be used here)
    - get-scouted-be/.env (DATABASE_URL = postgresql://postgres:postgres@localhost:5432/getscouted)
    - get-scouted-be/accounts/models.py (the model whose migration is generated)
  </read_first>
  <action>
    This performs the one-time AUTH_USER_MODEL DB reset that Pitfall 1 documents. The stock
    auth_user table already exists (Phase 1 ran django.contrib.auth migrations, 0 rows) — a
    swappable custom user model requires its own migration to be the ORIGIN of the user table,
    so an in-place ALTER is impossible; a clean re-migrate is required. Safe because
    auth_user/django_admin_log have 0 rows and Phase 1 import commands are idempotent.

    1. Create get-scouted-be/accounts/admin.py registering the User model with a UserAdmin
       subclass of django.contrib.auth.admin.UserAdmin: ordering=("email",);
       list_display=("email","display_name","role","is_active","is_staff");
       search_fields=("email","display_name"); fieldsets grouping email/password, then
       Profile(display_name, role), then Permissions(is_active,is_staff,is_superuser,groups,
       user_permissions), then Dates(last_login,date_joined); add_fieldsets exposing
       email/display_name/role/password1/password2.

    2. Generate the origin migration (creates accounts/migrations/ package if absent):
       cd get-scouted-be && ./.venv/bin/python manage.py makemigrations accounts
       Confirm accounts/migrations/0001_initial.py contains a CreateModel for User.

    3. Reset the dev Postgres DB:
       PGPASSWORD=postgres dropdb -h localhost -U postgres getscouted
       PGPASSWORD=postgres createdb -h localhost -U postgres getscouted
       If dropdb reports the DB is in use, terminate connections first with a
       pg_terminate_backend query against pg_stat_activity WHERE datname='getscouted', then retry.

    4. Migrate fresh (auth_user as a concrete table is never created; accounts_user is the
       user table from the first migration):
       cd get-scouted-be && ./.venv/bin/python manage.py migrate

    5. Repopulate the full real dataset (idempotent per STATE.md). Do NOT pass
       --skip-compatibility — the full ~8.1M-row compatibility import is required to restore
       the real dev dataset (this step takes several minutes):
       cd get-scouted-be && ./.venv/bin/python manage.py import_all
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/python manage.py showmigrations accounts | grep 0001_initial</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/python manage.py showmigrations accounts` shows `[X] 0001_initial` (applied)
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT to_regclass('public.accounts_user')"` prints `accounts_user` (table exists)
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT to_regclass('public.auth_user')"` prints an empty line (stock auth_user table does NOT exist)
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT count(*) FROM players_player"` prints `41708`
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT count(*) FROM clubs_club"` prints `1060`
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT count(*) FROM transfers_transfer"` prints `47201`
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT count(*) FROM players_playerrolescore"` prints `230139`
    - `PGPASSWORD=postgres psql -h localhost -U postgres -d getscouted -tAc "SELECT count(*) FROM players_playerclubcompatibility"` prints `8188712`
    - `grep -q "migrations.CreateModel" get-scouted-be/accounts/migrations/0001_initial.py` succeeds
    - `grep -q "@admin.register(User)" get-scouted-be/accounts/admin.py` succeeds
  </acceptance_criteria>
  <done>accounts.0001_initial applied against a freshly-reset DB; accounts_user exists and auth_user does not; the full Phase 1 dataset is restored (Player=41708, Club=1060, Transfer=47201); User is registered in Django admin.</done>
</task>

<task type="auto">
  <name>Task 3: Test infrastructure — UserFactory + authenticated_client(role) fixture + User model smoke test</name>
  <files>get-scouted-be/accounts/tests/__init__.py, get-scouted-be/accounts/tests/conftest.py, get-scouted-be/accounts/tests/test_user_model.py</files>
  <read_first>
    - get-scouted-be/core/tests/conftest.py (existing fixture-file style/conventions to follow)
    - get-scouted-be/conftest.py (repo-root conftest re-export pattern)
    - get-scouted-be/accounts/models.py (User fields the factory/fixtures build against)
    - .planning/phases/02-auth-access-control/02-RESEARCH.md (Validation Architecture — the authenticated_client fixture shape: RefreshToken.for_user(user) + APIClient().credentials(HTTP_AUTHORIZATION=f"Bearer {token.access_token}"))
  </read_first>
  <action>
    Create the accounts test package and the shared auth fixtures every downstream auth test
    (Plans 02 and 03) will reuse. Follow the existing core/tests/conftest.py style.

    1. get-scouted-be/accounts/tests/__init__.py (empty file).

    2. get-scouted-be/accounts/tests/conftest.py defining:
       - A factory_boy `UserFactory(factory.django.DjangoModelFactory)` for `accounts.models.User`:
         Meta.model = User; email a Sequence like `f"user{n}@example.com"`; display_name a
         Sequence; role default `User.Role.SCOUT`; a post_generation hook that calls
         `obj.set_password("testpass123")` and saves (so login tests can authenticate). Add a
         `django_get_or_create = ("email",)` to keep emails unique across reuse.
       - A pytest fixture `user_factory` returning the `UserFactory` class.
       - A pytest fixture `authenticated_client` that returns a callable
         `def _make(role="scout", **kwargs)`: creates a `UserFactory(role=role, **kwargs)` user,
         builds `refresh = RefreshToken.for_user(user)` (from
         `rest_framework_simplejwt.tokens import RefreshToken`), creates an
         `APIClient()` (from `rest_framework.test`), calls
         `client.credentials(HTTP_AUTHORIZATION=f"Bearer {refresh.access_token}")`, and returns
         `(client, user)`. Mark it so callers can request any role.

    3. get-scouted-be/accounts/tests/test_user_model.py — a smoke test (mark `@pytest.mark.django_db`):
       - `test_create_user_defaults_to_scout`: `User.objects.create_user(email="a@b.com",
         password="testpass123", display_name="A")` returns a user with `role == "scout"`,
         `is_active is True`, `check_password("testpass123")` True, and a UUID `id`.
       - `test_create_superuser_is_admin`: `User.objects.create_superuser(email="admin@b.com",
         password="testpass123", display_name="Admin")` has `role == "admin"`, `is_staff`,
         `is_superuser` all True.
       - `test_email_is_username_field`: `User.USERNAME_FIELD == "email"`.
       - `test_authenticated_client_fixture`: using the `authenticated_client` fixture, request
         a director client and assert the returned user's `role == "director"`.
  </action>
  <verify>
    <automated>cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_user_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd get-scouted-be && ./.venv/bin/pytest accounts/tests/test_user_model.py -x -q` exits 0 with all tests passing
    - `grep -q "class UserFactory" get-scouted-be/accounts/tests/conftest.py` succeeds
    - `grep -q "def authenticated_client" get-scouted-be/accounts/tests/conftest.py` succeeds
    - `grep -q "RefreshToken.for_user" get-scouted-be/accounts/tests/conftest.py` succeeds
    - `grep -q "HTTP_AUTHORIZATION" get-scouted-be/accounts/tests/conftest.py` succeeds
    - `grep -q "def test_create_superuser_is_admin" get-scouted-be/accounts/tests/test_user_model.py` succeeds
  </acceptance_criteria>
  <done>UserFactory + authenticated_client(role) fixtures exist and a green smoke test proves the User model, manager, and fixtures work end to end.</done>
</task>

</tasks>

<verification>
- `cd get-scouted-be && ./.venv/bin/python manage.py check` exits 0.
- `cd get-scouted-be && ./.venv/bin/pytest accounts -x -q` exits 0 (only the smoke test exists at this plan's close; more added in Plans 02/03).
- `showmigrations accounts` shows `[X] 0001_initial`; `accounts_user` table exists, `auth_user` does not.
- Full dataset restored: Player=41708, Club=1060, Transfer=47201, PlayerRoleScore=230139, PlayerClubCompatibility=8188712.
</verification>

<success_criteria>
- Custom `accounts.User` model (UUID PK, email login, role scout/analyst/director/admin) is the project's `AUTH_USER_MODEL`.
- DRF is wired with JWTAuthentication as the only auth class and IsAuthenticated as the global default (deny-by-default).
- simplejwt installed (>=5.5.1) with rotation + blacklist app configured; EMAIL_BACKEND console.
- Dev DB reset and full Phase 1 dataset re-imported.
- Shared auth test fixtures (UserFactory, authenticated_client) available for Plans 02/03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-access-control/02-01-SUMMARY.md`.
</output>

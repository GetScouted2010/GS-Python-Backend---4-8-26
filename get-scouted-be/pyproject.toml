[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings.local"
python_files = ["test_*.py"]
testpaths = ["clubs", "players", "transfers", "core", "accounts", "scoring", "workspace"]
markers = ["integration: tests that run against real/large CSV samples (slower)"]
addopts = "-ra"
